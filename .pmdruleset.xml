<?xml version="1.0" encoding="UTF-8"?>
<ruleset xmlns="http://pmd.sourceforge.net/ruleset/2.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         name="M2Eclipse PMD RuleSet"
         xsi:schemaLocation="http://pmd.sourceforge.net/ruleset/2.0.0 https://pmd.sourceforge.io/ruleset_2_0_0.xsd">
   <description>M2Eclipse PMD RuleSet</description>
   <exclude-pattern>.*C:/Develop/realworld-backend-spring-ddd/src/test/java.*</exclude-pattern>
   <exclude-pattern>.*C:/Develop/realworld-backend-spring-ddd/target.*</exclude-pattern>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AbstractClassWithoutAbstractMethodRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#abstractclasswithoutabstractmethod"
         language="java"
         message="This abstract class does not have any abstract methods"
         name="AbstractClassWithoutAbstractMethod"
         since="3.0">
      <description>
The abstract class does not contain any abstract methods. An abstract class suggests
an incomplete implementation, which is to be completed by subclasses implementing the
abstract methods. If the class is intended to be used as a base class only (not to be instantiated
directly) a protected constructor can be provided to prevent direct instantiation.
        </description>
      <priority>3</priority>
      <example>

public abstract class Foo {
  void int method1() { ... }
  void int method2() { ... }
  // consider using abstract methods or removing
  // the abstract modifier and adding protected constructors
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AccessorClassGenerationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#accessorclassgeneration"
         language="java"
         maximumLanguageVersion="10"
         message="Avoid instantiation through private constructors from outside of the constructor's class."
         name="AccessorClassGeneration"
         since="1.04">
      <description>
Instantiation by way of private constructors from outside the constructor's class often causes the
generation of an accessor. A factory method, or non-privatization of the constructor can eliminate this
situation. The generated class file is actually an interface.  It gives the accessing class the ability
to invoke a new hidden package scope constructor that takes the interface as a supplementary parameter.
This turns a private constructor effectively into one with package scope, and is challenging to discern.

_Note:_ This rule is only executed for Java 10 or lower.
Since Java 11, [JEP 181: Nest-Based Access Control](https://openjdk.org/jeps/181) has been implemented. This
means that in Java 11 and above accessor classes are not generated anymore.
        </description>
      <priority>3</priority>
      <example>

public class Outer {
 void method(){
  Inner ic = new Inner();//Causes generation of accessor class
 }
 public class Inner {
  private Inner(){}
 }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AccessorMethodGenerationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#accessormethodgeneration"
         language="java"
         maximumLanguageVersion="10"
         message="Consider giving this member package visibility to access it from {0} without a synthetic accessor method"
         name="AccessorMethodGeneration"
         since="5.5.4">
      <description>
When accessing private fields / methods from another class, the Java compiler will generate accessor methods
with package-private visibility. This adds overhead, and to the dex method count on Android. This situation can
be avoided by changing the visibility of the field / method from private to package-private.


_Note:_ This rule is only executed for Java 10 or lower.
Since Java 11, [JEP 181: Nest-Based Access Control](https://openjdk.org/jeps/181) has been implemented. This
means that in Java 11 and above accessor classes are not generated anymore.
        </description>
      <priority>3</priority>
      <example>

public class OuterClass {
    private int counter;
    /* package */ int id;

    public class InnerClass {
        InnerClass() {
            OuterClass.this.counter++; // wrong accessor method will be generated
        }

        public int getOuterClassId() {
            return OuterClass.this.id; // id is package-private, no accessor method needed
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.ArrayIsStoredDirectlyRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#arrayisstoreddirectly"
         language="java"
         message="The user-supplied array ''{0}'' is stored directly."
         name="ArrayIsStoredDirectly"
         since="2.2">
      <description>
Constructors and methods receiving arrays should clone objects and store the copy.
This prevents future changes from the user from affecting the original array.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    private String [] x;
        public void foo (String [] param) {
        // Don't do this, make a copy of the array at least
        this.x=param;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidmessagedigestfield"
         language="java"
         message="You shouldn't declare field of MessageDigest type, because unsynchronized access could cause problems"
         name="AvoidMessageDigestField"
         since="6.18.0">
      <description>
            Declaring a MessageDigest instance as a field make this instance directly available to multiple threads.
            Such sharing of MessageDigest instances should be avoided if possible since it leads to wrong results
            if the access is not synchronized correctly.
            Just create a new instance and use it locally, where you need it.
            Creating a new instance is easier than synchronizing access to a shared instance.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FieldDeclaration/ClassType[pmd-java:typeIs('java.security.MessageDigest')]"/>
      </properties>
      <example>
            
import java.security.MessageDigest;
public class AvoidMessageDigestFieldExample {
    private final MessageDigest sharedMd;
    public AvoidMessageDigestFieldExample() throws Exception {
        sharedMd = MessageDigest.getInstance("SHA-256");
    }
    public byte[] calculateHashShared(byte[] data) {
        // sharing a MessageDigest like this without synchronizing access
        // might lead to wrong results
        sharedMd.reset();
        sharedMd.update(data);
        return sharedMd.digest();
    }

    // better
    public byte[] calculateHash(byte[] data) throws Exception {
        MessageDigest md = MessageDigest.getInstance("SHA-256");
        md.update(data);
        return md.digest();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidprintstacktrace"
         language="java"
         message="Avoid printStackTrace(); use a logger call instead."
         name="AvoidPrintStackTrace"
         since="3.2">
      <description>
Avoid printStackTrace(); use a logger call instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[ pmd-java:matchesSig(&#34;java.lang.Throwable#printStackTrace()&#34;) ]"/>
      </properties>
      <example>

class Foo {
    void bar() {
        try {
            // do something
        } catch (Exception e) {
            e.printStackTrace();
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AvoidReassigningCatchVariablesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidreassigningcatchvariables"
         language="java"
         message="Avoid reassigning caught exception ''{0}''"
         name="AvoidReassigningCatchVariables"
         since="6.27.0">
      <description>
Reassigning exception variables caught in a catch statement should be avoided because of:

1) If it is needed, multi catch can be easily added and code will still compile.

2) Following the principle of least surprise we want to make sure that a variable caught in a catch statement
is always the one thrown in a try block.
        </description>
      <priority>3</priority>
      <example>
public class Foo {
    public void foo() {
        try {
            // do something
        } catch (Exception e) {
            e = new NullPointerException(); // not recommended
        }

        try {
            // do something
        } catch (MyException | ServerException e) {
            e = new RuntimeException(); // won't compile
        }
    }
}
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AvoidReassigningLoopVariablesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidreassigningloopvariables"
         language="java"
         message="Avoid reassigning the loop control variable ''{0}''"
         name="AvoidReassigningLoopVariables"
         since="6.11.0">
      <description>
Reassigning loop variables can lead to hard-to-find bugs. Prevent or limit how these variables can be changed.

In foreach-loops, configured by the `foreachReassign` property:
- `deny`: Report any reassignment of the loop variable in the loop body. _This is the default._
- `allow`: Don't check the loop variable.
- `firstOnly`: Report any reassignments of the loop variable, except as the first statement in the loop body.
            _This is useful if some kind of normalization or clean-up of the value before using is permitted, but any other change of the variable is not._

In for-loops, configured by the `forReassign` property:
- `deny`: Report any reassignment of the control variable in the loop body. _This is the default._
- `allow`: Don't check the control variable.
- `skip`: Report any reassignments of the control variable, except conditional increments/decrements (`++`, `--`, `+=`, `-=`).
            _This prevents accidental reassignments or unconditional increments of the control variable._
        </description>
      <priority>3</priority>
      <example>

public class Foo {
  private void foo() {
    for (String s : listOfStrings()) {
      s = s.trim(); // OK, when foreachReassign is "firstOnly" or "allow"
      doSomethingWith(s);

      s = s.toUpper(); // OK, when foreachReassign is "allow"
      doSomethingElseWith(s);
    }

    for (int i=0; i &lt; 10; i++) {
      if (check(i)) {
        i++; // OK, when forReassign is "skip" or "allow"
      }

      i = 5;  // OK, when forReassign is "allow"

      doSomethingWith(i);
    }
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AvoidReassigningParametersRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidreassigningparameters"
         language="java"
         message="Avoid reassigning parameters such as ''{0}''"
         name="AvoidReassigningParameters"
         since="1.0">
      <description>
Reassigning values to incoming parameters of a method or constructor is not recommended, as this can
make the code more difficult to understand. The code is often read with the assumption that parameter values
don't change and an assignment violates therefore the principle of least astonishment. This is especially a
problem if the parameter is documented e.g. in the method's javadoc and the new content differs from the original
documented content.

Use temporary local variables instead. This allows you to assign a new name, which makes the code better
understandable.

Note that this rule considers both methods and constructors. If there are multiple assignments for a formal
parameter, then only the first assignment is reported.
        </description>
      <priority>2</priority>
      <example>

public class Hello {
  private void greet(String name) {
    name = name.trim();
    System.out.println("Hello " + name);

    // preferred
    String trimmedName = name.trim();
    System.out.println("Hello " + trimmedName);
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidstringbufferfield"
         language="java"
         message="StringBuffers can grow quite a lot, and so may become a source of memory leak (if the owning class has a long life time)."
         name="AvoidStringBufferField"
         since="4.2">
      <description>
StringBuffers/StringBuilders can grow considerably, and so may become a source of memory leaks
if held within objects with long lifetimes.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FieldDeclaration/ClassType[pmd-java:typeIs('java.lang.StringBuffer') or pmd-java:typeIs('java.lang.StringBuilder')]"/>
      </properties>
      <example>

public class Foo {
    private StringBuffer buffer;    // potential memory leak as an instance variable;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.AvoidUsingHardCodedIPRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#avoidusinghardcodedip"
         language="java"
         message="Do not hard code the IP address ${variableName}"
         name="AvoidUsingHardCodedIP"
         since="4.1">
      <description>
Application with hard-coded IP addresses can become impossible to deploy in some cases.
Externalizing IP addresses is preferable.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    private String ip = "127.0.0.1";     // not recommended
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.CheckResultSetRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#checkresultset"
         language="java"
         message="Always check the return of one of the navigation method (next,previous,first,last) of a ResultSet."
         name="CheckResultSet"
         since="4.1">
      <description>
Always check the return values of navigation methods (next, previous, first, last) of a ResultSet.
If the value return is 'false', it should be handled properly.
        </description>
      <priority>3</priority>
      <example>

Statement stat = conn.createStatement();
ResultSet rst = stat.executeQuery("SELECT name FROM person");
rst.next();     // what if it returns false? bad form
String firstName = rst.getString(1);

Statement stat = conn.createStatement();
ResultSet rst = stat.executeQuery("SELECT name FROM person");
if (rst.next()) {    // result is properly examined and used
    String firstName = rst.getString(1);
    } else  {
        // handle missing data
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#constantsininterface"
         language="java"
         message="Using constants in interfaces is a bad practice."
         name="ConstantsInInterface"
         since="5.5">
      <description>
Using constants in interfaces is a bad practice. Interfaces define types, constants are implementation details better placed in classes or enums. If the constants are best viewed as members of an enumerated type, you should export them with an enum type.
For other scenarios, consider using a utility class. See Effective Java's 'Use interfaces only to define types'.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[@Interface = true()][$ignoreIfHasMethods= false() or not(ClassBody/MethodDeclaration)]/ClassBody/FieldDeclaration"/>
         <property description="Whether to ignore constants in interfaces if the interface defines any methods"
                   name="ignoreIfHasMethods"
                   type="Boolean">
            <value>true</value>
         </property>
      </properties>
      <example>

public interface ConstantInterface {
    public static final int CONST1 = 1; // violation, no fields allowed in interface!
    static final int CONST2 = 1;        // violation, no fields allowed in interface!
    final int CONST3 = 1;               // violation, no fields allowed in interface!
    int CONST4 = 1;                     // violation, no fields allowed in interface!
}

// with ignoreIfHasMethods = false
public interface AnotherConstantInterface {
    public static final int CONST1 = 1; // violation, no fields allowed in interface!

    int anyMethod();
}

// with ignoreIfHasMethods = true
public interface YetAnotherConstantInterface {
    public static final int CONST1 = 1; // no violation

    int anyMethod();
}
 
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#defaultlabelnotlastinswitch"
         language="java"
         message="The default label should be the last label in a switch statement or expression"
         name="DefaultLabelNotLastInSwitch"
         since="1.5">
      <description>
By convention, the default label should be the last label in a switch statement or switch expression.

Note: This rule has been renamed from "DefaultLabelNotLastInSwitchStmt" with PMD 7.7.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//SwitchLabel[@Default = true() and not(.. is ../../*[last()])]"/>
      </properties>
      <example>

public class Foo {
  void bar(int a) {
   switch (a) {
    case 1:  // do something
       break;
    default:  // the default case should be last, by convention
       break;
    case 2:
       break;
   }
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#doublebraceinitialization"
         language="java"
         message="Double-brace initialization should be avoided"
         name="DoubleBraceInitialization"
         since="6.16.0">
      <description>
            Double brace initialisation is a pattern to initialise eg collections concisely. But it implicitly
            generates a new .class file, and the object holds a strong reference to the enclosing object. For those
            reasons, it is preferable to initialize the object normally, even though it's verbose.

            This rule counts any anonymous class which only has a single initializer as an instance of double-brace
            initialization. There is currently no way to find out whether a method called in the initializer is not
            accessible from outside the anonymous class, and those legit cases should be suppressed for the time being.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ConstructorCall/AnonymousClassDeclaration/ClassBody[count(*)=1]/Initializer[@Static=false()]"/>
      </properties>
      <example>
// this is double-brace initialization
return new ArrayList&lt;String&gt;(){{
    add("a");
    add("b");
    add("c");
}};

// the better way is to not create an anonymous class:
List&lt;String&gt; a = new ArrayList&lt;&gt;();
a.add("a");
a.add("b");
a.add("c");
return a;

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#exhaustiveswitchhasdefault"
         language="java"
         message="The switch block is exhaustive even without the default case"
         name="ExhaustiveSwitchHasDefault"
         since="7.10.0">
      <description>
When switching over an enum or sealed class, the compiler will ensure that all possible cases are covered.
If a case is missing, this will result in a compilation error. But if a default case is added, this compiler
check is not performed anymore, leading to difficulties in noticing bugs at runtime.

Not using a default case makes sure, a compiler error is introduced whenever a new enum constant or a
new subclass to the sealed class hierarchy is added. We will discover this problem at compile time
rather than at runtime (if at all).

Note: The fix it not necessarily just removing the default case. Maybe a case is missing which needs to be implemented.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//(SwitchStatement | SwitchExpression)&#xA;                      [@Exhaustive = true()]&#xA;                      [@DefaultCase = true()]"/>
      </properties>
      <example>

class Foo {
    enum MyEnum { A, B };

    void doSomething(MyEnum e) {
        switch(e) {
            case A -&gt; System.out.println("a");
            case B -&gt; System.out.println("b");
            default -&gt; System.out.println("unnecessary default");
        };
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.ForLoopCanBeForeachRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#forloopcanbeforeach"
         language="java"
         message="This 'for' loop can be replaced by a 'foreach' loop"
         minimumLanguageVersion="1.5"
         name="ForLoopCanBeForeach"
         since="6.0.0">
      <description>
Reports loops that can be safely replaced with the foreach syntax. The rule considers loops over
lists, arrays and iterators. A loop is safe to replace if it only uses the index variable to
access an element of the list or array, only has one update statement, and loops through *every*
element of the list or array left to right.
        </description>
      <priority>3</priority>
      <example>

public class MyClass {
  void loop(List&lt;String&gt; l) {
    for (int i = 0; i &lt; l.size(); i++) { // pre Java 1.5
      System.out.println(l.get(i));
    }

    for (String s : l) {        // post Java 1.5
      System.out.println(s);
    }
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#forloopvariablecount"
         language="java"
         message="Too many control variables in the 'for' statement"
         name="ForLoopVariableCount"
         since="6.11.0">
      <description>
Having a lot of control variables in a 'for' loop makes it harder to see what range of values
the loop iterates over. By default this rule allows a regular 'for' loop with only one variable.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ForInit/LocalVariableDeclaration[count(VariableDeclarator) &gt; $maximumVariables]"/>
         <property description="A regular for statement will have 1 control variable"
                   max="100"
                   min="0"
                   name="maximumVariables"
                   type="Integer">
            <value>1</value>
         </property>
      </properties>
      <example>

// this will be reported with the default setting of at most one control variable in a for loop
for (int i = 0, j = 0; i &lt; 10; i++, j += 2) {
   foo();

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.GuardLogStatementRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#guardlogstatement"
         language="java"
         message="Logger calls should be surrounded by log level guards."
         name="GuardLogStatement"
         since="5.1.0">
      <description>
Whenever using a log level, one should check if it is actually enabled, or
otherwise skip the associate String creation and manipulation, as well as any method calls.

An alternative to checking the log level are substituting parameters, formatters or lazy logging
with lambdas. The available alternatives depend on the actual logging framework.
        </description>
      <priority>2</priority>
      <example>

// Add this for performance - avoid manipulating strings if the logger may drop it
if (log.isDebugEnabled()) {
    log.debug("log something" + param1 + " and " + param2 + "concat strings");
}

// Avoid the guarding if statement with substituting parameters
log.debug("log something {} and {}", param1, param2);

// Avoid the guarding if statement with formatters
log.debug("log something %s and %s", param1, param2);

// This is still an issue, method invocations may be expensive / have side-effects
log.debug("log something expensive: {}", calculateExpensiveLoggingText());

// Avoid the guarding if statement with lazy logging and lambdas
log.debug("log something expensive: {}", () -&gt; calculateExpensiveLoggingText());

// … alternatively use method references
log.debug("log something expensive: {}", this::calculateExpensiveLoggingText);

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.ImplicitFunctionalInterfaceRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#implicitfunctionalinterface"
         language="java"
         message="Annotate this interface with @FunctionalInterface or with @SuppressWarnings(&#34;PMD.ImplicitFunctionalInterface&#34;) to clarify your intent."
         name="ImplicitFunctionalInterface"
         since="7.12.0">
      <description>
            Reports functional interfaces that were not explicitly declared as such with
            the annotation `@FunctionalInterface`. If an interface is accidentally a functional
            interface, then it should bear a `@SuppressWarnings("PMD.ImplicitFunctionalInterface")`
            annotation to make this clear.
        </description>
      <priority>2</priority>
      <example>
            
            // The intent on this declaration is unclear, and the rule will report it.
            public interface MyInterface {
                void doSomething();
            }

            // This is clearly intended as a functional interface.
            @FunctionalInterface
            public interface MyInterface {
                void doSomething();
            }

            // This is clearly NOT intended as a functional interface.
            @SuppressWarnings("PMD.ImplicitFunctionalInterface")
            public interface MyInterface {
                void doSomething();
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#junit4suitesshouldusesuiteannotation"
         language="java"
         message="JUnit 4 indicates test suites via annotations, not the suite method."
         name="JUnit4SuitesShouldUseSuiteAnnotation"
         since="4.0">
      <description>
In JUnit 3, test suites are indicated by the suite() method. In JUnit 4, suites are indicated
through the @RunWith(Suite.class) annotation.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name='suite' and ClassType[pmd-java:typeIs('junit.framework.Test')]]&#xA;                   [not(.//ReturnStatement/*[pmd-java:typeIs('junit.framework.JUnit4TestAdapter')])]"/>
      </properties>
      <example>

public class BadExample extends TestCase{

    public static Test suite(){
        return new Suite();
    }
}

@RunWith(Suite.class)
@SuiteClasses( { TestOne.class, TestTwo.class })
public class GoodTest {
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#junit5testshouldbepackageprivate"
         language="java"
         message="JUnit 5 tests should be package-private."
         name="JUnit5TestShouldBePackagePrivate"
         since="6.35.0">
      <description>
Reports JUnit 5 test classes and methods that are not package-private.
Contrary to JUnit 4 tests, which required public visibility to be run by the engine,
JUnit 5 tests can also be run if they're package-private. Marking them as such
is a good practice to limit their visibility.

Test methods are identified as those which use `@Test`, `@RepeatedTest`,
`@TestFactory`, `@TestTemplate` or `@ParameterizedTest`.
            </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[&#xA;    (: a Junit 5 test class, ie, it has methods with the annotation :)&#xA;    @Interface = false() and&#xA;    ClassBody/MethodDeclaration&#xA;    [ModifierList/Annotation[&#xA;               pmd-java:typeIs('org.junit.jupiter.api.Test')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.RepeatedTest')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.TestFactory')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.TestTemplate')&#xA;            or pmd-java:typeIs('org.junit.jupiter.params.ParameterizedTest')&#xA;    ]]&#xA;]/(&#xA;       self::*[@Abstract = false() and @Visibility = (&#34;public&#34;, &#34;protected&#34;)]&#xA;|      ClassBody/MethodDeclaration&#xA;       [@Visibility = (&#34;public&#34;, &#34;protected&#34;)]&#xA;       [ModifierList/Annotation[&#xA;               pmd-java:typeIs('org.junit.jupiter.api.Test')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.RepeatedTest')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.TestFactory')&#xA;            or pmd-java:typeIs('org.junit.jupiter.api.TestTemplate')&#xA;            or pmd-java:typeIs('org.junit.jupiter.params.ParameterizedTest')&#xA;       ]]&#xA;)"/>
      </properties>
      <example>
            
class MyTest { // not public, that's fine
    @Test
    public void testBad() { } // should not have a public modifier

    @Test
    protected void testAlsoBad() { } // should not have a protected modifier

    @Test
    private void testNoRun() { } // should not have a private modifier

    @Test
    void testGood() { } // package private as expected
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.JUnitUseExpectedRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#junituseexpected"
         language="java"
         message="In JUnit4, use the @Test(expected) annotation to denote tests that should throw exceptions"
         name="JUnitUseExpected"
         since="4.0">
      <description>
In JUnit4, use the @Test(expected) annotation to denote tests that should throw exceptions.
        </description>
      <priority>3</priority>
      <example>

public class MyTest {
    @Test
    public void testBad() {
        try {
            doSomething();
            fail("should have thrown an exception");
        } catch (Exception e) {
        }
    }

    @Test(expected=Exception.class)
    public void testGood() {
        doSomething();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.LiteralsFirstInComparisonsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#literalsfirstincomparisons"
         language="java"
         message="Position literals first in String comparisons"
         name="LiteralsFirstInComparisons"
         since="6.24.0">
      <description>
            Position literals first in all String comparisons, if the second argument is null then NullPointerExceptions
            can be avoided, they will just return false. Note that switching literal positions for compareTo and
            compareToIgnoreCase may change the result, see examples.

            Note that compile-time constant strings are treated like literals. This is because they are inlined into
            the class file, are necessarily non-null, and therefore cannot cause an NPE at runtime.
        </description>
      <priority>3</priority>
      <example>
            
class Foo {
    boolean bar(String x) {
        return x.equals("2"); // should be "2".equals(x)
    }
    boolean bar(String x) {
        return x.equalsIgnoreCase("2"); // should be "2".equalsIgnoreCase(x)
    }
    boolean bar(String x) {
        return (x.compareTo("bar") &gt; 0); // should be: "bar".compareTo(x) &lt; 0
    }
    boolean bar(String x) {
        return (x.compareToIgnoreCase("bar") &gt; 0); // should be: "bar".compareToIgnoreCase(x) &lt; 0
    }
    boolean bar(String x) {
        return x.contentEquals("bar"); // should be "bar".contentEquals(x)
    }

    static final String CONSTANT = "const";
    {
        CONSTANT.equals("literal"); // not reported, this is effectively the same as writing "const".equals("foo")
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.LooseCouplingRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#loosecoupling"
         language="java"
         message="Avoid using implementation types like ''{0}''; use the interface instead"
         name="LooseCoupling"
         since="0.7">
      <description>
Excessive coupling to implementation types (e.g., `HashSet`) limits your ability to use alternate
implementations in the future as requirements change. Whenever available, declare variables
and parameters using a more general type (e.g, `Set`).

This rule reports uses of concrete collection types. User-defined types that should be treated
the same as interfaces can be configured with the property `allowedTypes`.
        </description>
      <priority>3</priority>
      <example>

import java.util.ArrayList;
import java.util.HashSet;

public class Bar {
    // sub-optimal approach
    private ArrayList&lt;SomeType&gt; list = new ArrayList&lt;&gt;();

    public HashSet&lt;SomeType&gt; getFoo() {
        return new HashSet&lt;SomeType&gt;();
    }

    // preferred approach
    private List&lt;SomeType&gt; list = new ArrayList&lt;&gt;();

    public Set&lt;SomeType&gt; getFoo() {
        return new HashSet&lt;SomeType&gt;();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.MethodReturnsInternalArrayRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#methodreturnsinternalarray"
         language="java"
         message="Returning ''{0}'' may expose an internal array."
         name="MethodReturnsInternalArray"
         since="2.2">
      <description>
Exposing internal arrays to the caller violates object encapsulation since elements can be
removed or replaced outside of the object that owns it. It is safer to return a copy of the array.
        </description>
      <priority>3</priority>
      <example>

public class SecureSystem {
    UserData [] ud;
    public UserData [] getUserData() {
        // Don't return directly the internal array, return a copy
        return ud;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.MissingOverrideRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#missingoverride"
         language="java"
         message="The method ''{0}'' is missing an @Override annotation."
         minimumLanguageVersion="1.5"
         name="MissingOverride"
         since="6.2.0">
      <description>
            Annotating overridden methods with @Override ensures at compile time that
            the method really overrides one, which helps refactoring and clarifies intent.
        </description>
      <priority>3</priority>
      <example>
            
            public class Foo implements Runnable {
                // This method is overridden, and should have an @Override annotation
                public void run() {

                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#nonexhaustiveswitch"
         language="java"
         message="Switch statements or expressions should be exhaustive, add a default case (or missing enum branches)"
         name="NonExhaustiveSwitch"
         since="1.0">
      <description>
            Switch statements should be exhaustive, to make their control flow
            easier to follow. This can be achieved by adding a `default` case, or,
            if the switch is on an enum type, by ensuring there is one switch branch
            for each enum constant.

            This rule doesn't consider Switch Statements, that use Pattern Matching, since for these the
            compiler already ensures that all cases are covered. The same is true for Switch Expressions,
            which are also not considered by this rule.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//SwitchStatement&#xA;                    (: exclude empty switches :)&#xA;                    [count(*) &gt; 1]&#xA;                    [@DefaultCase = false()]&#xA;                    [@ExhaustiveEnumSwitch = false()]&#xA;                    (: exclude pattern tests - for these, the compiler will ensure exhaustiveness :)&#xA;                    [not(*/SwitchLabel[@PatternLabel = true()])]"/>
      </properties>
      <example>
            
class Foo {{
    int x = 2;
    switch (x) {
      case 1: int j = 6;
      case 2: int j = 8;
      // missing default: here
    }
}}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#onedeclarationperline"
         language="java"
         message="Use one line for each declaration, it enhances code readability."
         name="OneDeclarationPerLine"
         since="5.0">
      <description>
Java allows the use of several variables declaration of the same type on one line.
However, it can lead to quite messy code. This rule looks for several declarations on the same line.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//LocalVariableDeclaration&#xA;   [not(parent::ForInit)]&#xA;   [count(VariableDeclarator) &gt; 1]&#xA;   [$strictMode or count(distinct-values(VariableDeclarator/@BeginLine)) != count(VariableDeclarator)]&#xA;|&#xA;//FieldDeclaration&#xA;   [count(VariableDeclarator) &gt; 1]&#xA;   [$strictMode or count(distinct-values(VariableDeclarator/@BeginLine)) != count(VariableDeclarator)]"/>
         <property description="If true, mark combined declaration even if the declarations are on separate lines."
                   name="strictMode"
                   type="Boolean">
            <value>false</value>
         </property>
      </properties>
      <example>

String name;            // separate declarations
String lastname;

String name, lastname;  // combined declaration, a violation

String name,
       lastname;        // combined declaration on multiple lines, no violation by default.
                        // Set property strictMode to true to mark this as violation.

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.PreserveStackTraceRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#preservestacktrace"
         language="java"
         message="Thrown exception does not preserve the stack trace of exception ''{0}'' on all code paths"
         name="PreserveStackTrace"
         since="3.7">
      <description>
Reports exceptions that are thrown from within a catch block, yet don't refer to the
exception parameter declared by that catch block. The stack trace of the original
exception could be lost, which makes the thrown exception less informative.

To preserve the stack trace, the original exception may be used as the cause of
the new exception, using `Throwable#initCause`, or passed as a constructor argument
to the new exception. It may also be preserved using `Throwable#addSuppressed`.
The rule actually assumes that any method or constructor that takes the original
exception as argument preserves the original stack trace.

The rule allows `InvocationTargetException` and `PrivilegedActionException` to be
replaced by their cause exception. The discarded part of the stack trace is in those
cases only JDK-internal code, which is not very useful. The rule also ignores exceptions
whose name starts with `ignored`.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    void good() {
        try{
            Integer.parseInt("a");
        } catch (Exception e) {
            throw new Exception(e); // Ok, this initializes the cause of the new exception
        }
        try {
            Integer.parseInt("a");
        } catch (Exception e) {
            throw (IllegalStateException)new IllegalStateException().initCause(e); // second possibility to create exception chain.
        }
    }
    void wrong() {
        try{
            Integer.parseInt("a");
        } catch (Exception e) {
            // Violation: this only preserves the message and not the stack trace
            throw new Exception(e.getMessage());
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.PrimitiveWrapperInstantiationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#primitivewrapperinstantiation"
         language="java"
         message="Do not use `new {0}(...)`, prefer `{0}.valueOf(...)`"
         name="PrimitiveWrapperInstantiation"
         since="6.37.0">
      <description>
            Reports usages of primitive wrapper constructors. They are deprecated
            since Java 9 and should not be used. Even before Java 9, they can
            be replaced with usage of the corresponding static `valueOf` factory method
            (which may be automatically inserted by the compiler since Java 1.5).
            This has the advantage that it may reuse common instances instead of creating
            a new instance each time.

            Note that for `Boolean`, the named constants `Boolean.TRUE` and `Boolean.FALSE`
            are preferred instead of `Boolean.valueOf`.
        </description>
      <priority>3</priority>
      <example>

            public class Foo {
                private Integer ZERO = new Integer(0);      // violation
                private Integer ZERO1 = Integer.valueOf(0); // better
                private Integer ZERO1 = 0;                  // even better
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.RelianceOnDefaultCharsetRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#relianceondefaultcharset"
         language="java"
         message="Specify a character set instead of relying on the default charset"
         name="RelianceOnDefaultCharset"
         since="7.17.0">
      <description>
Be sure to specify a character set for APIs that use the JVM's default character set to ensure
stable encoding behavior between different JVMs, programs, and servers. Using the platform's
default charset makes the code less portable and might lead to unexpected behavior when running
on different systems.

Additional, since Java 18, the default charset for these APIs is consistently UTF-8
(see [JEP 400](https://openjdk.org/jeps/400)). While this reduces unexpected behavior
on different systems, it is still advised to explicitly specify a character set,
especially if UTF-8 is not the desired charset.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    void bad() throws IOException {
        new InputStreamReader(inputStream);  // violation
        new OutputStreamWriter(outputStream);  // violation
        URLEncoder.encode("test string");  // violation (deprecated)
        new PrintStream(outputStream);  // violation
        new PrintWriter("output.txt");  // violation
        new Scanner(inputStream);  // violation
        new Formatter();  // violation
        "test".getBytes();  // violation
        new ByteArrayOutputStream().toString();  // violation
        new FileReader("input.txt");  // violation
        new FileWriter("output.txt");  // violation
    }

    void good() throws IOException {
        new InputStreamReader(inputStream, StandardCharsets.UTF_8);  // ok
        new OutputStreamWriter(outputStream, StandardCharsets.UTF_8);  // ok
        URLEncoder.encode("test string", StandardCharsets.UTF_8);  // ok
        new PrintStream(outputStream, true, StandardCharsets.UTF_8);  // ok
        new PrintWriter("output.txt", StandardCharsets.UTF_8);  // ok
        new Scanner(inputStream, StandardCharsets.UTF_8);  // ok
        new Formatter(Locale.US);  // ok
        "test".getBytes(StandardCharsets.UTF_8);  // ok
        new ByteArrayOutputStream().toString(StandardCharsets.UTF_8);  // ok
        new FileReader("input.txt", StandardCharsets.UTF_8);  // ok
        new FileWriter("output.txt", StandardCharsets.UTF_8);  // ok
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#replaceenumerationwithiterator"
         language="java"
         message="Consider replacing this Enumeration with the newer java.util.Iterator"
         name="ReplaceEnumerationWithIterator"
         since="3.4">
      <description>
Consider replacing Enumeration usages with the newer java.util.Iterator
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ImplementsList/ClassType[pmd-java:typeIsExactly('java.util.Enumeration')]"/>
      </properties>
      <example>

public class Foo implements Enumeration {
    private int x = 42;
    public boolean hasMoreElements() {
        return true;
    }
    public Object nextElement() {
        return String.valueOf(i++);
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#replacehashtablewithmap"
         language="java"
         message="Consider replacing this Hashtable with the newer java.util.Map"
         name="ReplaceHashtableWithMap"
         since="3.4">
      <description>
Consider replacing Hashtable usage with the newer java.util.Map if thread safety is not required.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassType[pmd-java:typeIsExactly('java.util.Hashtable')]"/>
      </properties>
      <example>

public class Foo {
    void bar() {
        Hashtable h = new Hashtable();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#replacevectorwithlist"
         language="java"
         message="Consider replacing this Vector with the newer java.util.List"
         name="ReplaceVectorWithList"
         since="3.4">
      <description>
Consider replacing Vector usages with the newer java.util.ArrayList if expensive thread-safe operations are not required.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassType[pmd-java:typeIsExactly('java.util.Vector')]"/>
      </properties>
      <example>

import java.util.Vector;
public class Foo {
    void bar() {
        Vector v = new Vector();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.SimplifiableTestAssertionRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#simplifiabletestassertion"
         language="java"
         message="Assertion may be simplified using {0}"
         name="SimplifiableTestAssertion"
         since="6.37.0">
      <description>
            Reports test assertions that may be simplified using a more specific
            assertion method. This enables better error messages, and makes the
            assertions more readable.
        </description>
      <priority>3</priority>
      <example>

import org.junit.Test;
import static org.junit.Assert.*;

class SomeTestClass {
    Object a,b;
    @Test
    void testMethod() {
        assertTrue(a.equals(b)); // could be assertEquals(a, b);
        assertTrue(!a.equals(b)); // could be assertNotEquals(a, b);

        assertTrue(!something); // could be assertFalse(something);
        assertFalse(!something); // could be assertTrue(something);

        assertTrue(a == b); // could be assertSame(a, b);
        assertTrue(a != b); // could be assertNotSame(a, b);

        assertTrue(a == null); // could be assertNull(a);
        assertTrue(a != null); // could be assertNotNull(a);
    }
}
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#systemprintln"
         language="java"
         message="Usage of System.out/err"
         name="SystemPrintln"
         since="2.1">
      <description>
References to System.(out|err).print are usually intended for debugging purposes and can remain in
the codebase even in production code. By using a logger one can enable/disable this behaviour at
will (and by priority) and avoid clogging the Standard out log.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[ starts-with(@MethodName, 'print') ]&#xA;  /FieldAccess[ @Name = ('err', 'out') ]&#xA;  /TypeExpression[ pmd-java:typeIsExactly('java.lang.System') ]"/>
      </properties>
      <example>
 
class Foo{
    Logger log = Logger.getLogger(Foo.class.getName());
    public void testA () {
        System.out.println("Entering test");
        // Better use this
        log.fine("Entering test");
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnitTestAssertionsShouldIncludeMessageRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestassertionsshouldincludemessage"
         language="java"
         message="Unit test assertions should include a message"
         name="UnitTestAssertionsShouldIncludeMessage"
         since="1.04">
      <description>
Unit assertions should include an informative message - i.e., use the three-argument version of
`assertEquals()`, not the two-argument version.

This rule supports tests using JUnit (3, 4 and 5) and TestNG.

Note: This rule was named JUnitAssertionsShouldIncludeMessage before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <example>
            
public class Foo {
    @Test
    public void testSomething() {
        assertEquals("foo", "bar");
        // Use the form:
        // assertEquals("Foo does not equals bar", "foo", "bar");
        // instead
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnitTestContainsTooManyAssertsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestcontainstoomanyasserts"
         language="java"
         message="Unit tests should not contain more than ${maximumAsserts} assert(s)."
         name="UnitTestContainsTooManyAsserts"
         since="5.0">
      <description>
            Unit tests should not contain too many asserts. Many asserts are indicative of a complex test, for which
            it is harder to verify correctness.  Consider breaking the test scenario into multiple, shorter test scenarios.
            Customize the maximum number of assertions used by this Rule to suit your needs.

            This rule checks for JUnit (3, 4 and 5) and TestNG Tests.

            Note: This rule was named JUnitTestContainsTooManyAsserts before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <example>
            
public class MyTestCase {
    // Ok
    @Test
    public void testMyCaseWithOneAssert() {
        boolean myVar = false;
        assertFalse("should be false", myVar);
    }

    // Bad, too many asserts (assuming max=1)
    @Test
    public void testMyCaseWithMoreAsserts() {
        boolean myVar = false;
        assertFalse("myVar should be false", myVar);
        assertEquals("should equals false", false, myVar);
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnitTestShouldIncludeAssertRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestshouldincludeassert"
         language="java"
         message="This unit test should include assert() or fail()"
         name="UnitTestShouldIncludeAssert"
         since="2.0">
      <description>
            Unit tests should include at least one assertion. This makes the tests more robust, and using assert
            with messages provide the developer a clearer idea of what the test does.

            This rule checks for JUnit (3, 4 and 5) and TestNG Tests.

            Note: This rule was named JUnitTestsShouldIncludeAssert before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <example>
            
public class Foo {
   @Test
   public void testSomething() {
      Bar b = findBar();
      // This is better than having a NullPointerException
      // assertNotNull("bar not found", b);
      b.work();
   }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestshoulduseafterannotation"
         language="java"
         message="Apply the correct annotation if this method is used to clean up the tests"
         name="UnitTestShouldUseAfterAnnotation"
         since="4.0">
      <description>
This rule detects methods called `tearDown()` that are not properly annotated as a cleanup method.
This is primarily intended to assist in upgrading from JUnit 3, where tear down methods were required to be called `tearDown()`.
To a lesser extent, this may help detect omissions even under newer JUnit versions or under TestNG,
as long as you are following this convention to name the methods.

* JUnit 4 will only execute methods annotated with `@After` after running each test.
* JUnit 5 introduced `@AfterEach` and `@AfterAll` annotations to execute methods after each test or after
  all tests in the class, respectively.
* TestNG provides the annotations `@AfterMethod` and `@AfterClass` to execute methods after each test or after
  tests in the class, respectively.

Note: This rule was named JUnit4TestShouldUseAfterAnnotation before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name='tearDown' and @Arity=0]&#xA;    [not(ModifierList/Annotation[&#xA;           pmd-java:typeIs('org.junit.After')&#xA;        or pmd-java:typeIs('org.junit.jupiter.api.AfterEach')&#xA;        or pmd-java:typeIs('org.junit.jupiter.api.AfterAll')&#xA;        or pmd-java:typeIs('org.testng.annotations.AfterClass')&#xA;        or pmd-java:typeIs('org.testng.annotations.AfterMethod')&#xA;    ])]&#xA;    (: Make sure this is a JUnit 4/5 or TestNG class :)&#xA;    [../MethodDeclaration[&#xA;            pmd-java:hasAnnotation('org.junit.Test')&#xA;         or pmd-java:hasAnnotation('org.junit.jupiter.api.Test')&#xA;         or pmd-java:hasAnnotation('org.testng.annotations.Test')&#xA;    ]]"/>
      </properties>
      <example>

public class MyTest {
    public void tearDown() {
        bad();
    }
}
public class MyTest2 {
    @After public void tearDown() {
        good();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestshouldusebeforeannotation"
         language="java"
         message="Apply the correct annotation if this method is used to set up the tests"
         name="UnitTestShouldUseBeforeAnnotation"
         since="4.0">
      <description>
This rule detects methods called `setUp()` that are not properly annotated as a setup method.
This is primarily intended to assist in upgrading from JUnit 3, where setup methods were required to be called `setUp()`.
To a lesser extent, this may help detect omissions even under newer JUnit versions or under TestNG,
as long as you are following this convention to name the methods.

* JUnit 4 will only execute methods annotated with `@Before` before all tests.
* JUnit 5 introduced `@BeforeEach` and `@BeforeAll` annotations to execute methods before each test or before all
  tests in the class, respectively.
* TestNG provides the annotations `@BeforeMethod` and `@BeforeClass` to execute methods before each test or before
  tests in the class, respectively.

Note: This rule was named JUnit4TestShouldUseBeforeAnnotation before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name='setUp' and @Arity=0]&#xA;    [not(ModifierList/Annotation[&#xA;           pmd-java:typeIs('org.junit.Before')&#xA;        or pmd-java:typeIs('org.junit.jupiter.api.BeforeEach')&#xA;        or pmd-java:typeIs('org.junit.jupiter.api.BeforeAll')&#xA;        or pmd-java:typeIs('org.testng.annotations.BeforeMethod')&#xA;        or pmd-java:typeIs('org.testng.annotations.BeforeClass')&#xA;    ])]&#xA;    (: Make sure this is a JUnit 4/5 or TestNG class :)&#xA;    [../MethodDeclaration[&#xA;               pmd-java:hasAnnotation('org.junit.Test')&#xA;            or pmd-java:hasAnnotation('org.junit.jupiter.api.Test')&#xA;            or pmd-java:hasAnnotation('org.testng.annotations.Test')&#xA;    ]]"/>
      </properties>
      <example>
            
public class MyTest {
    public void setUp() {
        bad();
    }
}
public class MyTest2 {
    @Before public void setUp() {
        good();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unittestshouldusetestannotation"
         language="java"
         message="Unit tests should use the @Test annotation or won't be run. In case of JUnit 5, test methods might use @RepeatedTest, @TestFactory, @TestTemplate or @ParameterizedTest annotations instead."
         name="UnitTestShouldUseTestAnnotation"
         since="4.0">
      <description>
            The rule will detect any test method starting with "test" that is not properly annotated, and will therefore not be run.

            In JUnit 4, only methods annotated with the `@Test` annotation are executed.
            In JUnit 5, one of the following annotations should be used for tests: `@Test`, `@RepeatedTest`, `@TestFactory`, `@TestTemplate` or `@ParameterizedTest`.
            In TestNG, only methods annotated with the `@Test` annotation are executed.

            Note: This rule was named JUnit4TestShouldUseTestAnnotation before PMD 7.7.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[matches(@SimpleName, $testClassPattern) or pmd-java:typeIs('junit.framework.TestCase')]&#xA;    (: a junit 3 method :)&#xA;    /ClassBody/MethodDeclaration[&#xA;        @Visibility=&#34;public&#34;&#xA;        and starts-with(@Name, 'test')&#xA;        and not(ModifierList/Annotation[&#xA;          pmd-java:typeIs('org.junit.Test')&#xA;          or pmd-java:typeIs('org.junit.jupiter.api.Test')&#xA;          or pmd-java:typeIs('org.junit.jupiter.api.RepeatedTest')&#xA;          or pmd-java:typeIs('org.junit.jupiter.api.TestFactory')&#xA;          or pmd-java:typeIs('org.junit.jupiter.api.TestTemplate')&#xA;          or pmd-java:typeIs('org.junit.jupiter.params.ParameterizedTest')&#xA;          or pmd-java:typeIs('org.testng.annotations.Test')&#xA;          ]&#xA;        )&#xA;    ]"/>
         <property description="The regex pattern used to identify test classes"
                   name="testClassPattern"
                   type="Regex">
            <value>Test</value>
         </property>
      </properties>
      <example>
            
public class MyTest {
    public void testBad() {
        doSomething();
    }

    @Test
    public void testGood() {
        doSomething();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnnecessaryVarargsArrayCreationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unnecessaryvarargsarraycreation"
         language="java"
         message="Unnecessary explicit array creation for varargs method call"
         name="UnnecessaryVarargsArrayCreation"
         since="7.1.0">
      <description>
            Reports explicit array creation when a varargs is expected.
            For instance:
            ```java
            Arrays.asList(new String[] { "foo", "bar", });
            ```
            can be replaced by:
            ```java
            Arrays.asList("foo", "bar");
            ```
        </description>
      <priority>3</priority>
      <example>
import java.util.Arrays;

class C {
    static {
        Arrays.asList(new String[]{"foo", "bar",});
        // should be
        Arrays.asList("foo", "bar");
    }
}
            </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.impl.UnnecessaryPmdSuppressionRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unnecessarywarningsuppression"
         language="java"
         message="Unused suppression {0}."
         name="UnnecessaryWarningSuppression"
         since="7.14.0">
      <description>
            This rule reports suppression comments and annotations that did not suppress any PMD violation.
            Note that violations of this rule cannot be suppressed.

            Please note:
            - The rule will report those suppressions comments/annotations that did not suppress a violation
            _during the current run_. That means you cannot run this rule separately from other rules, it must
            always be run with all the rules that could produce a warning. This is most likely not a problem, as
            you can just include this rule in your regular ruleset.
            - The rule for now only reports annotations specific to PMD, like `@SuppressWarnings("PMD")`. For instance
            `@SuppressWarnings("all")` is never reported as we cannot know if another tool is producing a
            warning there that must be suppressed. In the future we might be able to check for other common ones
            like `@SuppressWarnings("unchecked")` or `"fallthrough"`.
        </description>
      <priority>3</priority>
      <example>

            public class Something {
                // Unless some rule triggered on the following line, this rule will report the comment:
                private void foo() {} // NOPMD
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnusedAssignmentRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unusedassignment"
         language="java"
         message="The value assigned to this variable is never used or always overwritten"
         name="UnusedAssignment"
         since="6.26.0">
      <description>
            Reports assignments to variables that are never used before the variable is overwritten,
            or goes out of scope. Unused assignments are those for which
            1. The variable is never read after the assignment, or
            2. The assigned value is always overwritten by other assignments before the next read of
            the variable.

            The rule tracks assignments to fields of `this`, and static fields of the current class.
            This may cause some false positives in timing-sensitive concurrent code, which the rule cannot detect.

            The rule may be suppressed with the standard `@SuppressWarnings("unused")` tag.

            The rule subsumes {% rule "UnusedLocalVariable" %}, and {% rule "UnusedFormalParameter" %}.
            Those violations are filtered
            out by default, in case you already have enabled those rules, but may be enabled with the property
            `reportUnusedVariables`. Variables whose name starts with `ignored` or `unused` are filtered out, as
            is standard practice for exceptions.

            Limitations:
            * The rule currently cannot know which method calls throw exceptions, or which exceptions they throw.
            In the body of a try block, every method or constructor call is assumed to throw.  This may cause false-negatives.
            The only other language construct that is assumed to throw is the `throw` statement, in particular,
            things like `assert` statements, or NullPointerExceptions on dereference are ignored.
            * The rule cannot resolve assignments across constructors, when they're called with the special
            `this(...)` syntax. This may cause false-negatives.

            Both of those limitations may be partly relaxed in PMD 7.
        </description>
      <priority>3</priority>
      <example>
            
            class A {
                // this field initializer is redundant,
                // it is always overwritten in the constructor
                int f = 1;

                A(int f) {
                    this.f = f;
                }
            }
        
        </example>
      <example>
class B {

    int method(int i, int j) {
        // this initializer is redundant,
        // it is overwritten in all branches of the `if`
        int k = 0;

        // Both the assignments to k are unused, because k is
        // not read after the if/else
        // This may hide a bug: the programmer probably wanted to return k
        if (i &lt; j)
            k = i;
        else
            k = j;

        return j;
    }

}
        

        </example>
      <example>
class C {

    int method() {
        int i = 0;

        checkSomething(++i);
        checkSomething(++i);
        checkSomething(++i);
        checkSomething(++i);

        // That last increment is not reported unless
        // the property `checkUnusedPrefixIncrement` is
        // set to `true`
        // Technically it could be written (i+1), but it
        // is not very important
    }

}
        

        </example>
      <example>
class C {

    // variables that are truly unused (at most assigned to, but never accessed)
    // are only reported if property `reportUnusedVariables` is true

    void method(int param) { } // for example this method parameter

    // even then, you can suppress the violation with an annotation:

    void method(@SuppressWarning("unused") int param) { } // no violation, even if `reportUnusedVariables` is true

    // For catch parameters, or for resources which don't need to be used explicitly,
    // you can give a name that starts with "ignored" to ignore such warnings

    {
        try (Something ignored = Something.create()) {
            // even if ignored is unused, it won't be flagged
            // its purpose might be to side-effect in the create/close routines

        } catch (Exception e) { // this is unused and will cause a warning if `reportUnusedVariables` is true
            // you should choose a name that starts with "ignored"
            return;
        }
    }

}
        

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnusedFormalParameterRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unusedformalparameter"
         language="java"
         message="Avoid unused {0} parameters such as ''{1}''."
         name="UnusedFormalParameter"
         since="0.8">
      <description>
Reports parameters of methods and constructors that are not referenced them in the method body.
Parameters whose name starts with `ignored` or `unused` are filtered out.

Removing unused formal parameters from public methods could cause a ripple effect through the code base.
Hence, by default, this rule only considers private methods. To include non-private methods, set the
`checkAll` property to `true`. The same applies to public constructors.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    private void bar(String howdy) {
        // howdy is not used
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnusedLocalVariableRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unusedlocalvariable"
         language="java"
         message="Avoid unused local variables such as ''{0}''."
         name="UnusedLocalVariable"
         since="0.1">
      <description>
Detects when a local variable is declared and/or assigned, but not used.
Variables whose name starts with `ignored` or `unused` are filtered out.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    public void doSomething() {
        int i = 5; // Unused
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnusedPrivateFieldRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unusedprivatefield"
         language="java"
         message="Avoid unused private fields such as ''{0}''."
         name="UnusedPrivateField"
         since="0.1">
      <description>
Detects when a private field is declared and/or assigned a value, but not used.

Since PMD 6.50.0 private fields are ignored, if the fields are annotated with any annotation or the
enclosing class has any annotation. Annotations often enable a framework (such as dependency injection, mocking
or e.g. Lombok) which use the fields by reflection or other means. This usage can't be detected by static code analysis.
Previously these frameworks where explicitly allowed by listing their annotations in the property
"ignoredAnnotations", but that turned out to be prone of false positive for any not explicitly considered framework.
        </description>
      <priority>3</priority>
      <example>

public class Something {
    private static int FOO = 2; // Unused
    private int i = 5; // Unused
    private int j = 6;
    public int addOne() {
        return j++;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UnusedPrivateMethodRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#unusedprivatemethod"
         language="java"
         message="Avoid unused private methods such as ''{0}''."
         name="UnusedPrivateMethod"
         since="0.7">
      <description>
Unused Private Method detects when a private method is declared but is unused.
        </description>
      <priority>3</priority>
      <example>

public class Something {
    private void foo() {} // unused
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UseCollectionIsEmptyRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#usecollectionisempty"
         language="java"
         message="Substitute calls to size() == 0 (or size() != 0, size() &gt; 0, size() &lt; 1) with calls to isEmpty()"
         name="UseCollectionIsEmpty"
         since="3.9">
      <description>
The isEmpty() method on java.util.Collection is provided to determine if a collection has any elements.
Comparing the value of size() to 0 does not convey intent as well as the isEmpty() method.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    void good() {
        List foo = getList();
        if (foo.isEmpty()) {
            // blah
        }
    }

    void bad() {
        List foo = getList();
        if (foo.size() == 0) {
            // blah
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UseEnumCollectionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#useenumcollections"
         language="java"
         message="This collection could be an {0}"
         name="UseEnumCollections"
         since="7.3.0">
      <description>
            Wherever possible, use `EnumSet` or `EnumMap` instead of `HashSet` and `HashMap` when the keys
            are of an enum type. The specialized enum collections are more space- and time-efficient.
            This rule reports constructor expressions for hash sets or maps whose key
            type is an enum type.
        </description>
      <priority>3</priority>
      <example>

            import java.util.EnumMap;
            import java.util.HashSet;

            enum Example {
                A, B, C;

                public static Set&lt;Example&gt; newSet() {
                    return new HashSet&lt;&gt;(); // Could be EnumSet.noneOf(Example.class)
                }

                public static &lt;V&gt; Map&lt;Example, V&gt; newMap() {
                    return new HashMap&lt;&gt;(); // Could be new EnumMap&lt;&gt;(Example.class)
                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#usestandardcharsets"
         language="java"
         message="Please use StandardCharsets constants"
         minimumLanguageVersion="1.7"
         name="UseStandardCharsets"
         since="6.34.0">
      <description>
Starting with Java 7, StandardCharsets provides constants for common Charset objects, such as UTF-8.
Using the constants is less error prone, and can provide a small performance advantage compared to `Charset.forName(...)`
since no scan across the internal `Charset` caches is needed.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[@MethodName = 'forName'][pmd-java:typeIs('java.nio.charset.Charset')]&#xA;    [&#xA;        ArgumentList/StringLiteral&#xA;            [@Image = ('&#34;US-ASCII&#34;', '&#34;ISO-8859-1&#34;', '&#34;UTF-8&#34;', '&#34;UTF-16BE&#34;', '&#34;UTF-16LE&#34;', '&#34;UTF-16&#34;')]&#xA;    ]"/>
      </properties>
      <example>

public class UseStandardCharsets {
    public void run() {

        // looking up the charset dynamically
        try (OutputStreamWriter osw = new OutputStreamWriter(out, Charset.forName("UTF-8"))) {
            osw.write("test");
        }

        // best to use StandardCharsets
        try (OutputStreamWriter osw = new OutputStreamWriter(out, StandardCharsets.UTF_8)) {
            osw.write("test");
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.bestpractices.UseTryWithResourcesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#usetrywithresources"
         language="java"
         message="Consider using a try-with-resources statement instead of explicitly closing the resource"
         minimumLanguageVersion="1.7"
         name="UseTryWithResources"
         since="6.12.0">
      <description>
Java 7 introduced the try-with-resources statement. This statement ensures that each resource is closed at the end
of the statement. It avoids the need of explicitly closing the resources in a finally block. Additionally exceptions
are better handled: If an exception occurred both in the `try` block and `finally` block, then the exception from
the try block was suppressed. With the `try`-with-resources statement, the exception thrown from the try-block is
preserved.
        </description>
      <priority>3</priority>
      <example>

public class TryWithResources {
    public void run() {
        InputStream in = null;
        try {
            in = openInputStream();
            int i = in.read();
        } catch (IOException e) {
            e.printStackTrace();
        } finally {
            try {
                if (in != null) in.close();
            } catch (IOException ignored) {
                // ignored
            }
        }

        // better use try-with-resources
        try (InputStream in2 = openInputStream()) {
            int i = in2.read();
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#usevarargs"
         language="java"
         message="Consider using varargs for methods or constructors which take an array the last parameter."
         minimumLanguageVersion="1.5"
         name="UseVarargs"
         since="5.0">
      <description>
Java 5 introduced the varargs parameter declaration for methods and constructors. This syntactic
sugar provides flexibility for users of these methods and constructors, allowing them to avoid
having to deal with the creation of an array.

Byte arrays in any method and String arrays in `public static void main(String[])` methods are ignored.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//FormalParameters[not(parent::MethodDeclaration[@Overridden=true() or @MainMethod=true()])]&#xA;  /FormalParameter[position()=last()]&#xA;   [@Varargs=false()]&#xA;   [ArrayType[not(PrimitiveType[@Kind = &#34;byte&#34;] or ClassType[pmd-java:typeIs('java.lang.Byte')])]&#xA;    or VariableId[ArrayDimensions] and (PrimitiveType[not(@Kind=&#34;byte&#34;)] or ClassType[not(pmd-java:typeIs('java.lang.Byte'))])]"/>
      </properties>
      <example>

public class Foo {
    public void foo(String s, Object[] args) {
        // Do something here...
    }

    public void bar(String s, Object... args) {
        // Ahh, varargs tastes much better...
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_bestpractices.html#whileloopwithliteralboolean"
         language="java"
         message="The loop can be simplified."
         name="WhileLoopWithLiteralBoolean"
         since="6.13.0">
      <description>
`do {} while (true);` requires reading the end of the statement before it is
apparent that it loops forever, whereas `while (true) {}` is easier to understand.

`do {} while (false);` is redundant, and if an inner variable scope is required,
a block `{}` is sufficient.

`while (false) {}` will never execute the block and can be removed in its entirety.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="(: while loops with single boolean literal 'false', maybe parenthesized :)&#xA;//WhileStatement/BooleanLiteral[@True = false()]&#xA;|&#xA;(: do-while loops with single boolean literal ('false' or 'true'), maybe parenthesized :)&#xA;//DoStatement/BooleanLiteral&#xA;|&#xA;(: while loops with conditional or'ed boolean literals, maybe parenthesized :)&#xA;//WhileStatement[(InfixExpression[@Operator = ('|', '||')])&#xA;    (: no var access :)&#xA;    [count(VariableAccess) = 0]&#xA;    (: at least one false literal :)&#xA;    [count(BooleanLiteral[@True = false()]) &gt;= 1]]&#xA;|&#xA;(: while loops with conditional and'ed boolean literals, maybe parenthesized :)&#xA;//WhileStatement[(InfixExpression[@Operator = ('&amp;', '&amp;&amp;')])&#xA;    (: at least one false literal :)&#xA;    [count(BooleanLiteral[@True = false()]) &gt;= 1]]&#xA;|&#xA;(: do-while loops with conditional or'ed boolean literals, maybe parenthesized :)&#xA;//DoStatement[(InfixExpression[@Operator = ('|', '||')])&#xA;    (: at least one true literal :)&#xA;    [count(BooleanLiteral[@True = true()]) &gt;= 1&#xA;      (: or only boolean literal and no no var access :)&#xA;      or count(BooleanLiteral) &gt;= 1&#xA;      and count(VariableAccess) = 0&#xA;    ]]&#xA;|&#xA;(: do-while loops with conditional and'ed boolean literals, maybe parenthesized :)&#xA;//DoStatement[(InfixExpression[@Operator = ('&amp;', '&amp;&amp;')])&#xA;    (: at least one false literal :)&#xA;    [count(BooleanLiteral[@True = false()]) &gt;= 1&#xA;      (: or only boolean literal and no no var access :)&#xA;      or count(BooleanLiteral) &gt;= 1&#xA;      and count(VariableAccess) = 0&#xA;    ]]"/>
      </properties>
      <example>

public class Example {
  {
    while (true) { } // allowed
    while (false) { } // disallowed
    do { } while (true); // disallowed
    do { } while (false); // disallowed
    do { } while (false | false); // disallowed
    do { } while (false || false); // disallowed
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.AssignmentInOperandRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#assignmentinoperand"
         language="java"
         message="Avoid assignment to {0} in operand"
         name="AssignmentInOperand"
         since="1.03">
      <description>
Avoid assignments in operands; this can make code more complicated and harder to read.
        </description>
      <priority>3</priority>
      <example>

public void bar() {
    int x = 2;
    if ((x = getX()) == 3) {
      System.out.println("3!");
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.AssignmentToNonFinalStaticRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#assignmenttononfinalstatic"
         language="java"
         message="Possible unsafe assignment to non-final static field ''{0}'' in a constructor."
         name="AssignmentToNonFinalStatic"
         since="2.2">
      <description>
Identifies a possible unsafe usage of a static field.
        </description>
      <priority>3</priority>
      <example>

public class StaticField {
   static int x;
   public FinalFields(int y) {
    x = y; // unsafe
   }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidaccessibilityalteration"
         language="java"
         message="You should not modify visibility of constructors, methods or fields using setAccessible()"
         name="AvoidAccessibilityAlteration"
         since="4.1">
      <description>
Methods such as `getDeclaredConstructors()`, `getDeclaredMethods()`, and `getDeclaredFields()` also
return private constructors, methods and fields. These can be made accessible by calling `setAccessible(true)`.
This gives access to normally protected data which violates the principle of encapsulation.

This rule detects calls to `setAccessible` and finds possible accessibility alterations.
If the call to `setAccessible` is wrapped within a `PrivilegedAction`, then the access alteration
is assumed to be deliberate and is not reported.

Note that with Java 17 the Security Manager, which is used for `PrivilegedAction` execution,
is deprecated: [JEP 411: Deprecate the Security Manager for Removal](https://openjdk.org/jeps/411).
For future-proof code, deliberate access alteration should be suppressed using the usual
suppression methods (e.g. by using `@SuppressWarnings` annotation).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[&#xA;          pmd-java:matchesSig(&#34;java.lang.reflect.AccessibleObject#setAccessible(boolean)&#34;)&#xA;       or pmd-java:matchesSig(&#34;_#setAccessible(java.lang.reflect.AccessibleObject[],boolean)&#34;)&#xA;    ]&#xA;    [not(ArgumentList/BooleanLiteral[@True = false()])]&#xA;    (: exclude anonymous privileged action classes :)&#xA;    [not(ancestor::ConstructorCall[1][pmd-java:typeIs('java.security.PrivilegedAction')]/AnonymousClassDeclaration)]&#xA;    (: exclude inner privileged action classes :)&#xA;    [not(ancestor::ClassDeclaration[1][pmd-java:typeIs('java.security.PrivilegedAction')])]&#xA;    (: exclude privileged action lambdas :)&#xA;    [not(ancestor::LambdaExpression[pmd-java:typeIs('java.security.PrivilegedAction')])]"/>
      </properties>
      <example>

import java.lang.reflect.Constructor;
import java.lang.reflect.Field;
import java.lang.reflect.Method;
import java.security.AccessController;
import java.security.PrivilegedAction;

public class Violation {
    private void invalidSetAccessCalls() throws NoSuchMethodException, SecurityException {
        Constructor&lt;?&gt; constructor = this.getClass().getDeclaredConstructor(String.class);
        // call to forbidden setAccessible
        constructor.setAccessible(true);

        Method privateMethod = this.getClass().getDeclaredMethod("aPrivateMethod");
        // call to forbidden setAccessible
        privateMethod.setAccessible(true);

        // deliberate accessibility alteration
        String privateField = AccessController.doPrivileged(new PrivilegedAction&lt;String&gt;() {
            @Override
            public String run() {
                try {
                    Field field = Violation.class.getDeclaredField("aPrivateField");
                    field.setAccessible(true);
                    return (String) field.get(null);
                } catch (ReflectiveOperationException | SecurityException e) {
                    throw new RuntimeException(e);
                }
            }
        });
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidassertasidentifier"
         language="java"
         maximumLanguageVersion="1.3"
         message="Avoid using assert as an identifier; it became a reserved word in JDK 1.4"
         name="AvoidAssertAsIdentifier"
         since="3.4">
      <description>
Use of the term `assert` will conflict with newer versions of Java since it is a reserved word.

Since Java 1.4, the token `assert` became a reserved word and using it as an identifier will
result in a compilation failure for Java 1.4 and later. This rule is therefore only useful
for old Java code before Java 1.4. It can be used to identify problematic code prior to a Java update.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath" value="//VariableId[@Name='assert']"/>
      </properties>
      <example>

public class A {
    public class Foo {
        String assert = "foo";
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.AvoidBranchingStatementAsLastInLoopRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidbranchingstatementaslastinloop"
         language="java"
         message="Avoid using a branching statement as the last in a loop."
         name="AvoidBranchingStatementAsLastInLoop"
         since="5.0">
      <description>
Using a branching statement as the last part of a loop may be a bug, and/or is confusing.
Ensure that the usage is not a bug, or consider using another approach.
        </description>
      <priority>2</priority>
      <example>

// unusual use of branching statement in a loop
for (int i = 0; i &lt; 10; i++) {
    if (i*i &lt;= 25) {
        continue;
    }
    break;
}

// this makes more sense...
for (int i = 0; i &lt; 10; i++) {
    if (i*i &gt; 25) {
        break;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidcallingfinalize"
         language="java"
         message="Avoid calling finalize() explicitly"
         name="AvoidCallingFinalize"
         since="3.0">
      <description>
The method Object.finalize() is called by the garbage collector on an object when garbage collection determines
that there are no more references to the object. It should not be invoked by application logic.

Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[pmd-java:matchesSig(&#34;java.lang.Object#finalize()&#34;)]&#xA;    (: it's ok inside finalize :)&#xA;    [not(SuperExpression and ancestor::*[self::MethodDeclaration or self::Initializer][1][@Name = 'finalize'][@Arity = 0][VoidType])]"/>
      </properties>
      <example>

void foo() {
    Bar b = new Bar();
    b.finalize();
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidcatchingnpe"
         language="java"
         message="Avoid catching NullPointerException; consider removing the cause of the NPE."
         name="AvoidCatchingNPE"
         since="1.8">
      <description>
Code should never throw NullPointerExceptions under normal circumstances.  A catch block may hide the
original error, causing other, more subtle problems later on.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CatchClause/CatchParameter/ClassType[pmd-java:typeIsExactly('java.lang.NullPointerException')]"/>
      </properties>
      <example>

public class Foo {
    void bar() {
        try {
            // do something
        } catch (NullPointerException npe) {
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidcatchingthrowable"
         language="java"
         message="A catch statement should never catch throwable since it includes errors."
         name="AvoidCatchingThrowable"
         since="1.2">
      <description>
Catching Throwable errors is not recommended since its scope is very broad. It includes runtime issues such as
OutOfMemoryError that should be exposed and managed separately.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CatchParameter[ClassType[pmd-java:typeIsExactly('java.lang.Throwable')]]/VariableId"/>
      </properties>
      <example>

public void bar() {
    try {
        // do something
    } catch (Throwable th) {  // should not catch Throwable
        th.printStackTrace();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoiddecimalliteralsinbigdecimalconstructor"
         language="java"
         message="Avoid creating BigDecimal with a decimal (float/double) literal. Use a String literal"
         name="AvoidDecimalLiteralsInBigDecimalConstructor"
         since="3.4">
      <description>
One might assume that the result of "new BigDecimal(0.1)" is exactly equal to 0.1, but it is actually
equal to .1000000000000000055511151231257827021181583404541015625.
This is because 0.1 cannot be represented exactly as a double (or as a binary fraction of any finite
length). Thus, the long value that is being passed in to the constructor is not exactly equal to 0.1,
appearances notwithstanding.

The (String) constructor, on the other hand, is perfectly predictable: 'new BigDecimal("0.1")' is
exactly equal to 0.1, as one would expect.  Therefore, it is generally recommended that the
(String) constructor be used in preference to this one.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ConstructorCall[pmd-java:matchesSig('java.math.BigDecimal#new(double)')]"/>
      </properties>
      <example>

BigDecimal bd = new BigDecimal(1.123);       // loss of precision, this would trigger the rule

BigDecimal bd = new BigDecimal("1.123");     // preferred approach

BigDecimal bd = new BigDecimal(12);          // preferred approach, ok for integer values

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.AvoidDuplicateLiteralsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidduplicateliterals"
         language="java"
         message="The String literal {0} appears {1} times in this file; the first occurrence is on line {2}"
         name="AvoidDuplicateLiterals"
         since="1.0">
      <description>
Code containing duplicate String literals can usually be improved by declaring the String as a constant field.
        </description>
      <priority>3</priority>
      <example>

private void bar() {
     buz("Howdy");
     buz("Howdy");
     buz("Howdy");
     buz("Howdy");
}
private void buz(String x) {}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidenumasidentifier"
         language="java"
         maximumLanguageVersion="1.4"
         message="Avoid using enum as an identifier; it's a reserved word in JDK 1.5"
         name="AvoidEnumAsIdentifier"
         since="3.4">
      <description>
Use of the term `enum` will conflict with newer versions of Java since it is a reserved word.

Since Java 1.5, the token `enum` became a reserved word and using it as an identifier will
result in a compilation failure for Java 1.5 and later. This rule is therefore only useful
for old Java code before Java 1.5. It can be used to identify problematic code prior to a Java update.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath" value="//VariableId[@Name='enum']"/>
      </properties>
      <example>

public class A {
    public class Foo {
        String enum = "foo";
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidfieldnamematchingmethodname"
         language="java"
         message="Field {0} has the same name as a method"
         name="AvoidFieldNameMatchingMethodName"
         since="3.0">
      <description>
It can be confusing to have a field name with the same name as a method. While this is permitted,
having information (field) and actions (method) is not clear naming. Developers versed in
Smalltalk often prefer this approach as the methods denote accessor methods.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FieldDeclaration/VariableDeclarator/VariableId&#xA;    [some $method in ../../..[self::ClassBody or self::EnumBody]/MethodDeclaration&#xA;     satisfies lower-case(@Name) = lower-case($method/@Name)]"/>
      </properties>
      <example>

public class Foo {
    Object bar;
    // bar is data or an action or both?
    void bar() {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidfieldnamematchingtypename"
         language="java"
         message="It is somewhat confusing to have a field name matching the declaring class name"
         name="AvoidFieldNameMatchingTypeName"
         since="3.0">
      <description>
It is somewhat confusing to have a field name matching the declaring type name.
This probably means that type and/or field names should be chosen more carefully.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FieldDeclaration/VariableDeclarator/VariableId&#xA;    [lower-case(@Name) = lower-case(ancestor::ClassDeclaration[1]/@SimpleName)]"/>
      </properties>
      <example>

public class Foo extends Bar {
    int foo;    // There is probably a better name that can be used
}
public interface Operation {
    int OPERATION = 1; // There is probably a better name that can be used
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidinstanceofchecksincatchclause"
         language="java"
         message="An instanceof check is being performed on the caught exception. Create a separate catch clause for {0}."
         name="AvoidInstanceofChecksInCatchClause"
         since="3.0">
      <description>
Each caught exception type should be handled in its own catch clause.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CatchParameter&#xA;    /following-sibling::Block//InfixExpression[@Operator = 'instanceof']&#xA;        /VariableAccess[@Name = ./ancestor::Block/preceding-sibling::CatchParameter/@Name]&#xA;            /following-sibling::TypeExpression/*"/>
      </properties>
      <example>

try { // Avoid this
    // do something
} catch (Exception ee) {
    if (ee instanceof IOException) {
        cleanup();
    }
}

try {  // Prefer this:
    // do something
} catch (IOException ee) {
    cleanup();
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidliteralsinifcondition"
         language="java"
         message="Avoid using literals such as {0} in if statements"
         name="AvoidLiteralsInIfCondition"
         since="4.2.6">
      <description>
Avoid using hard-coded literals in conditional statements. By declaring them as static variables
or private members with descriptive names maintainability is enhanced. By default, the literals "-1" and "0" are ignored.
More exceptions can be defined with the property "ignoreMagicNumbers".

The rule doesn't consider deeper expressions by default, but this can be enabled via the property `ignoreExpressions`.
With this property set to false, if-conditions like `i == 1 + 5` are reported as well. Note that in that case,
the property ignoreMagicNumbers is not taken into account, if there are multiple literals involved in such an expression.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="(: simple case - no deep expressions - this is always executed :)&#xA;//IfStatement/*[1]/*[pmd-java:nodeIs('Literal')]&#xA;    [not(pmd-java:nodeIs('NullLiteral'))]&#xA;    [not(pmd-java:nodeIs('BooleanLiteral'))]&#xA;    [empty(index-of(tokenize($ignoreMagicNumbers, '\s*,\s*'), @Image))]&#xA;|&#xA;(: consider also deeper expressions :)&#xA;//IfStatement[$ignoreExpressions = false()]/*[1]//*[not(self::UnaryExpression[@Operator = '-'])]/*[pmd-java:nodeIs('Literal')]&#xA;    [not(pmd-java:nodeIs('NullLiteral'))]&#xA;    [not(pmd-java:nodeIs('BooleanLiteral'))]&#xA;    [empty(index-of(tokenize($ignoreMagicNumbers, '\s*,\s*'), @Image))]&#xA;|&#xA;(: consider negative literals :)&#xA;//IfStatement[$ignoreExpressions = false()]/*[1]//UnaryExpression[@Operator = '-']/*[pmd-java:nodeIs('Literal')]&#xA;    [not(pmd-java:nodeIs('NullLiteral'))]&#xA;    [not(pmd-java:nodeIs('BooleanLiteral'))]&#xA;    [empty(index-of(tokenize($ignoreMagicNumbers, '\s*,\s*'), concat('-', @Image)))]&#xA;|&#xA;(: consider multiple literals in expressions :)&#xA;//IfStatement[$ignoreExpressions = false()]/*[1][count(*[pmd-java:nodeIs('Literal')]&#xA;    [not(pmd-java:nodeIs('NullLiteral'))]&#xA;    [not(pmd-java:nodeIs('BooleanLiteral'))]) &gt; 1]"/>
         <property description="Comma-separated list of magic numbers, that should be ignored"
                   name="ignoreMagicNumbers"
                   type="String">
            <value>-1,0</value>
         </property>
         <property description="If true, only literals in simple if conditions are considered. Otherwise literals in expressions are checked, too."
                   name="ignoreExpressions"
                   type="Boolean">
            <value>true</value>
         </property>
      </properties>
      <example>

private static final int MAX_NUMBER_OF_REQUESTS = 10;

public void checkRequests() {

    if (i == 10) {                        // magic number, buried in a method
      doSomething();
    }

    if (i == MAX_NUMBER_OF_REQUESTS) {    // preferred approach
      doSomething();
    }

    if (aString.indexOf('.') != -1) {}     // magic number -1, by default ignored
    if (aString.indexOf('.') &gt;= 0) { }     // alternative approach

    if (aDouble &gt; 0.0) {}                  // magic number 0.0
    if (aDouble &gt;= Double.MIN_VALUE) {}    // preferred approach

    // with rule property "ignoreExpressions" set to "false"
    if (i == pos + 5) {}  // violation: magic number 5 within an (additive) expression
    if (i == pos + SUFFIX_LENGTH) {} // preferred approach
    if (i == 5 &amp;&amp; "none".equals(aString)) {} // 2 violations: magic number 5 and literal "none"
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="true"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidlosingexceptioninformation"
         language="java"
         message="Avoid statements in a catch block that invoke accessors on the exception without using the information"
         name="AvoidLosingExceptionInformation"
         since="4.2.6">
      <description>
Statements in a catch block that invoke accessors on the exception without using the information
only add to code size.  Either remove the invocation, or use the return result.

**Deprecated:** This rule is deprecated since PMD 7.17.0 and will be removed with PMD 8.0.0.
This rule has been replaced by {% rule UselessPureMethodCall %}.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//CatchClause/Block//ExpressionStatement/MethodCall[&#xA;    pmd-java:matchesSig(&#34;java.lang.Throwable#getMessage()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Throwable#getLocalizedMessage()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Throwable#getCause()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Throwable#getStackTrace()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Object#toString()&#34;)&#xA;]"/>
      </properties>
      <example>

public void bar() {
    try {
        // do something
    } catch (SomeException se) {
        se.getMessage();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidmultipleunaryoperators"
         language="java"
         message="Using multiple unary operators may be a bug, and/or is confusing."
         name="AvoidMultipleUnaryOperators"
         since="4.2">
      <description>
The use of multiple unary operators may be problematic, and/or confusing.
Ensure that the intended usage is not a bug, or consider simplifying the expression.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="(: Only report on the toplevel one :)&#xA;                    //UnaryExpression[UnaryExpression and not(parent::UnaryExpression)]"/>
      </properties>
      <example>

// These are typo bugs, or at best needlessly complex and confusing:
int i = - -1;
int j = + - +1;
int z = ~~2;
boolean b = !!true;
boolean c = !!!true;

// These are better:
int i = 1;
int j = -1;
int z = 2;
boolean b = true;
boolean c = false;

// And these just make your brain hurt:
int i = ~-2;
int j = -~7;

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.AvoidUsingOctalValuesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#avoidusingoctalvalues"
         language="java"
         message="Avoid integer literals that start with zero (interpreted as octal), remove the leading 0 to get a decimal literal (or use explicit 0x, 0b prefixes)"
         name="AvoidUsingOctalValues"
         since="3.9">
      <description>
Integer literals that start with zero are interpreted as octal (base-8) values in Java, which can lead to 
unexpected behavior and bugs. Most developers expect decimal (base-10) interpretation, making octal literals 
a common source of confusion and errors. For example, 012 equals 10 in decimal, not 12 as might be expected.
This rule helps prevent such mistakes by flagging integer literals that could be misinterpreted as decimal values. 
Use decimal literals without leading zeros, or use explicit prefixes like 0x for hexadecimal or 0b for binary values to make the intended base clear.
        </description>
      <priority>3</priority>
      <example>

// Bad: These look like decimal but are actually octal
int timeout = 060;     // Actually 48 in decimal, not 60!
int count = 012;       // Actually 10 in decimal, not 12!

// Good: Use decimal literals
int timeout = 60;      // Clear decimal value
int count = 12;        // Clear decimal value

// Good: Use explicit prefixes for other bases
int hexValue = 0xFF;   // Clearly hexadecimal
int binaryValue = 0b1010; // Clearly binary (Java 7+)

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.BrokenNullCheckRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#brokennullcheck"
         language="java"
         message="This expression will throw a NullPointerException"
         name="BrokenNullCheck"
         since="3.8">
      <description>
The null check is broken since it will throw a NullPointerException itself.
It is likely that you used || instead of &amp;&amp; or vice versa.
        </description>
      <priority>2</priority>
      <example>

public String bar(String string) {
  // should be &amp;&amp;
    if (string!=null || !string.equals(""))
        return string;
  // should be ||
    if (string==null &amp;&amp; string.equals(""))
        return string;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#callsuperfirst"
         language="java"
         message="super should be called at the start of the method"
         name="CallSuperFirst"
         since="4.2.5">
      <description>Super should be called at the start of the method</description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;    [&#xA;      pmd-java:typeIs('android.app.Activity') or&#xA;      pmd-java:typeIs('android.app.Application') or&#xA;      pmd-java:typeIs('android.app.Service')&#xA;    ]&#xA;    //MethodDeclaration&#xA;    [&#xA;      @Name=('onCreate', 'onConfigurationChanged', 'onPostCreate', 'onPostResume', 'onRestart',&#xA;             'onRestoreInstanceState', 'onResume', 'onStart')&#xA;    ]&#xA;    [not(Block/*[1]/MethodCall[SuperExpression][@MethodName = ancestor::MethodDeclaration/@Name])]"/>
      </properties>
      <example>

import android.app.Activity;
import android.os.Bundle;

public class DummyActivity extends Activity {
    public void onCreate(Bundle bundle) {
        // missing call to super.onCreate(bundle)
        foo();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#callsuperlast"
         language="java"
         message="super should be called at the end of the method"
         name="CallSuperLast"
         since="4.2.5">
      <description>
Super should be called at the end of the method
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;    [&#xA;      pmd-java:typeIs('android.app.Activity') or&#xA;      pmd-java:typeIs('android.app.Application') or&#xA;      pmd-java:typeIs('android.app.Service')&#xA;    ]&#xA;    //MethodDeclaration&#xA;    [&#xA;      @Name=('finish', 'onDestroy', 'onPause', 'onSaveInstanceState', 'onStop', 'onTerminate')&#xA;    ]&#xA;    [not(Block/*[last()]/MethodCall[SuperExpression][@MethodName = ancestor::MethodDeclaration/@Name])]"/>
      </properties>
      <example>

import android.app.Activity;

public class DummyActivity extends Activity {
    public void onPause() {
        foo();
        // missing call to super.onPause()
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.CheckSkipResultRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#checkskipresult"
         language="java"
         message="Check the value returned by the skip() method of an InputStream to see if the requested number of bytes has been skipped."
         name="CheckSkipResult"
         since="5.0">
      <description>
The skip() method may skip a smaller number of bytes than requested. Check the returned value to find out if it was the case or not.
        </description>
      <priority>3</priority>
      <example>

public class Foo {

   private FileInputStream _s = new FileInputStream("file");

   public void skip(int n) throws IOException {
      _s.skip(n); // You are not sure that exactly n bytes are skipped
   }

   public void skipExactly(int n) throws IOException {
      while (n != 0) {
         long skipped = _s.skip(n);
         if (skipped == 0)
            throw new EOFException();
         n -= skipped;
      }
   }

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#classcastexceptionwithtoarray"
         language="java"
         message="This usage of the Collection.toArray() method will throw a ClassCastException."
         name="ClassCastExceptionWithToArray"
         since="3.4">
      <description>
When deriving an array of a specific class from your Collection, one should provide an array of
the same class as the parameter of the `toArray()` method. Doing otherwise will result
in a `ClassCastException`.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CastExpression[ArrayType/ClassType[not(pmd-java:typeIsExactly('java.lang.Object'))]]&#xA;    /MethodCall[pmd-java:matchesSig(&#34;java.util.Collection#toArray()&#34;)]"/>
      </properties>
      <example>

Collection c = new ArrayList();
Integer obj = new Integer(1);
c.add(obj);

    // this would trigger the rule (and throw a ClassCastException if executed)
Integer[] a = (Integer [])c.toArray();

   // this is fine and will not trigger the rule
Integer[] b = (Integer [])c.toArray(new Integer[0]);

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#clonemethodmustbepublic"
         language="java"
         message="clone() method must be public if the class implements Cloneable"
         name="CloneMethodMustBePublic"
         since="5.4.0">
      <description>
The java manual says "By convention, classes that implement this interface should override
Object.clone (which is protected) with a public method."
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[not(pmd-java:modifiers() = &#34;public&#34;)]&#xA;    [@Name = 'clone']&#xA;    [@Arity = 0]"/>
      </properties>
      <example>

public class Foo implements Cloneable {
    @Override
    protected Object clone() throws CloneNotSupportedException { // Violation, must be public
    }
}

public class Foo implements Cloneable {
    @Override
    protected Foo clone() { // Violation, must be public
    }
}

public class Foo implements Cloneable {
    @Override
    public Object clone() // Ok
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.CloneMethodMustImplementCloneableRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#clonemethodmustimplementcloneable"
         language="java"
         message="clone() method should be implemented only if implementing Cloneable interface"
         name="CloneMethodMustImplementCloneable"
         since="1.9">
      <description>
The method clone() should only be implemented if the class implements the Cloneable interface with the exception of
a final method that only throws CloneNotSupportedException.

The rule can also detect, if the class implements or extends a Cloneable class.
        </description>
      <priority>3</priority>
      <example>

public class MyClass {
 public Object clone() throws CloneNotSupportedException {
  return foo;
 }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#clonemethodreturntypemustmatchclassname"
         language="java"
         message="The return type of the clone() method must be the class name when implements Cloneable"
         minimumLanguageVersion="1.5"
         name="CloneMethodReturnTypeMustMatchClassName"
         since="5.4.0">
      <description>
If a class implements `Cloneable` the return type of the method `clone()` must be the class name. That way, the caller
of the clone method doesn't need to cast the returned clone to the correct type.

Note: Such a covariant return type is only possible with Java 1.5 or higher.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration&#xA;    [@Name = 'clone']&#xA;    [@Arity = 0]&#xA;    [ClassType[1]/@SimpleName != ancestor::ClassDeclaration[1]/@SimpleName]"/>
      </properties>
      <example>

public class Foo implements Cloneable {
    @Override
    protected Object clone() { // Violation, Object must be Foo
    }
}

public class Foo implements Cloneable {
    @Override
    public Foo clone() { //Ok
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.CloseResourceRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#closeresource"
         language="java"
         message="Ensure that resources like this {0} object are closed after use"
         name="CloseResource"
         since="1.2.2">
      <description>
Ensure that resources (like `java.sql.Connection`, `java.sql.Statement`, and `java.sql.ResultSet` objects
and any subtype of `java.lang.AutoCloseable`) are always closed after use.
Failing to do so might result in resource leaks.

Note: It suffices to configure the super type, e.g. `java.lang.AutoCloseable`, so that this rule automatically triggers
on any subtype (e.g. `java.io.FileInputStream`). Additionally specifying `java.sql.Connection` helps in detecting
the types, if the type resolution / auxclasspath is not correctly setup.

Note: Since PMD 6.16.0 the default value for the property `types` contains `java.lang.AutoCloseable` and detects
now cases where the standard `java.io.*Stream` classes are involved. In order to restore the old behaviour,
just remove "AutoCloseable" from the types.
        </description>
      <priority>3</priority>
      <example>

public class Bar {
    public void withSQL() {
        Connection c = pool.getConnection();
        try {
            // do stuff
        } catch (SQLException ex) {
           // handle exception
        } finally {
            // oops, should close the connection using 'close'!
            // c.close();
        }
    }

    public void withFile() {
        InputStream file = new FileInputStream(new File("/tmp/foo"));
        try {
            int c = file.in();
        } catch (IOException e) {
            // handle exception
        } finally {
            // TODO: close file
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.CollectionTypeMismatchRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#collectiontypemismatch"
         language="java"
         message="Object of type ''{0}'' cannot be in collection expecting ''{1}''"
         name="CollectionTypeMismatch"
         since="7.17.0">
      <description>
Detects method calls on collections where the passed object cannot possibly be in the collection
due to type mismatch. This helps catch potential programming errors where incompatible types
are used with collection methods like contains(), remove(), indexOf(), etc.

Methods checked include:
- Collection: contains(), remove(), removeAll(), retainAll(), containsAll()
- List: indexOf(), lastIndexOf()
- Map: containsKey(), containsValue(), get(), getOrDefault(), remove()
- Deque: removeFirstOccurrence(), removeLastOccurrence()
- Hashtable: contains() (legacy method that checks values)
- ConcurrentHashMap: contains() (legacy method that checks values)
        </description>
      <priority>3</priority>
      <example>

List&lt;Integer&gt; numbers = Arrays.asList(1, 2, 3);
numbers.remove("string"); // violation: String cannot be in Integer list

Map&lt;String, Integer&gt; map = new HashMap&lt;&gt;();
map.get(42); // violation: Integer key cannot be in String-keyed map

Set&lt;String&gt; names = new HashSet&lt;&gt;(); 
names.contains(123); // violation: Integer cannot be in String set

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#compareobjectswithequals"
         language="java"
         message="Use equals() to compare object references."
         name="CompareObjectsWithEquals"
         since="3.2">
      <description>
Use `equals()` to compare object references; avoid comparing them with `==`.

Since comparing objects with named constants is useful in some cases (eg, when
defining constants for sentinel values), the rule ignores comparisons against
fields with all-caps name (eg `this == SENTINEL`), which is a common naming
convention for constant fields.

You may allow some types to be compared by reference by listing the exceptions
in the `typesThatCompareByReference` property.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//InfixExpression&#xA;    [@Operator = (&#34;==&#34;, &#34;!=&#34;)]&#xA;    [count(*&#xA;        [not(self::NullLiteral)]&#xA;        [pmd-java:typeIs('java.lang.Object')]&#xA;        [not(some $t in $typesThatCompareByReference satisfies pmd-java:typeIs($t))]&#xA;      ) = 2&#xA;    ]&#xA;    [not(ancestor::MethodDeclaration[1][@Name = &#34;equals&#34;])]&#xA;    (: Is not a field access with an all-caps identifier :)&#xA;    [not(FieldAccess[upper-case(@Name)=@Name]&#xA;     or VariableAccess[upper-case(@Name)=@Name])]"/>
         <property description="List of canonical type names for which reference comparison is allowed."
                   name="typesThatCompareByReference"
                   type="List[String]">
            <value>java.lang.Enum,java.lang.Class</value>
         </property>
      </properties>
      <example>

class Foo {
  boolean bar(String a, String b) {
    return a == b;
  }
}


        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#comparisonwithnan"
         language="java"
         message="Comparisons with NaN always return false"
         name="ComparisonWithNaN"
         since="6.36.0">
      <description>
            Reports comparisons with double and float `NaN` (Not-a-Number) values.
            These are [specified](https://docs.oracle.com/javase/specs/jls/se8/html/jls-15.html#jls-15.21.1)
            to have unintuitive behavior: NaN is considered unequal to itself.
            This means a check like `someDouble == Double.NaN` will always return
            false, even if `someDouble` is really the NaN value. To test whether a
            value is the NaN value, one should instead use `Double.isNaN(someDouble)`
            (or `Float.isNaN`). The `!=` operator should be treated similarly.
            Finally, comparisons like `someDouble &lt;= Double.NaN` are nonsensical
            and will always evaluate to false.
            
            This rule has been renamed from "BadComparison" in PMD 6.36.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//InfixExpression[@Operator = (&#34;==&#34;, &#34;!=&#34;, &#34;&lt;=&#34;, &#34;&gt;=&#34;, &#34;&lt;&#34;, &#34;&gt;&#34;)]/FieldAccess[@Name='NaN' and (pmd-java:typeIs('double') or pmd-java:typeIs('float'))]"/>
      </properties>
      <example>
            
boolean x = (y == Double.NaN);

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.ConfusingArgumentToVarargsMethodRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#confusingargumenttovarargsmethod"
         language="java"
         message="Unclear if a varargs or non-varargs call is intended. Cast to {0} or {0}[], or pass varargs parameters separately to clarify intent."
         name="ConfusingArgumentToVarargsMethod"
         since="7.1.0">
      <description>
            Reports a confusing argument passed to a varargs method.

            This can occur when an array is passed as a single varargs argument, when the array type is not exactly the
            type of array that the varargs method expects. If, that array is a subtype of the component type of the expected
            array type, then it might not be clear what value the called varargs method will receive.
            For instance if you have:
            ```java
            void varargs(Object... parm);
            ```
            and call it like so:
            ```java
            varargs(new String[]{"a"});
            ```
            it is not clear whether you intended the method to receive the value `new Object[]{ new String[] {"a"} }` or
            just `new String[] {"a"}` (the latter happens). This confusion occurs because `String[]` is both a subtype
            of `Object[]` and of `Object`. To clarify your intent in this case, use a cast or pass individual elements like so:
            ```java
            // varargs call
            // parm will be `new Object[] { "a" }`
            varargs("a");

            // non-varargs call
            // parm will be `new String[] { "a" }`
            varargs((Object[]) new String[]{"a"});

            // varargs call
            // parm will be `new Object[] { new String[] { "a" } }`
            varargs((Object) new String[]{"a"});
            ```

            Another confusing case is when you pass `null` as the varargs argument. Here it is not clear whether you intended
            to pass an array with a single null element, or a null array (the latter happens). This can similarly be clarified
            with a cast.
        </description>
      <priority>3</priority>
      <example>
            import java.util.Arrays;

            abstract class C {
                abstract void varargs(Object... args);
                static {
                    varargs(new String[] { "a" });
                    varargs(null);
                }
            }
            </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.ConstructorCallsOverridableMethodRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#constructorcallsoverridablemethod"
         language="java"
         message="Overridable {0} called during object construction{1}"
         name="ConstructorCallsOverridableMethod"
         since="1.04">
      <description>
Reports calls to overridable methods on `this` during object initialization. These
are invoked on an incompletely constructed object and can be difficult to debug if overridden.
This is because the subclass usually assumes that the superclass is completely initialized
in all methods. If that is not the case, bugs can appear in the constructor, for instance,
some fields that are still null may cause a NullPointerException or be stored somewhere
else to blow up later.

To avoid this problem, only use methods that are static, private, or final in constructors.
Note that those methods also must not call overridable methods transitively to be safe.
        </description>
      <priority>1</priority>
      <example>

public class SeniorClass {
  public SeniorClass(){
      toString(); //may throw NullPointerException if overridden
  }
  public String toString(){
    return "IAmSeniorClass";
  }
}
public class JuniorClass extends SeniorClass {
  private String name;
  public JuniorClass(){
    super(); //Automatic call leads to NullPointerException
    name = "JuniorClass";
  }
  public String toString(){
    return name.toUpperCase();
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.DetachedTestCaseRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#detachedtestcase"
         language="java"
         message="Probable detached JUnit test case."
         name="DetachedTestCase"
         since="6.13.0">
      <description>
The method appears to be a test case since it has public or default visibility,
non-static access, no arguments, no return value, has no annotations, but is a
member of a class that has one or more JUnit test cases. If it is a utility
method, it should likely have private visibility. If it is an ignored test, it
should be annotated with @Test and @Ignore.
        </description>
      <priority>3</priority>
      <example>

public class MyTest {
    @Test
    public void someTest() {
    }

    // violation: Not annotated
    public void someOtherTest () {
    }

}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#donotcallgarbagecollectionexplicitly"
         language="java"
         message="Do not explicitly trigger a garbage collection."
         name="DoNotCallGarbageCollectionExplicitly"
         since="4.2">
      <description>
Calls to `System.gc()`, `Runtime.getRuntime().gc()`, and `System.runFinalization()` are not advised.
Code should have the same behavior whether the garbage collection is disabled using the option
`-Xdisableexplicitgc` or not.

Moreover, "modern" JVMs do a very good job handling garbage collections. If memory usage issues unrelated to memory
leaks develop within an application, it should be dealt with JVM options rather than within the code itself.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[&#xA;       pmd-java:matchesSig(&#34;java.lang.System#gc()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Runtime#gc()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.System#runFinalization()&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Runtime#runFinalization()&#34;)&#xA;]"/>
      </properties>
      <example>

public class GCCall {
    public GCCall() {
        // Explicit gc call !
        System.gc();
    }

    public void doSomething() {
        // Explicit gc call !
        Runtime.getRuntime().gc();
    }

    public explicitGCcall() {
        // Explicit gc call !
        System.gc();
    }

    public void doSomething() {
        // Explicit gc call !
        Runtime.getRuntime().gc();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#donotextendjavalangthrowable"
         language="java"
         message="Exceptions should not extend java.lang.Throwable"
         name="DoNotExtendJavaLangThrowable"
         since="6.0.0">
      <description>
Extend Exception or RuntimeException instead of Throwable.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration/ExtendsList/ClassType&#xA;  [pmd-java:typeIsExactly('java.lang.Throwable')]"/>
      </properties>
      <example>

public class Foo extends Throwable { }

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#donothardcodesdcard"
         language="java"
         message="Do not hardcode /sdcard."
         name="DoNotHardCodeSDCard"
         since="4.2.6">
      <description>
Use Environment.getExternalStorageDirectory() instead of "/sdcard"
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath" value="//StringLiteral[starts-with(@Image,'&#34;/sdcard')]"/>
      </properties>
      <example>

public class MyActivity extends Activity {
    protected void foo() {
        String storageLocation = "/sdcard/mypackage";   // hard-coded, poor approach

       storageLocation = Environment.getExternalStorageDirectory() + "/mypackage"; // preferred approach
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#donotterminatevm"
         language="java"
         message="System.exit() should not be used in J2EE/JEE apps"
         name="DoNotTerminateVM"
         since="4.1">
      <description>
Web applications should not call `System.exit()`, since only the web container or the
application server should stop the JVM. Otherwise a web application would terminate all other applications
running on the same application server.

This rule also checks for the equivalent calls `Runtime.getRuntime().exit()` and `Runtime.getRuntime().halt()`.

This rule has been renamed from "DoNotCallSystemExit" in PMD 6.29.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//(MethodDeclaration[@MainMethod = false()] | Initializer)//MethodCall[&#xA;    pmd-java:matchesSig(&#34;java.lang.System#exit(int)&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Runtime#exit(int)&#34;)&#xA;    or pmd-java:matchesSig(&#34;java.lang.Runtime#halt(int)&#34;)&#xA;]"/>
      </properties>
      <example>

public void bar() {
    System.exit(0);                 // never call this when running in an application server!
}
public void foo() {
    Runtime.getRuntime().exit(0);   // never stop the JVM manually, the container will do this.
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#donotthrowexceptioninfinally"
         language="java"
         message="A throw statement in a finally block makes the control flow hard to understand."
         name="DoNotThrowExceptionInFinally"
         since="4.2">
      <description>
Throwing exceptions within a 'finally' block is confusing since they may mask other exceptions
or code defects.
Note: This is a PMD implementation of the Lint4j rule "A throw in a finally block"
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath" value="//FinallyClause[descendant::ThrowStatement]"/>
      </properties>
      <example>

public class Foo {
    public void bar() {
        try {
            // Here do some stuff
        } catch( Exception e) {
            // Handling the issue
        } finally {
            // is this really a good idea ?
            throw new Exception();
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#dontimportsun"
         language="java"
         message="Avoid importing anything from the 'sun.*' packages"
         name="DontImportSun"
         since="1.5">
      <description>
Avoid importing anything from the 'sun.*' packages. These packages are not portable
and are likely to change.

If you find yourself having to depend on Sun APIs, confine this dependency to as
small a scope as possible, for instance by writing a stable wrapper class around
the unstable API. You can then suppress this rule in the implementation of the wrapper.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ImportDeclaration[starts-with(@ImportedName, 'sun.')]"/>
      </properties>
      <example>

import sun.misc.foo;
public class Foo {}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#dontusefloattypeforloopindices"
         language="java"
         message="Don't use floating point for loop indices. If you must use floating point, use double."
         name="DontUseFloatTypeForLoopIndices"
         since="4.3">
      <description>
Don't use floating point for loop indices. If you must use floating point, use double
unless you're certain that float provides enough precision and you have a compelling
performance need (space or time).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ForStatement/ForInit//VariableId[pmd-java:typeIs('float')]"/>
      </properties>
      <example>

public class Count {
  public static void main(String[] args) {
    final int START = 2000000000;
    int count = 0;
    for (float f = START; f &lt; START + 50; f++)
      count++;
      //Prints 0 because (float) START == (float) (START + 50).
      System.out.println(count);
      //The termination test misbehaves due to floating point granularity.
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#emptycatchblock"
         language="java"
         message="Avoid empty catch blocks"
         name="EmptyCatchBlock"
         since="0.1">
      <description>
Empty Catch Block finds instances where an exception is caught, but nothing is done.
In most circumstances, this swallows an exception which should either be acted on
or reported.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CatchClause[&#xA;  Block[&#xA;      count(*) = 0&#xA;      and ($allowCommentedBlocks = false() or @containsComment = false())&#xA;  ]&#xA;  and CatchParameter/VariableId[not(matches(@Name, $allowExceptionNameRegex))]&#xA;]"/>
         <property description="Empty blocks containing comments will be skipped"
                   name="allowCommentedBlocks"
                   type="Boolean">
            <value>false</value>
         </property>
         <property description="Empty blocks catching exceptions with names matching this regular expression will be skipped"
                   name="allowExceptionNameRegex"
                   type="Regex">
            <value>^(ignored|expected)$</value>
         </property>
      </properties>
      <example>

public void doSomething() {
    try {
        FileInputStream fis = new FileInputStream("/tmp/bugger");
    } catch (IOException ioe) {
        // not good
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#emptyfinalizer"
         language="java"
         message="Avoid empty finalize methods"
         name="EmptyFinalizer"
         since="1.5">
      <description>
Empty finalize methods serve no purpose and should be removed. Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name='finalize'][@Arity = 0]&#xA;  /Block[not(*)]"/>
      </properties>
      <example>

public class Foo {
   protected void finalize() {}
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#equalsnull"
         language="java"
         message="Avoid using equals() to compare against null"
         name="EqualsNull"
         since="1.9">
      <description>
Tests for null should not use the equals() method. The '==' operator should be used instead.
        </description>
      <priority>1</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[@MethodName = &#34;equals&#34; and ArgumentList[count(*) = 1 and NullLiteral]]"/>
      </properties>
      <example>

String x = "foo";

if (x.equals(null)) {   // bad form
    doSomething();
}

if (x == null) {        // preferred
    doSomething();
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#finalizedoesnotcallsuperfinalize"
         language="java"
         message="Last statement in finalize method should be a call to super.finalize()"
         name="FinalizeDoesNotCallSuperFinalize"
         since="1.5">
      <description>
If the finalize() is implemented, its last action should be to call super.finalize. Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name = &#34;finalize&#34;][@Arity = 0]&#xA;   /Block/*[last()]&#xA;      [not(MethodCall[@MethodName = &#34;finalize&#34;]/SuperExpression)]&#xA;      [not(FinallyClause/Block/ExpressionStatement/&#xA;          MethodCall[@MethodName = &#34;finalize&#34;]/SuperExpression)]"/>
      </properties>
      <example>

protected void finalize() {
    something();
    // neglected to call super.finalize()
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#finalizeonlycallssuperfinalize"
         language="java"
         message="Finalize should do something besides just calling super.finalize()"
         name="FinalizeOnlyCallsSuperFinalize"
         since="1.5">
      <description>
If the finalize() is implemented, it should do something besides just calling super.finalize(). Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name='finalize'][@Arity = 0]&#xA;   [Block[@Size=1]/ExpressionStatement/MethodCall[@MethodName = &#34;finalize&#34;][SuperExpression]]"/>
      </properties>
      <example>

protected void finalize() {
    super.finalize();
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#finalizeoverloaded"
         language="java"
         message="Finalize methods should not be overloaded"
         name="FinalizeOverloaded"
         since="1.5">
      <description>
Methods named finalize() should not have parameters.  It is confusing and most likely an attempt to
overload Object.finalize(). It will not be called by the VM.

Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath" value="//MethodDeclaration[@Name='finalize'][@Arity &gt; 0]"/>
      </properties>
      <example>

public class Foo {
    // this is confusing and probably a bug
    protected void finalize(int a) {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#finalizeshouldbeprotected"
         language="java"
         message="If you override finalize(), make it protected"
         name="FinalizeShouldBeProtected"
         since="1.1">
      <description>
When overriding the finalize(), the new method should be set as protected.  If made public,
other classes may invoke it at inappropriate times.

Note that Oracle has declared Object.finalize() as deprecated since JDK 9.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Visibility != &#34;protected&#34;][@Name='finalize'][@Arity = 0]"/>
      </properties>
      <example>

public void finalize() {
    // do something
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.IdempotentOperationsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#idempotentoperations"
         language="java"
         message="Avoid idempotent operations (like assigning a variable to itself)."
         name="IdempotentOperations"
         since="2.0">
      <description>
Avoid idempotent operations - they have no effect.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
 public void bar() {
  int x = 2;
  x = x;
 }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.ImplicitSwitchFallThroughRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#implicitswitchfallthrough"
         language="java"
         message="This switch case may be reached by fallthrough from the previous case"
         name="ImplicitSwitchFallThrough"
         since="3.0">
      <description>
Switch statements without break or return statements for each case option
may indicate problematic behaviour. Empty cases are ignored as these indicate
an intentional fall-through.

You can ignore a violation by commenting `// fallthrough` before the case label
which is reached by fallthrough, or with `@SuppressWarnings("fallthrough")`.

This rule has been renamed from "MissingBreakInSwitch" in PMD 6.37.0.
        </description>
      <priority>3</priority>
      <example>

public void bar(int status) {
    switch(status) {
      case CANCELLED:
        doCancelled();
        // break; hm, should this be commented out?
      case NEW:
        doNew();
        // is this really a fall-through?
        // what happens if you add another case after this one?
      case REMOVED:
        doRemoved();
        // fallthrough - this comment just clarifies that you want a fallthrough
      case OTHER: // empty case - this is interpreted as an intentional fall-through
      case ERROR:
        doErrorHandling();
        break;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#instantiationtogetclass"
         language="java"
         message="Avoid instantiating an object just to call getClass() on it; use the .class public member instead"
         name="InstantiationToGetClass"
         since="2.0">
      <description>
Avoid instantiating an object just to call getClass() on it; use the .class public member instead.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall&#xA; [@MethodName='getClass']&#xA; [ConstructorCall]"/>
      </properties>
      <example>

// replace this
Class c = new String().getClass();

// with this:
Class c = String.class;

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.InvalidLogMessageFormatRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#invalidlogmessageformat"
         language="java"
         message="Invalid message format"
         name="InvalidLogMessageFormat"
         since="5.5.0">
      <description>
Check for messages in slf4j and log4j2 (since 6.19.0) loggers with non matching number of arguments and placeholders.

Since 6.32.0 in addition to parameterized message placeholders (`{}`) also format specifiers of string formatted
messages are supported (`%s`).

This rule has been renamed from "InvalidSlf4jMessageFormat" in PMD 6.19.0.
        </description>
      <priority>5</priority>
      <example>

LOGGER.error("forget the arg {}");
LOGGER.error("forget the arg %s");
LOGGER.error("too many args {}", "arg1", "arg2");
LOGGER.error("param {}", "arg1", new IllegalStateException("arg")); //The exception is shown separately, so is correct.

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#jumbledincrementer"
         language="java"
         message="Avoid modifying an outer loop incrementer in an inner loop for update expression"
         name="JumbledIncrementer"
         since="1.0">
      <description>
Avoid jumbled loop incrementers - it's usually a mistake, and is confusing even if intentional.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ForStatement&#xA;    [not(ForInit) or ForInit//VariableId/@Name != ForUpdate//VariableAccess/@Name]&#xA;    [ForUpdate//VariableAccess[@AccessType = 'WRITE']/@Name&#xA;     =&#xA;     ancestor::ForStatement/ForInit//VariableId/@Name&#xA;    ]"/>
      </properties>
      <example>
 
public class JumbledIncrementerRule1 {
    public void foo() {
        for (int i = 0; i &lt; 10; i++) {          // only references 'i'
            for (int k = 0; k &lt; 20; i++) {      // references both 'i' and 'k'
                System.out.println("Hello");
            }
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.JUnitSpellingRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#junitspelling"
         language="java"
         message="You may have misspelled a JUnit framework method (setUp or tearDown)"
         name="JUnitSpelling"
         since="1.0">
      <description>
            In JUnit 3, the setUp method is used to set up all data entities required in running tests.
            The tearDown method is used to clean up all data entities required in running tests.
            You should not misspell method name if you want your test to set up and clean up everything correctly.
        </description>
      <priority>3</priority>
      <example>

import junit.framework.*;

public class Foo extends TestCase {
    public void setup() {}    // oops, should be setUp
    public void TearDown() {} // oops, should be tearDown
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.JUnitStaticSuiteRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#junitstaticsuite"
         language="java"
         message="You have a suite() method that is not both public and static, so JUnit won't call it to get your TestSuite.  Is that what you wanted to do?"
         name="JUnitStaticSuite"
         since="1.0">
      <description>
The suite() method in a JUnit test needs to be both public and static.
        </description>
      <priority>3</priority>
      <example>

import junit.framework.*;

public class Foo extends TestCase {
    public void suite() {}         // oops, should be static
}

        </example>
      <example>

import junit.framework.*;

public class Foo extends TestCase {
    private static void suite() {} // oops, should be public
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#methodwithsamenameasenclosingclass"
         language="java"
         message="A method should not have the same name as its containing class"
         name="MethodWithSameNameAsEnclosingClass"
         since="1.5">
      <description>
A method should not have the same name as its containing class.
This would be confusing as it would look like a constructor. 
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name = ancestor::ClassDeclaration/@SimpleName]"/>
      </properties>
      <example>

public class MyClass {

    public MyClass() {}         // this is OK because it is a constructor

    public void MyClass() {}    // this is bad because it is a method
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#misplacednullcheck"
         language="java"
         message="The null check here is misplaced; if the variable ''{0}'' is null there will be a NullPointerException"
         name="MisplacedNullCheck"
         since="3.5">
      <description>
The null check here is misplaced. If the variable is null a `NullPointerException` will be thrown.
Either the check is useless (the variable will never be `null`) or it is incorrect.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//InfixExpression[@Operator = '&amp;&amp;']&#xA;    /InfixExpression[@Operator = '!=']&#xA;        (: one side is null :)&#xA;        [NullLiteral]&#xA;        (: other side checks for the variable used somewhere in the first child of conditional and expression :)&#xA;        [VariableAccess]&#xA;        [some $var in preceding-sibling::*//VariableAccess&#xA;            [parent::MethodCall or parent::FieldAccess]&#xA;            [not(ancestor::InfixExpression[@Operator = '||'])]&#xA;            /@Name&#xA;            satisfies $var = VariableAccess/@Name&#xA;        ]&#xA;    /VariableAccess&#xA;|&#xA;//InfixExpression[@Operator = '||']&#xA;    /InfixExpression[@Operator = '==']&#xA;        (: one side is null :)&#xA;        [NullLiteral]&#xA;        (: other side checks for the variable used somewhere in the first child of conditional or expression :)&#xA;        [VariableAccess]&#xA;        [some $var in preceding-sibling::*//VariableAccess&#xA;            [parent::MethodCall or parent::FieldAccess]&#xA;            [not(ancestor::InfixExpression[@Operator = '&amp;&amp;'])]&#xA;            /@Name&#xA;            satisfies $var = VariableAccess/@Name&#xA;        ]&#xA;    /VariableAccess"/>
      </properties>
      <example>

public class Foo {
    void bar() {
        if (a.equals(baz) &amp;&amp; a != null) {} // a could be null, misplaced null check

        if (a != null &amp;&amp; a.equals(baz)) {} // correct null check
    }
}

        </example>
      <example>

public class Foo {
    void bar() {
        if (a.equals(baz) || a == null) {} // a could be null, misplaced null check

        if (a == null || a.equals(baz)) {} // correct null check
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#missingserialversionuid"
         language="java"
         message="Classes implementing Serializable should set a serialVersionUID"
         name="MissingSerialVersionUID"
         since="3.0">
      <description>
Serializable classes should provide a serialVersionUID field.
The serialVersionUID field is also needed for abstract base classes. Each individual class in the inheritance
chain needs an own serialVersionUID field. See also [Should an abstract class have a serialVersionUID](https://stackoverflow.com/questions/893259/should-an-abstract-class-have-a-serialversionuid).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;    [@Interface = false()]&#xA;    [count(ClassBody/FieldDeclaration/VariableDeclarator/VariableId[@Name='serialVersionUID']) = 0]&#xA;    [(ImplementsList | ExtendsList)/ClassType[pmd-java:typeIs('java.io.Serializable')]]"/>
      </properties>
      <example>

public class Foo implements java.io.Serializable {
    String name;
    // Define serialization id to avoid serialization related bugs
    // i.e., public static final long serialVersionUID = 4328743;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#missingstaticmethodinnoninstantiatableclass"
         language="java"
         message="Class cannot be instantiated and does not provide any static methods or fields"
         name="MissingStaticMethodInNonInstantiatableClass"
         since="3.0">
      <description>
A class that has private constructors and does not have any static methods or fields cannot be used.

When one of the private constructors is annotated with one of the annotations, then the class is not considered
non-instantiatable anymore and no violation will be reported.
See the property `annotations`.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="let $topLevelClass := /*/ClassDeclaration return&#xA;let $isLombokUtility := exists($topLevelClass[pmd-java:hasAnnotation('lombok.experimental.UtilityClass')]) return&#xA;$topLevelClass[&#xA;        (: non-instantiable :)&#xA;        $isLombokUtility or&#xA;        (&#xA;            (: no lombok produced constructors :)&#xA;            not(pmd-java:hasAnnotation('lombok.NoArgsConstructor') or&#xA;                pmd-java:hasAnnotation('lombok.RequiredArgsConstructor') or&#xA;                pmd-java:hasAnnotation('lombok.AllArgsConstructor') or&#xA;                pmd-java:hasAnnotation('lombok.Builder')) and&#xA;            (: or has non-default constructors … :)&#xA;            ClassBody/ConstructorDeclaration and&#xA;                (: … but only private … :)&#xA;                not(ClassBody/ConstructorDeclaration[@Visibility != &#34;private&#34;]) and&#xA;                (: … and none annotated … :)&#xA;                (every $x in $annotations satisfies&#xA;                      not(ClassBody/ConstructorDeclaration/ModifierList/Annotation[pmd-java:typeIs($x)]))&#xA;        )&#xA;    ]&#xA;    [&#xA;        (: With no visible static methods … :)&#xA;        not(ClassBody/MethodDeclaration[($isLombokUtility or pmd-java:modifiers() = &#34;static&#34;) and @Visibility != &#34;private&#34;]) and&#xA;        (: … nor fields … :)&#xA;        not(ClassBody/FieldDeclaration[($isLombokUtility or pmd-java:modifiers() = &#34;static&#34;) and @Visibility != &#34;private&#34;]) and&#xA;        (: … no nested classes, that are non-private and static … :)&#xA;        not(ClassBody/ClassDeclaration&#xA;            [pmd-java:modifiers() = &#34;static&#34; and @Visibility != &#34;private&#34;]&#xA;            (: … and a non-private method returning the outer class type … :)&#xA;            [(ClassBody/MethodDeclaration&#xA;                [@Visibility != &#34;private&#34;]&#xA;                [descendant::ReturnStatement/*[1][pmd-java:typeIs(ancestor::ClassDeclaration[@Nested = false()]/@BinaryName)]]&#xA;            ) or (&#xA;                (: … or the inner class extends the outer class :)&#xA;                ExtendsList/ClassType[@SimpleName = ancestor::ClassDeclaration[@Nested = false()]/@SimpleName]&#xA;            )]&#xA;    )]"/>
         <property description="If a constructor is annotated with one of these annotations, then the class is ignored."
                   name="annotations"
                   type="List[String]">
            <value>org.springframework.beans.factory.annotation.Autowired,jakarta.inject.Inject,com.google.inject.Inject,lombok.Builder</value>
         </property>
      </properties>
      <example>

// This class is unusable, since it cannot be
// instantiated (private constructor),
// and no static method can be called.

public class Foo {
  private Foo() {}
  void foo() {}
}


        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#morethanonelogger"
         language="java"
         message="Class contains more than one logger."
         name="MoreThanOneLogger"
         since="2.0">
      <description>
Normally only one logger is used in each class. This rule supports slf4j, log4j, Java Util Logging and
log4j2 (since 6.19.0).
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[&#xA;  count(&#xA;    ClassBody/FieldDeclaration/ClassType[&#xA;      pmd-java:typeIs(&#34;org.apache.log4j.Logger&#34;) or&#xA;      pmd-java:typeIs(&#34;org.apache.logging.log4j.Logger&#34;) or&#xA;      pmd-java:typeIs(&#34;java.util.logging.Logger&#34;) or&#xA;      pmd-java:typeIs(&#34;org.slf4j.Logger&#34;)&#xA;    ]&#xA;  ) &gt; 1&#xA;]"/>
      </properties>
      <example>

public class Foo {
    Logger log = Logger.getLogger(Foo.class.getName());
    // It is very rare to see two loggers on a class, normally
    // log information is multiplexed by levels
    Logger log2= Logger.getLogger(Foo.class.getName());
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#noncaselabelinswitch"
         language="java"
         message="A non-case label was present in a switch statement or expression"
         name="NonCaseLabelInSwitch"
         since="1.5">
      <description>
A non-case label (e.g. a named break/continue label) was present in a switch statement or switch expression.
This is legal, but confusing. It is easy to mix up the case labels and the non-case labels.

Note: This rule was renamed from `NonCaseLabelInSwitchStatement` with PMD 7.7.0.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//(SwitchStatement|SwitchExpression)//LabeledStatement"/>
      </properties>
      <example>

public class Foo {
  void bar(int a) {
   switch (a) {
     case 1:
       // do something
     mylabel: // this is legal, but confusing!
       break;
     default:
       break;
    }
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.NonSerializableClassRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#nonserializableclass"
         language="java"
         message="The field ''{0}'' of serializable class ''{1}'' is of non-serializable type ''{2}''."
         name="NonSerializableClass"
         since="1.1">
      <description>
If a class is marked as `Serializable`, then all fields need to be serializable as well. In order to exclude
a field, it can be marked as transient. Static fields are not considered.

This rule reports all fields, that are not serializable.

If a class implements the methods to perform manual serialization (`writeObject`, `readObject`) or uses
a replacement object (`writeReplace`, `readResolve`) then this class is ignored.

Note: This rule has been revamped with PMD 6.52.0. It was previously called "BeanMembersShouldSerialize".
The property `prefix` has been deprecated, since in a serializable class all fields have to be
serializable regardless of the name.
        </description>
      <priority>3</priority>
      <example>
            
class Buzz implements java.io.Serializable {
    private static final long serialVersionUID = 1L;

    private transient int someFoo;          // good, it's transient
    private static int otherFoo;            // also OK, it's static
    private java.io.FileInputStream stream; // bad - FileInputStream is not serializable

    public void setStream(FileInputStream stream) {
        this.stream = stream;
    }

    public int getSomeFoo() {
          return this.someFoo;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#nonstaticinitializer"
         language="java"
         message="Non-static initializers are confusing"
         name="NonStaticInitializer"
         since="1.5">
      <description>
A non-static initializer block will be called any time a constructor is invoked (just prior to
invoking the constructor).  While this is a valid language construct, it is rarely used and is
confusing.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//Initializer[@Static=false()][not(ancestor::*[3][self::ConstructorCall or self::EnumConstant])]"/>
      </properties>
      <example>

public class MyClass {
  // this block gets run before any call to a constructor
  {
    System.out.println("I am about to construct myself");
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.NullAssignmentRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#nullassignment"
         language="java"
         message="Assigning an Object to null is a code smell.  Consider refactoring."
         name="NullAssignment"
         since="1.02">
      <description>
Assigning a "null" to a variable (outside of its declaration) is usually bad form.  Sometimes, this type
of assignment is an indication that the programmer doesn't completely understand what is going on in the code.

NOTE: This sort of assignment may used in some cases to dereference objects and encourage garbage collection.
        </description>
      <priority>3</priority>
      <example>
 
public void bar() {
  Object x = null; // this is OK
  x = new Object();
     // big, complex piece of code here
  x = null; // this is not required
     // big, complex piece of code here
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.OverrideBothEqualsAndHashcodeRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#overridebothequalsandhashcode"
         language="java"
         message="Ensure you override both equals() and hashCode()"
         name="OverrideBothEqualsAndHashcode"
         since="0.4">
      <description>
Override both `public boolean Object.equals(Object other)` and `public int Object.hashCode()` or override neither.
Even if you are inheriting a `hashCode()` from a parent class, consider implementing `hashCode()` and explicitly
delegating to your superclass.

Not overriding both methods could violate the contract between `equals()` and `hashCode()`. Most importantly,
if two instances are equal, then they must have the same hash code. Using such invalid instances in hash-based
collections like `HashSet` or `HashMap` could lead to duplicated or missing entries.

This rule does not consider types that implement `Comparable`. There is a separate rule
{% rule OverrideBothEqualsAndHashCodeOnComparable %} for this.
        </description>
      <priority>3</priority>
      <example>

public class Bar {        // poor, missing a hashcode() method
    public boolean equals(Object o) {
      // do some comparison
    }
}

public class Baz {        // poor, missing an equals() method
    public int hashCode() {
      // return some hash value
    }
}

public class Foo {        // perfect, both methods provided
    public boolean equals(Object other) {
      // do some comparison
    }
    public int hashCode() {
      // return some hash value
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.OverrideBothEqualsAndHashCodeOnComparableRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#overridebothequalsandhashcodeoncomparable"
         language="java"
         message="When implementing Comparable both equals() and hashCode() should be overridden"
         name="OverrideBothEqualsAndHashCodeOnComparable"
         since="7.17.0">
      <description>
            Classes that implement `Comparable` should override both `equals()` and
            `hashCode()` if instances of these classes are used in collections. This is
            to ensure that the class's natural ordering is consistent with `equals()`.
            Failing to do so can lead to unexpected behavior in sets which then do not
            conform to the `Set` interface. While the `Set` interface relies on
            `equals()` to determine object equality, sorted sets like `TreeSet` use
            `compareTo()` instead. The same issue can arise when such objects are used
            as keys in sorted maps.

            Note 1: This rule is related to {% rule OverrideBothEqualsAndHashcode %}. It
            will report missing `equals()` and/or `hashCode()` methods for classes only
            that implement `Comparable`.

            Note 2: This rule also reports records that implement `Comparable`. While
            for records, `equals()` and `hashCode()` are generated, you still must make
            sure that `compareTo()` is consistent with `equals()`.
        </description>
      <priority>3</priority>
      <example>
            
public class Bar implements Comparable&lt;Bar&gt; {  // poor - missing equals() and hashCode()
    public int compareTo(Bar other) {
        // some comparison
    }
}

public class Baz implements Comparable&lt;Baz&gt; {  // poor - missing hashCode()
    public int compareTo(Baz other) {
        // some comparison
    }
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        return compareTo((Baz) o) == 0;
    }
}

public class Foo implements Comparable&lt;Foo&gt; {  // correct
    public int compareTo(Foo other) {
        // some comparison
    }
    public boolean equals(Object o) {
        if (o == null || getClass() != o.getClass()) {
            return false;
        }
        return compareTo((Foo) o) == 0;
    }
    public int hashCode() {
        // return hash code
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.ProperCloneImplementationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#propercloneimplementation"
         language="java"
         message="Object clone() should be implemented with super.clone()"
         name="ProperCloneImplementation"
         since="1.4">
      <description>
Object clone() should be implemented with super.clone().
        </description>
      <priority>2</priority>
      <example>

class Foo{
    public Object clone(){
        return new Foo(); // This is bad
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#properlogger"
         language="java"
         message="Logger should be defined private static final and have the correct class"
         name="ProperLogger"
         since="3.3">
      <description>
A logger should normally be defined private static final and be associated with the correct class.
`private final Log log;` is also allowed for rare cases where loggers need to be passed around,
with the restriction that the logger needs to be passed into the constructor.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FieldDeclaration&#xA;    [ClassType[pmd-java:typeIs($loggerClass)]]&#xA;    [&#xA;        (: check modifiers :)&#xA;        (not(pmd-java:modifiers() = 'private') or not(pmd-java:modifiers() = 'final'))&#xA;        (: check logger name :)&#xA;        or (pmd-java:modifiers() = 'static' and VariableDeclarator/VariableId/@Name != $staticLoggerName)&#xA;        or (not(pmd-java:modifiers() = 'static') and VariableDeclarator/VariableId/@Name != $loggerName)&#xA;        (: check logger argument type matches class or enum name :)&#xA;        or .//ArgumentList/ClassLiteral/ClassType/@SimpleName != ancestor::ClassDeclaration/@SimpleName&#xA;        or .//ArgumentList/ClassLiteral/ClassType/@SimpleName != ancestor::EnumDeclaration/@SimpleName&#xA;&#xA;        (: special case - final logger initialized inside constructor :)&#xA;        or (VariableDeclarator/@Initializer = false()&#xA;            and not(pmd-java:modifiers() = 'static')&#xA;            and not(ancestor::ClassBody/ConstructorDeclaration&#xA;                //AssignmentExpression[@Operator = '=']&#xA;                    [FieldAccess[1]/@Name = $loggerName or VariableAccess[1]/@Name = $loggerName]&#xA;                    [*[2][@Name = ancestor::ConstructorDeclaration//FormalParameter/VariableId/@Name]])&#xA;        )&#xA;    ]"/>
         <property description="Name of the static Logger variable"
                   name="staticLoggerName"
                   type="String">
            <value>LOG</value>
         </property>
         <property description="Name of the Logger instance variable"
                   name="loggerName"
                   type="String">
            <value>log</value>
         </property>
         <property description="Class name of the logger"
                   name="loggerClass"
                   type="String">
            <value>org.apache.commons.logging.Log</value>
         </property>
      </properties>
      <example>
 
public class Foo {

    private static final Log LOG = LogFactory.getLog(Foo.class);    // proper way

    protected Log LOG = LogFactory.getLog(Testclass.class);         // wrong approach
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#replacejavautilcalendar"
         language="java"
         message="Usage of java.util.Calendar should be replaced with classes from java.time"
         minimumLanguageVersion="1.8"
         name="ReplaceJavaUtilCalendar"
         since="7.16.0">
      <description>
The legacy `java.util.Calendar` API is error-prone, mutable, and not thread-safe. It has confusing month indexing
(January = 0), inconsistent field semantics, and verbose usage patterns. The modern `java.time API`
(introduced in Java 8) provides immutable, thread-safe alternatives with clear, intuitive methods.

Use `LocalDate` (for date-only operations), `LocalDateTime` (for date and time), or `ZonedDateTime`
(when timezone is important) from `java.time` package instead.

Note: Since JPA 3.2 (Jakarta Persistence) usage of `java.util.Date` and `java.util.Calendar` and others is
deprecated there as well in favour of `java.time` API.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//LocalVariableDeclaration[ClassType[pmd-java:typeIs('java.util.Calendar')]] |&#xA;                //FormalParameter[ClassType[pmd-java:typeIs('java.util.Calendar')]] |&#xA;                //FieldDeclaration[ClassType[pmd-java:typeIs('java.util.Calendar')]] |&#xA;                //ConstructorCall[ClassType[pmd-java:typeIs('java.util.Calendar')]]"/>
      </properties>
      <example>
            
import java.util.Calendar;
import java.util.GregorianCalendar;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.ZonedDateTime;

public class Foo {
    void problematic() {
        // Problematic - using legacy Calendar API
        Calendar cal = Calendar.getInstance();
        cal.set(2024, Calendar.JANUARY, 15); // Month indexing is confusing (0-based)
        cal.add(Calendar.DAY_OF_MONTH, 7);
        
        Calendar specific = new GregorianCalendar(2024, 0, 15); // Also problematic
    }
    
    void preferred() {
        // Preferred - using modern java.time API
        LocalDate date = LocalDate.of(2024, 1, 15); // Month indexing is intuitive (1-based)
        LocalDate weekLater = date.plusDays(7);
        
        LocalDateTime dateTime = LocalDateTime.now();
        ZonedDateTime zonedDateTime = ZonedDateTime.now();
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#replacejavautildate"
         language="java"
         message="Usage of java.util.Date should be replaced with classes from java.time"
         minimumLanguageVersion="1.8"
         name="ReplaceJavaUtilDate"
         since="7.16.0">
      <description>
The legacy `java.util.Date` class is mutable, not thread-safe, and has a confusing API. Many of its methods
are deprecated, it doesn't handle timezones properly, and it represents both date and time even when only
one is needed. The constructor parameters are particularly error-prone: year is "years since 1900" and
month is 0-based (January = 0). The modern java.time API (introduced in Java 8) provides better type safety,
immutability, and clearer semantics.

Use `LocalDate` (date only), `LocalTime` (time only), `LocalDateTime` (date and time), `Instant` (timestamp),
or `ZonedDateTime` (date-time with timezone) from `java.time` package instead.

Note: This includes subtypes such as `java.sql.Date`, `java.sql.Time` and `java.sql.Timestamp`.
Since JPA 3.2 (Jakarta Persistence) usage of `java.util.Date` and `java.util.Calendar` and others is
deprecated there as well in favour of `java.time` API.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//LocalVariableDeclaration[ClassType[pmd-java:typeIs('java.util.Date')]] |&#xA;                //FormalParameter[ClassType[pmd-java:typeIs('java.util.Date')]] |&#xA;                //FieldDeclaration[ClassType[pmd-java:typeIs('java.util.Date')]] |&#xA;                //ConstructorCall[ClassType[pmd-java:typeIs('java.util.Date')]]"/>
      </properties>
      <example>
            
import java.util.Date;
import java.time.Instant;
import java.time.LocalDate;
import java.time.LocalDateTime;
import java.time.ZonedDateTime;

public class Foo {
    void problematic() {
        // Problematic - using legacy Date API
        Date now = new Date();
        Date epoch = new Date(0L);
        Date custom = new Date(124, 0, 15); // Deprecated constructor: year=1900+124=2024, month=0=January
        
        // Mutable operations are error-prone
        now.setTime(System.currentTimeMillis());
    }
    
    void preferred() {
        // Preferred - using modern java.time API
        Instant now = Instant.now(); // For timestamps
        LocalDate today = LocalDate.now(); // For date only
        LocalDateTime dateTime = LocalDateTime.now(); // For date and time
        ZonedDateTime zonedDateTime = ZonedDateTime.now(); // With timezone
        
        // Immutable operations are safer
        LocalDate tomorrow = today.plusDays(1);
        LocalDateTime nextHour = dateTime.plusHours(1);
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#returnemptycollectionratherthannull"
         language="java"
         message="Return an empty collection rather than 'null'."
         name="ReturnEmptyCollectionRatherThanNull"
         since="6.37.0">
      <description>
For any method that returns an collection (such as an array, Collection or Map), it is better to return
an empty one rather than a null reference. This removes the need for null checking all results and avoids
inadvertent NullPointerExceptions.

See Effective Java, 3rd Edition, Item 54: Return empty collections or arrays instead of null
        </description>
      <priority>1</priority>
      <properties>
         <property name="xpath"
                   value="//ReturnStatement/NullLiteral&#xA;[ancestor::MethodDeclaration[1]&#xA;    [ArrayType&#xA;     or ClassType[pmd-java:typeIs('java.util.Collection')&#xA;        or pmd-java:typeIs('java.util.Map')]]&#xA;]&#xA;[not(./ancestor::LambdaExpression)]"/>
      </properties>
      <example>

public class Example {
    // Not a good idea...
    public int[] badBehavior() {
        // ...
        return null;
    }

    // Good behavior
    public String[] bonnePratique() {
        //...
        return new String[0];
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#returnfromfinallyblock"
         language="java"
         message="Avoid returning from a finally block"
         name="ReturnFromFinallyBlock"
         since="1.05">
      <description>
Avoid returning from a finally block, this can discard exceptions.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//FinallyClause//ReturnStatement except //FinallyClause//(MethodDeclaration|LambdaExpression)//ReturnStatement"/>
      </properties>
      <example>

public class Bar {
    public String foo() {
        try {
            throw new Exception( "My Exception" );
        } catch (Exception e) {
            throw e;
        } finally {
            return "A. O. K."; // return not recommended here
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#simpledateformatneedslocale"
         language="java"
         message="When instantiating a SimpleDateFormat object, specify a Locale"
         name="SimpleDateFormatNeedsLocale"
         since="2.0">
      <description>
Be sure to specify a Locale when creating SimpleDateFormat instances to ensure that locale-appropriate
formatting is used.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ConstructorCall&#xA;    [pmd-java:typeIs('java.text.SimpleDateFormat')]&#xA;    [ArgumentList/@Size = 1]"/>
      </properties>
      <example>

public class Foo {
  // Should specify Locale.US (or whatever)
  private SimpleDateFormat sdf = new SimpleDateFormat("pattern");
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.SingleMethodSingletonRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#singlemethodsingleton"
         language="java"
         message="Class contains multiple getInstance methods. Please review."
         name="SingleMethodSingleton"
         since="5.4">
      <description>
Some classes contain overloaded getInstance. The problem with overloaded getInstance methods
is that the instance created using the overloaded method is not cached and so,
for each call and new objects will be created for every invocation.
        </description>
      <priority>2</priority>
      <example>

public class Singleton {

    private static Singleton singleton = new Singleton( );

    private Singleton(){ }

    public static Singleton getInstance( ) {
        return singleton;
    }

    public static Singleton getInstance(Object obj){
        Singleton singleton = (Singleton) obj;
        return singleton;           //violation
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.SingletonClassReturningNewInstanceRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#singletonclassreturningnewinstance"
         language="java"
         message="getInstance method always creates a new object and hence does not comply to Singleton Design Pattern behaviour. Please review"
         name="SingletonClassReturningNewInstance"
         since="5.4">
      <description>
            A singleton class should only ever have one instance. Failure to check
            whether an instance has already been created may result in multiple
            instances being created.
        </description>
      <priority>2</priority>
      <example>

class Singleton {
    private static Singleton instance = null;
    public static Singleton getInstance() {
        synchronized(Singleton.class) {
            return new Singleton(); // this should be assigned to the field
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#staticejbfieldshouldbefinal"
         language="java"
         message="EJB's shouldn't have non-final static fields"
         name="StaticEJBFieldShouldBeFinal"
         since="4.1">
      <description>
According to the J2EE specification, an EJB should not have any static fields
with write access. However, static read-only fields are allowed. This ensures proper
behavior especially when instances are distributed by the container on several JREs.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[ImplementsList/ClassType[&#xA;        pmd-java:typeIs('jakarta.ejb.SessionBean')&#xA;     or pmd-java:typeIs('jakarta.ejb.EJBHome')&#xA;     or pmd-java:typeIs('jakarta.ejb.EJBLocalObject')&#xA;     or pmd-java:typeIs('jakarta.ejb.EJBLocalHome')&#xA;     or pmd-java:typeIs('jakarta.ejb.EJBObject')&#xA;    ]]&#xA;    /ClassBody/FieldDeclaration&#xA;        [pmd-java:modifiers() = 'static']&#xA;        [not(pmd-java:modifiers() = 'final')]"/>
      </properties>
      <example>

public class SomeEJB extends EJBObject implements EJBLocalHome {

    private static int CountA;          // poor, field can be edited

    private static final int CountB;    // preferred, read-only access
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#stringbufferinstantiationwithchar"
         language="java"
         message="Argument to `new StringBuilder()` or `new StringBuffer()` is implicitly converted from char to int"
         name="StringBufferInstantiationWithChar"
         since="3.9">
      <description>
Individual character values provided as initialization arguments will be converted into integers.
This can lead to internal buffer sizes that are larger than expected. Some examples:

```
new StringBuffer()      //  16
new StringBuffer(6)     //  6
new StringBuffer("hello world")  // 11 + 16 = 27
new StringBuffer('A')   //  chr(A) = 65
new StringBuffer("A")   //  1 + 16 = 17

new StringBuilder()     //  16
new StringBuilder(6)    //  6
new StringBuilder("hello world")  // 11 + 16 = 27
new StringBuilder('C')   //  chr(C) = 67
new StringBuilder("A")   //  1 + 16 = 17
```
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ConstructorCall[ArgumentList/*[pmd-java:typeIsExactly('char')]]&#xA;    [pmd-java:matchesSig('java.lang.StringBuilder#new(int)')&#xA;     or pmd-java:matchesSig('java.lang.StringBuffer#new(int)')]"/>
      </properties>
      <example>

// misleading instantiation, these buffers
// are actually sized to 99 characters long
StringBuffer  sb1 = new StringBuffer('c');
StringBuilder sb2 = new StringBuilder('c');

// in these forms, just single characters are allocated
StringBuffer  sb3 = new StringBuffer("c");
StringBuilder sb4 = new StringBuilder("c");

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#suspiciousequalsmethodname"
         language="java"
         message="The method name and parameter number are suspiciously close to equals(Object)"
         name="SuspiciousEqualsMethodName"
         since="2.0">
      <description>
The method name and parameter number are suspiciously close to `Object.equals`, which can denote an
intention to override it. However, the method does not override `Object.equals`, but overloads it instead.
Overloading `Object.equals` method is confusing for other programmers, error-prone and hard to maintain,
especially when using inheritance, because `@Override` annotations used in subclasses can provide a false
sense of security. For more information on `Object.equals` method, see Effective Java, 3rd Edition,
Item 10: Obey the general contract when overriding equals.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[@Name = 'equals'][&#xA;    (@Arity = 1&#xA;     and not(FormalParameters/FormalParameter[pmd-java:typeIsExactly('java.lang.Object')])&#xA;     or not(PrimitiveType[@Kind = 'boolean'])&#xA;    ) or (&#xA;     @Arity = 2&#xA;     and PrimitiveType[@Kind = 'boolean']&#xA;     and FormalParameters/FormalParameter[pmd-java:typeIsExactly('java.lang.Object')]&#xA;     and not(pmd-java:hasAnnotation('java.lang.Override'))&#xA;    )&#xA;]&#xA;| //MethodDeclaration[@Name = 'equal'][&#xA;    @Arity = 1&#xA;    and FormalParameters/FormalParameter[pmd-java:typeIsExactly('java.lang.Object')]&#xA;]"/>
      </properties>
      <example>

public class Foo {
   public int equals(Object o) {
     // oops, this probably was supposed to be boolean equals
   }
   public boolean equals(String s) {
     // oops, this probably was supposed to be equals(Object)
   }
   public boolean equals(Object o1, Object o2) {
     // oops, this probably was supposed to be equals(Object)
   }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#suspicioushashcodemethodname"
         language="java"
         message="The method name and return type are suspiciously close to hashCode()"
         name="SuspiciousHashcodeMethodName"
         since="1.5">
      <description>
The method name and return type are suspiciously close to hashCode(), which may denote an intention
to override the hashCode() method.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[&#xA;        lower-case(@Name) = 'hashcode'&#xA;    and @Name != 'hashCode'&#xA;    and @Arity = 0&#xA;    and PrimitiveType[@Kind = 'int']&#xA;]"/>
      </properties>
      <example>

public class Foo {
    public int hashcode() { // oops, this probably was supposed to be 'hashCode'
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.SuspiciousOctalEscapeRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#suspiciousoctalescape"
         language="java"
         message="Suspicious decimal characters following octal escape in string literal: {0}"
         name="SuspiciousOctalEscape"
         since="1.5">
      <description>
A suspicious octal escape sequence was found inside a String literal.
The Java language specification (section 3.10.6) says an octal
escape sequence inside a literal String shall consist of a backslash
followed by:

    OctalDigit | OctalDigit OctalDigit | ZeroToThree OctalDigit OctalDigit

Any octal escape sequence followed by non-octal digits can be confusing,
e.g. "\038" is interpreted as the octal escape sequence "\03" followed by
the literal character "8".
        </description>
      <priority>3</priority>
      <example>

public void foo() {
  // interpreted as octal 12, followed by character '8'
  System.out.println("suspicious: \128");
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.TestClassWithoutTestCasesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#testclasswithouttestcases"
         language="java"
         message="The class ''{0}'' might be a test class, but it contains no test cases."
         name="TestClassWithoutTestCases"
         since="3.0">
      <description>
Test classes typically end with the suffix "Test", "Tests" or "TestCase". Having a non-test class with that name
is not a good practice, since most people will assume it is a test case. Test classes have test methods
named "testXXX" (JUnit3) or use annotations (e.g. `@Test`).

The suffix can be configured using the property `testClassPattern`. To disable the detection of possible test classes
by name, set this property to an empty string.
        </description>
      <priority>3</priority>
      <example>

//Consider changing the name of the class if it is not a test
//Consider adding test methods if it is a test
public class CarTest {
   public static void main(String[] args) {
    // do something
   }
   // code
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#unconditionalifstatement"
         language="java"
         message="Do not use 'if' statements that are always true or always false"
         name="UnconditionalIfStatement"
         since="1.5">
      <description>
Do not use "if" statements whose conditionals are always true or always false.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath" value="//IfStatement[BooleanLiteral[1]]"/>
      </properties>
      <example>

public class Foo {
    public void close() {
        if (true) {        // fixed conditional, not recommended
            // ...
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#unnecessarybooleanassertion"
         language="java"
         message="assertTrue(true) or similar statements are unnecessary"
         name="UnnecessaryBooleanAssertion"
         since="3.0">
      <description>
A JUnit test assertion with a boolean literal is unnecessary since it always will evaluate to the same thing.
Consider using flow control (in case of `assertTrue(false)` or similar) or simply removing
statements like `assertTrue(true)` and `assertFalse(false)`. If you just want a test to halt after finding
an error, use the `fail()` method and provide an indication message of why it did.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;    [pmd-java:typeIs('junit.framework.TestCase')&#xA;     or .//Annotation[pmd-java:typeIs('org.junit.Test')&#xA;                   or pmd-java:typeIs('org.junit.jupiter.api.Test')&#xA;                   or pmd-java:typeIs('org.junit.jupiter.api.RepeatedTest')&#xA;                   or pmd-java:typeIs('org.junit.jupiter.api.TestFactory')&#xA;                   or pmd-java:typeIs('org.junit.jupiter.api.TestTemplate')&#xA;                   or pmd-java:typeIs('org.junit.jupiter.params.ParameterizedTest')&#xA;     ]&#xA;    ]&#xA;    //MethodCall[@MethodName = ('assertTrue', 'assertFalse')]&#xA;        [ArgumentList&#xA;            [&#xA;                BooleanLiteral or&#xA;                UnaryExpression[@Operator = '!'][BooleanLiteral]&#xA;            ]&#xA;        ]"/>
      </properties>
      <example>

public class SimpleTest extends TestCase {
    public void testX() {
        assertTrue(true);            // serves no real purpose - remove it
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.UnnecessaryCaseChangeRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#unnecessarycasechange"
         language="java"
         message="Using equalsIgnoreCase() is cleaner than using toUpperCase/toLowerCase().equals()."
         name="UnnecessaryCaseChange"
         since="3.3">
      <description>
Using equalsIgnoreCase() is faster than using toUpperCase/toLowerCase().equals()
        </description>
      <priority>3</priority>
      <example>

boolean answer1 = buz.toUpperCase().equals("BAZ");              // should be buz.equalsIgnoreCase("BAZ")

boolean answer2 = buz.toUpperCase().equalsIgnoreCase("BAZ");    // another unnecessary toUpperCase()

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#unnecessaryconversiontemporary"
         language="java"
         message="Avoid unnecessary temporaries when converting primitives to Strings"
         name="UnnecessaryConversionTemporary"
         since="0.1">
      <description>
Avoid the use temporary objects when converting primitives to Strings. Use the static conversion methods
on the wrapper classes instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[@MethodName = 'toString']&#xA;    [ConstructorCall[position() = 1]&#xA;        [&#xA;            pmd-java:typeIs('java.lang.Integer')&#xA;         or pmd-java:typeIs('java.lang.Long')&#xA;         or pmd-java:typeIs('java.lang.Float')&#xA;         or pmd-java:typeIs('java.lang.Byte')&#xA;         or pmd-java:typeIs('java.lang.Double')&#xA;         or pmd-java:typeIs('java.lang.Short')&#xA;        ]&#xA;    ]"/>
      </properties>
      <example>

public String convert(int x) {
    String foo = new Integer(x).toString(); // this wastes an object

    return Integer.toString(x);             // preferred approach
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#unusednullcheckinequals"
         language="java"
         message="Invoke equals() on the object you''ve already ensured is not null"
         name="UnusedNullCheckInEquals"
         since="3.5">
      <description>
After checking an object reference for null, you should invoke equals() on that object rather than passing
it to another object's equals() method.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//InfixExpression[@Operator = '&amp;&amp;']&#xA;  /MethodCall[pmd-java:matchesSig(&#34;java.lang.Object#equals(java.lang.Object)&#34;)]&#xA;             [not(StringLiteral)]&#xA;             [not(VariableAccess[@CompileTimeConstant = true()])]&#xA;             [ArgumentList/VariableAccess/@Name = ..//InfixExpression[@Operator = '!='][NullLiteral]/VariableAccess/@Name]"/>
      </properties>
      <example>

public class Test {

    public String method1() { return "ok";}
    public String method2() { return null;}

    public void method(String a) {
        String b;
        // I don't know it method1() can be "null"
        // but I know "a" is not null..
        // I'd better write a.equals(method1())

        if (a!=null &amp;&amp; method1().equals(a)) { // will trigger the rule
            //whatever
        }

        if (method1().equals(a) &amp;&amp; a != null) { // won't trigger the rule
            //whatever
        }

        if (a!=null &amp;&amp; method1().equals(b)) { // won't trigger the rule
            //whatever
        }

        if (a!=null &amp;&amp; "LITERAL".equals(a)) { // won't trigger the rule
            //whatever
        }

        if (a!=null &amp;&amp; !a.equals("go")) { // won't trigger the rule
            a=method2();
            if (method1().equals(a)) {
                //whatever
            }
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#usecorrectexceptionlogging"
         language="java"
         message="Use the correct logging statement for logging exceptions"
         name="UseCorrectExceptionLogging"
         since="3.2">
      <description>
To make sure the full stacktrace is printed out, use the logging statement with two arguments: a String and a Throwable.

This rule only applies to [Apache Commons Logging](https://commons.apache.org/proper/commons-logging/).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//CatchClause/Block//MethodCall&#xA;    [pmd-java:matchesSig('org.apache.commons.logging.Log#_(java.lang.Object)')]&#xA;    [ArgumentList[not(MethodCall)]//VariableAccess/@Name = ancestor::CatchClause/CatchParameter/@Name]"/>
      </properties>
      <example>

public class Main {
    private static final Log _LOG = LogFactory.getLog( Main.class );
    void bar() {
        try {
        } catch( Exception e ) {
            _LOG.error( e ); //Wrong!
        } catch( OtherException oe ) {
            _LOG.error( oe.getMessage(), oe ); //Correct
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#useequalstocomparestrings"
         language="java"
         message="Use equals() to compare strings instead of ''=='' or ''!=''"
         name="UseEqualsToCompareStrings"
         since="4.1">
      <description>
Using '==' or '!=' to compare strings is only reliable if the interned string (`String#intern()`)
is used on both sides.

Use the `equals()` method instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//InfixExpression[@Operator = ('==', '!=')]&#xA;                 [count(*[pmd-java:typeIsExactly('java.lang.String')]) = 2]"/>
      </properties>
      <example>

public boolean test(String s) {
    if (s == "one") return true;        // unreliable
    if ("two".equals(s)) return true;   // better
    return false;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.UselessOperationOnImmutableRule"
         deprecated="true"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#uselessoperationonimmutable"
         language="java"
         message="The result of an operation on an immutable object is ignored"
         name="UselessOperationOnImmutable"
         since="3.5">
      <description>
An operation on an immutable object will not change the object itself since the result of the operation is a new object.
Therefore, ignoring the result of such an operation is likely a mistake. The operation can probably be removed.

This rule recognizes the types `String`, `BigDecimal`, `BigInteger` or any type from `java.time.*` as immutable.

**Deprecated:** This rule is deprecated since PMD 7.17.0 and will be removed with PMD 8.0.0.
This rule has been replaced by {% rule UselessPureMethodCall %}.
        </description>
      <priority>3</priority>
      <example>

import java.math.*;

class Test {
    void method1() {
        BigDecimal bd=new BigDecimal(10);
        bd.add(new BigDecimal(5));      // this will trigger the rule
    }
    void method2() {
        BigDecimal bd=new BigDecimal(10);
        bd = bd.add(new BigDecimal(5)); // this won't trigger the rule
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.errorprone.UselessPureMethodCallRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#uselesspuremethodcall"
         language="java"
         message="Do not call pure method {0} if the result is not used."
         name="UselessPureMethodCall"
         since="7.17.0">
      <description>
This rule detects method calls of pure methods whose result is unused. A pure method is a method without
side effects. Therefore, ignoring the result of such a method call is likely a mistake.

Either the method call can be removed or the result should be used.
        </description>
      <priority>3</priority>
      <example>
            
public class Something {
    public void foo() {
        List.of("foo").size(); // result unused
        Stream.of("bar").map(item -&gt; System.out.format("%s", item)); // result unused
        Stream.of("bar").forEach(item -&gt; System.out.format("%s", item)); // better
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#uselocalewithcaseconversions"
         language="java"
         message="When doing a String.toLowerCase()/toUpperCase() call, use a Locale"
         name="UseLocaleWithCaseConversions"
         since="2.0">
      <description>
When doing `String::toLowerCase()/toUpperCase()` conversions, use an explicit locale argument to specify the case
transformation rules.

Using `String::toLowerCase()` without arguments implicitly uses `Locale::getDefault()`.
The problem is that the default locale depends on the current JVM setup (and usually on the system in which
it is running). Using the system default may be exactly what you want (e.g. if you are manipulating strings
you got through standard input), but it may as well not be the case (e.g. if you are getting the string over
the network or a file, and the encoding is well-defined and independent of the environment). In the latter case,
using the default locale makes the case transformation brittle, as it may yield unexpected results on a machine
whose locale has other case translation rules. For example, in Turkish, the uppercase form of `i` is `İ` (U+0130,
not ASCII) and not `I` (U+0049) as in English.

The rule is intended to *force* developers to think about locales when dealing with strings. By taking a
conscious decision about the choice of locale at the time of writing, you reduce the risk of surprising
behaviour down the line, and communicate your intent to future readers.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[pmd-java:matchesSig(&#34;java.lang.String#toLowerCase()&#34;) or pmd-java:matchesSig(&#34;java.lang.String#toUpperCase()&#34;)]&#xA;            [not(MethodCall[@MethodName = &#34;toHexString&#34;])]"/>
      </properties>
      <example>

// violation - implicitly system-dependent conversion
if (x.toLowerCase().equals("list")) {}

// The above will not match "LIST" on a system with a Turkish locale.
// It could be replaced with
if (x.toLowerCase(Locale.US).equals("list")) { }
// or simply
if (x.equalsIgnoreCase("list")) { }

// ok - system independent conversion
String z = a.toLowerCase(Locale.ROOT);

// ok - explicit system-dependent conversion
String z2 = a.toLowerCase(Locale.getDefault());

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_errorprone.html#useproperclassloader"
         language="java"
         message="In J2EE, getClassLoader() might not work as expected.  Use Thread.currentThread().getContextClassLoader() instead."
         name="UseProperClassLoader"
         since="3.7">
      <description>
In J2EE, the getClassLoader() method might not work as expected. Use
Thread.currentThread().getContextClassLoader() instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[pmd-java:matchesSig(&#34;java.lang.Class#getClassLoader()&#34;)]"/>
      </properties>
      <example>

public class Foo {
    ClassLoader cl = Bar.class.getClassLoader();
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.AtLeastOneConstructorRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#atleastoneconstructor"
         language="java"
         message="Each class should declare at least one constructor"
         name="AtLeastOneConstructor"
         since="1.04">
      <description>

Each non-static class should declare at least one constructor.
Classes with solely static members are ignored, refer to [UseUtilityClassRule](pmd_rules_java_design.html#useutilityclass) to detect those.

        </description>
      <priority>3</priority>
      <example>

public class Foo {
   // missing constructor
  public void doSomething() { ... }
  public void doOtherThing { ... }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#avoiddollarsigns"
         language="java"
         message="Avoid using dollar signs in variable/method/class/interface names"
         name="AvoidDollarSigns"
         since="1.5">
      <description>
Avoid using dollar signs in variable/method/class/interface names.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration           [contains(@SimpleName, '$')]&#xA;    | //EnumDeclaration            [contains(@SimpleName, '$')]&#xA;    | //AnnotationTypeDeclaration  [contains(@SimpleName, '$')]&#xA;    | //RecordDeclaration          [contains(@SimpleName, '$')]&#xA;    | //VariableId                 [contains(@Name, '$')]&#xA;    | //MethodDeclaration          [contains(@Name, '$')]"/>
      </properties>
      <example>

public class Fo$o {  // not a recommended name
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#avoidprotectedfieldinfinalclass"
         language="java"
         message="Avoid protected fields in a final class.  Change to private or package access."
         name="AvoidProtectedFieldInFinalClass"
         since="2.1">
      <description>
Do not use protected fields in final classes since they cannot be subclassed.
Clarify your intent by using private or package access modifiers instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[@Final = true()]&#xA;/ClassBody&#xA;/FieldDeclaration[@Visibility = &#34;protected&#34;]"/>
      </properties>
      <example>

public final class Bar {
  private int x;
  protected int y;  // bar cannot be subclassed, so is y really private or package visible?
  Bar() {}
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#avoidprotectedmethodinfinalclassnotextending"
         language="java"
         message="Avoid protected methods in a final class that doesn't extend anything other than Object.  Change to private or package access."
         name="AvoidProtectedMethodInFinalClassNotExtending"
         since="5.1">
      <description>
Do not use protected methods in most final classes since they cannot be subclassed. This should
only be allowed in final classes that extend other classes with protected methods (whose
visibility cannot be reduced). Clarify your intent by using private or package access modifiers instead.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[@Final= true() and not(ExtendsList)]&#xA;/ClassBody&#xA;/MethodDeclaration[@Visibility=&#34;protected&#34; and @Name != 'finalize']"/>
      </properties>
      <example>

public final class Foo {
  private int bar() {}
  protected int baz() {} // Foo cannot be subclassed, and doesn't extend anything, so is baz() really private or package visible?
}
 
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#avoidusingnativecode"
         language="java"
         message="The use of native code is not recommended."
         name="AvoidUsingNativeCode"
         since="4.1">
      <description>
Unnecessary reliance on Java Native Interface (JNI) calls directly reduces application portability
and increases the maintenance burden.
        </description>
      <priority>2</priority>
      <properties>
         <property name="xpath"
                   value="//MethodCall[TypeExpression/ClassType[pmd-java:typeIs('java.lang.System')]&#xA;                                    and @MethodName = 'loadLibrary']"/>
      </properties>
      <example>

public class SomeJNIClass {

     public SomeJNIClass() {
         System.loadLibrary("nativelib");
     }

     static {
         System.loadLibrary("nativelib");
     }

     public void invalidCallsInMethod() throws SecurityException, NoSuchMethodException {
         System.loadLibrary("nativelib");
     }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#booleangetmethodname"
         language="java"
         message="A 'getX()' method which returns a boolean or Boolean should be named 'isX()'"
         name="BooleanGetMethodName"
         since="4.0">
      <description>
            Methods that return boolean or Boolean results should be named as predicate statements to denote this.
            I.e., 'isReady()', 'hasValues()', 'canCommit()', 'willFail()', etc. Avoid the use of the 'get' prefix for these methods.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration&#xA;    [starts-with(@Name, 'get')]&#xA;    [@Arity = 0 or $checkParameterizedMethods = true()]&#xA;    [ (PrimitiveType[@Kind = 'boolean'] or ClassType[pmd-java:typeIs('java.lang.Boolean')]) and @Overridden = false() ]"/>
         <property description="Check parameterized methods"
                   name="checkParameterizedMethods"
                   type="Boolean">
            <value>false</value>
         </property>
      </properties>
      <example>
            
public boolean getFoo();            // bad
public Boolean getFoo();            // bad
public boolean isFoo();             // ok
public Boolean isFoo();             // ok
public boolean getFoo(boolean bar); // ok, unless checkParameterizedMethods=true

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#callsuperinconstructor"
         language="java"
         message="It is a good practice to call super() in a constructor"
         name="CallSuperInConstructor"
         since="3.0">
      <description>
It is a good practice to call super() in a constructor. If super() is not called but
another constructor (such as an overloaded constructor) is called, this rule will not report it.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[ExtendsList/*]&#xA;  /ClassBody&#xA;  /ConstructorDeclaration[ not(Block/ExplicitConstructorInvocation) ]"/>
      </properties>
      <example>

public class Foo extends Bar{
  public Foo() {
   // call the constructor of Bar
   super();
  }
 public Foo(int code) {
  // do something with code
   this();
   // no problem with this
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.ClassNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#classnamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="ClassNamingConventions"
         since="1.2">
      <description>
            Configurable naming conventions for type declarations. This rule reports
            type declarations which do not match the regex that applies to their
            specific kind (e.g. enum or interface). Each regex can be configured through
            properties.

            By default, this rule uses the standard Java naming convention (Pascal case).
            
            The rule can detect utility classes and enforce a different naming convention
            on those. E.g. setting the property `utilityClassPattern` to
            `[A-Z][a-zA-Z0-9]+(Utils?|Helper|Constants)` reports any utility class, whose name
            does not end in "Util(s)", "Helper" or "Constants".
            
            For this rule, a utility class is defined as: a concrete class that does not
            inherit from a super class or implement any interface and only has static fields
            or methods.

            This rule detects test classes using the following convention: Test classes are top-level classes, that
            either inherit from JUnit 3 TestCase or have at least one method annotated with the Test annotations from
            JUnit4/5 or TestNG.
        </description>
      <priority>1</priority>
      <example>

// This is Pascal case, the recommended naming convention in Java
// Note that the default values of this rule don't allow underscores
// or accented characters in type names
public class FooBar {}

// You may want abstract classes to be named 'AbstractXXX',
// in which case you can customize the regex for abstract
// classes to 'Abstract[A-Z]\w+'
public abstract class Thing {}

// This class doesn't respect the convention, and will be flagged
public class Éléphant {}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.CommentDefaultAccessModifierRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#commentdefaultaccessmodifier"
         language="java"
         message="Missing commented default access modifier on {0} ''{1}''"
         name="CommentDefaultAccessModifier"
         since="5.4.0">
      <description>
To avoid mistakes if we want that an Annotation, Class, Enum, Method, Constructor or Field have a default access modifier
we must add a comment at the beginning of its declaration.
By default, the comment must be `/* default */` or `/* package */`, if you want another, you have to provide a regular expression.

This rule ignores by default all cases that have a `@VisibleForTesting` annotation or any JUnit5/TestNG annotation. Use the
property "ignoredAnnotations" to customize the recognized annotations.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    final String stringValue = "some string";
    String getString() {
       return stringValue;
    }

    class NestedFoo {
    }
}

// should be
public class Foo {
    /* default */ final String stringValue = "some string";
    /* default */ String getString() {
       return stringValue;
    }

    /* default */ class NestedFoo {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.ConfusingTernaryRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#confusingternary"
         language="java"
         message="Avoid if (x != y) ..; else ..;"
         name="ConfusingTernary"
         since="1.9">
      <description>
Avoid negation within an "if" expression with an "else" clause.  For example, rephrase:
`if (x != y) diff(); else same();` as: `if (x == y) same(); else diff();`.

Most "if (x != y)" cases without an "else" are often return cases, so consistent use of this
rule makes the code easier to read.  Also, this resolves trivial ordering problems, such
as "does the error case go first?" or "does the common case go first?".
        </description>
      <priority>3</priority>
      <example>

boolean bar(int x, int y) {
    return (x != y) ? diff : same;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#controlstatementbraces"
         language="java"
         message="This statement should have braces"
         name="ControlStatementBraces"
         since="6.2.0">
      <description>
            Enforce a policy for braces on control statements. It is recommended to use braces on 'if ... else'
            statements and loop statements, even if they are optional. This usually makes the code clearer, and
            helps prepare the future when you need to add another statement. That said, this rule lets you control
            which statements are required to have braces via properties.

            From 6.2.0 on, this rule supersedes WhileLoopMustUseBraces, ForLoopMustUseBraces, IfStmtMustUseBraces,
            and IfElseStmtMustUseBraces.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//WhileStatement[$checkWhileStmt and not(Block) and not($allowEmptyLoop and EmptyStatement)]&#xA;                |&#xA;                //ForStatement[$checkForStmt and not(Block) and not($allowEmptyLoop and EmptyStatement)]&#xA;                |&#xA;                //ForeachStatement[$checkForStmt and not(Block) and not($allowEmptyLoop and EmptyStatement)]&#xA;                |&#xA;                //DoStatement[$checkDoWhileStmt and not(Block) and not($allowEmptyLoop and EmptyStatement)]&#xA;                |&#xA;                (: The violation is reported on the sub statement -- not the if statement :)&#xA;                //IfStatement[$checkIfElseStmt]&#xA;                    /*[position() &gt; 1 and not(self::Block or self::IfStatement)]&#xA;                      [ $checkSingleIfStmt&#xA;                            (: Inside this (...) is the definition of a &#34;single if statement&#34; :)&#xA;                            or not(parent::*/@Else = false() (: No else stmt :)&#xA;                                   (: Not the last branch of an 'if ... else if' chain :)&#xA;                                   and not(parent::IfStatement[parent::IfStatement]))]&#xA;&#xA;                |&#xA;                (: Reports case labels if one of their subordinate statements is not braced :)&#xA;                //SwitchFallthroughBranch[$checkCaseStmt]&#xA;                             [count(*) &gt; 1 and (count(*) &gt; 2 or not(child::*[2]/self::Block))]"/>
         <property description="Require that 'if ... else' statements use braces"
                   name="checkIfElseStmt"
                   type="Boolean">
            <value>true</value>
         </property>
         <property description="Require that 'if' statements with a single branch use braces"
                   name="checkSingleIfStmt"
                   type="Boolean">
            <value>true</value>
         </property>
         <property description="Require that 'while' loops use braces"
                   name="checkWhileStmt"
                   type="Boolean">
            <value>true</value>
         </property>
         <property description="Require that 'for' loops should use braces"
                   name="checkForStmt"
                   type="Boolean">
            <value>true</value>
         </property>
         <property description="Require that 'do ... while' loops use braces"
                   name="checkDoWhileStmt"
                   type="Boolean">
            <value>true</value>
         </property>
         <property description="Require that cases of a switch have braces"
                   name="checkCaseStmt"
                   type="Boolean">
            <value>false</value>
         </property>
         <property description="Allow loops with an empty statement, e.g. 'while(true);'"
                   name="allowEmptyLoop"
                   type="Boolean">
            <value>false</value>
         </property>
      </properties>
      <example>
            
while (true)    // not recommended
  x++;

while (true) {  // preferred approach
  x++;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#emptymethodinabstractclassshouldbeabstract"
         language="java"
         message="An empty method in an abstract class should be abstract instead"
         name="EmptyMethodInAbstractClassShouldBeAbstract"
         since="4.1">
      <description>
Empty or auto-generated methods in an abstract class should be tagged as abstract. This helps to remove their inappropriate
usage by developers who should be implementing their own versions in the concrete subclasses.
        </description>
      <priority>1</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[@RegularClass = true() and pmd-java:modifiers() = &#34;abstract&#34;]&#xA;    /ClassBody&#xA;    /MethodDeclaration&#xA;    [Block[&#xA;      let $size := count(*[not(self::EmptyStatement)])&#xA;      return $size = 0&#xA;             or $size = 1 and ReturnStatement[ NullLiteral or NumericLiteral[@ValueAsInt = 0] or StringLiteral[@Empty = true()]]&#xA;    ]]"/>
      </properties>
      <example>

public abstract class ShouldBeAbstract {
    public Object couldBeAbstract() {
        // Should be abstract method ?
        return null;
    }

    public void couldBeAbstract() {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.EmptyControlStatementRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#emptycontrolstatement"
         language="java"
         message="This control statement has an empty branch"
         name="EmptyControlStatement"
         since="6.46.0">
      <description>
            Reports control statements whose body is empty, as well as empty initializers.

            The checked code constructs are the following:
            - bodies of `try` statements
            - `finally` clauses of `try` statements
            - `switch` statements
            - `synchronized` statements
            - `if` statements
            - loop statements: `while`, `for`, `do .. while`
            - initializers
            - blocks used as statements (for scoping)

            This rule replaces the rules EmptyFinallyBlock, 
            EmptyIfStmt, EmptyInitializer, EmptyStatementBlock, 
            EmptySwitchStatements, EmptySynchronizedBlock, EmptyTryBlock, and EmptyWhileStmt.

            Notice that {% rule java/errorprone/EmptyCatchBlock %} is still an independent rule.

            EmptyStatementNotInLoop is replaced by {% rule java/codestyle/UnnecessarySemicolon %}.
        </description>
      <priority>3</priority>
      <example>

class Foo {
    {
        if (true); // empty if statement
        if (true) { // empty as well
        }
    }

    {} // empty initializer
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#extendsobject"
         language="java"
         message="No need to explicitly extend Object."
         name="ExtendsObject"
         since="5.0">
      <description>No need to explicitly extend Object.</description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ExtendsList/ClassType[pmd-java:typeIsExactly('java.lang.Object')]"/>
      </properties>
      <example>

public class Foo extends Object {     // not required
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.FieldDeclarationsShouldBeAtStartOfClassRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#fielddeclarationsshouldbeatstartofclass"
         language="java"
         message="Fields should be declared at the top of the class, before any method declarations, constructors, initializers or inner classes."
         name="FieldDeclarationsShouldBeAtStartOfClass"
         since="5.0">
      <description>
Fields should be declared at the top of the class, before any method declarations, constructors, initializers or inner classes.
        </description>
      <priority>3</priority>
      <example>

public class HelloWorldBean {

  // Field declared before methods / inner classes - OK
  private String _thing;

  public String getMessage() {
    return "Hello World!";
  }

  // Field declared after methods / inner classes - avoid this
  private String _fieldInWrongLocation;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.FieldNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#fieldnamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="FieldNamingConventions"
         since="6.7.0">
      <description>
            Configurable naming conventions for field declarations. This rule reports variable declarations
            which do not match the regex that applies to their specific kind ---e.g. constants (static final),
            enum constant, final field. Each regex can be configured through properties.

            By default this rule uses the standard Java naming convention (Camel case), and uses the ALL_UPPER
            convention for constants and enum constants.
        </description>
      <priority>1</priority>
      <example>
            
            class Foo {
                int myField = 1; // This is in camel case, so it's ok
                int my_Field = 1; // This contains an underscore, it's not ok by default
                                  // but you may allow it, or even require the "my_" prefix

                final int FinalField = 1; // you may configure a different convention for final fields,
                                          // e.g. here PascalCase: [A-Z][a-zA-Z0-9]*

                interface Interface {
                    double PI = 3.14; // interface "fields" use the constantPattern property
                }

                enum AnEnum {
                    ORG, NET, COM; // These use a separate property but are set to ALL_UPPER by default
                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#finalparameterinabstractmethod"
         language="java"
         message="Final parameter in abstract method"
         name="FinalParameterInAbstractMethod"
         since="6.42.0">
      <description>
            Declaring a method parameter as final for an interface method is useless because the implementation may choose to not respect it.
        </description>
      <priority>1</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration&#xA;    [FormalParameters/FormalParameter[@Final = true()]]&#xA;    [not(Block)]"/>
      </properties>
      <example>
            
public interface MyInterface {
  void process(final Object arg); // Avoid using final here
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#forloopshouldbewhileloop"
         language="java"
         message="This for loop could be simplified to a while loop"
         name="ForLoopShouldBeWhileLoop"
         since="1.02">
      <description>
Some for loops can be simplified to while loops, this makes them more concise.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ForStatement[not(ForInit | ForUpdate) and count(*) = 2]"/>
      </properties>
      <example>

public class Foo {
    void bar() {
        for (;true;) true; // No Init or Update part, may as well be: while (true)
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.FormalParameterNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#formalparameternamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="FormalParameterNamingConventions"
         since="6.6.0">
      <description>
            Configurable naming conventions for formal parameters of methods and lambdas.
            This rule reports formal parameters which do not match the regex that applies to their
            specific kind (e.g. lambda parameter, or final formal parameter). Each regex can be
            configured through properties.

            By default this rule uses the standard Java naming convention (Camel case).
        </description>
      <priority>1</priority>
      <example>
            
            class Foo {

                abstract void bar(int myInt); // This is Camel case, so it's ok

                void bar(int my_i) { // this will be reported

                }

                void lambdas() {

                    // lambdas parameters can be configured separately
                    Consumer&lt;String&gt; lambda1 = s_str -&gt; { };

                    // lambda parameters with an explicit type can be configured separately
                    Consumer&lt;String&gt; lambda1 = (String str) -&gt; { };

                }

            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="true"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#genericsnaming"
         language="java"
         message="Generics names should be a one letter long and upper case."
         name="GenericsNaming"
         since="4.2.6">
      <description>
Names for references to generic values should be limited to a single uppercase letter.

**Deprecated:** This rule is deprecated since PMD 7.17.0 and will be removed with PMD 8.0.0. This rule has been replaced by {% rule TypeParameterNamingConventions %}, which provides more comprehensive and configurable naming conventions for type parameters.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//TypeParameter[&#xA;  string-length(@Name) &gt; 1&#xA;  or&#xA;  upper-case(@Name) != @Name&#xA;]"/>
      </properties>
      <example>

public interface GenericDao&lt;E extends BaseModel, K extends Serializable&gt; extends BaseDao {
    // This is ok...
}

public interface GenericDao&lt;E extends BaseModel, K extends Serializable&gt; {
    // Also this
}

public interface GenericDao&lt;e extends BaseModel, K extends Serializable&gt; {
    // 'e' should be an 'E'
}

public interface GenericDao&lt;EF extends BaseModel, K extends Serializable&gt; {
   // 'EF' is not ok.
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.IdenticalCatchBranchesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#identicalcatchbranches"
         language="java"
         message="''catch'' branch identical to ''{0}'' branch"
         minimumLanguageVersion="1.7"
         name="IdenticalCatchBranches"
         since="6.4.0">
      <description>
            Identical `catch` branches use up vertical space and increase the complexity of code without
            adding functionality. It's better style to collapse identical branches into a single multi-catch
            branch.
        </description>
      <priority>3</priority>
      <example>
            
try {
    // do something
} catch (IllegalArgumentException e) {
    throw e;
} catch (IllegalStateException e) { // Can be collapsed into the previous block
    throw e;
}

try {
    // do something
} catch (IllegalArgumentException | IllegalStateException e) { // This is better
    throw e;
}
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.LambdaCanBeMethodReferenceRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#lambdacanbemethodreference"
         language="java"
         message="Lambda expression could be written as a method reference: `{0}`"
         name="LambdaCanBeMethodReference"
         since="7.1.0">
      <description>
            This rule reports lambda expressions that can be written more succinctly as a method reference. This is the case if the lambda is an expression lambda that only calls one method, passing the entire lambda parameter list in order to the method. For instance:
            ```java
                x -&gt; Foo.call(x) // can be Foo::call
                x -&gt; call(x)     // can be this::call, if call is an instance method
                (x, y, z) -&gt; call(x, y, z) // can be this::call
                () -&gt; foo.get() // can be foo::get
                x -&gt; x.foo()    // can be XType::foo (where XType is the type of x)
            ```

            In some cases rewriting a lambda to a method reference can change the semantics of the code. For instance in `(x) -&gt; someVar.call(x)`, the invocation of the lambda may produce a NullPointerException (NPE) if `someVar` is null. The method reference `someVar::call` will also NPE if `someVar` is null, but it will do so at the point the method reference is created, while the lambda is created without error and its NPE is only thrown if the lambda is invoked (which may be never). Code should probably not rely on this subtle semantic difference, therefore these potentially problematic lambdas are also reported by default. This behavior can be disabled by setting the property `ignoreIfMayNPE` to `true`.

            The property `ignoreIfMayNPE` is true by default. By default, calls whose receiver is itself a method call are ignored, because they could cause side effects. This may be changed by setting the property `ignoreIfReceiverIsMethod` to `false`.

            Scope limitations:
            - This rule will not report lambdas of the form `x -&gt; new CtorCall().something(x)`, because the semantics of the method reference would be to create a single new object, while the lambda creates one object per invocation.
            - The rule cannot know if the qualifier of a method call performs side effects. This means `(x) -&gt; sideEffectingMethod().foo(x)` will be reported. Suppress the warning in this case.
        </description>
      <priority>3</priority>
      <example>
            
            import java.util.stream.Stream;

            public class LambdaCanBeMethodReference {
                static {
                    Stream.of("abc", "d")
                            .mapToInt(s -&gt; s.length()) // could be String::length
                            .reduce((x, y) -&gt; Integer.sum(x, y)) // could be Integer::sum
                            .getAsInt();
                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.LinguisticNamingRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#linguisticnaming"
         language="java"
         message="Linguistics Antipattern - Method name and return type is inconsistent linguistically"
         name="LinguisticNaming"
         since="6.7.0">
      <description>
            This rule finds Linguistic Naming Antipatterns. It checks for fields, that are named, as if they should
            be boolean but have a different type. It also checks for methods, that according to their name, should
            return a boolean, but don't. Further, it checks, that getters return something and setters won't.
            Finally, it checks that methods, that start with "to" - so called transform methods - actually return
            something, since according to their name, they should convert or transform one object into another.
            There is additionally an option, to check for methods that contain "To" in their name - which are
            also transform methods. However, this is disabled by default, since this detection is prone to
            false positives.

            For more information, see [Linguistic Antipatterns - What They Are and How
Developers Perceive Them](https://doi.org/10.1007/s10664-014-9350-8).
        </description>
      <priority>3</priority>
      <example>

public class LinguisticNaming {
    int isValid;    // the field name indicates a boolean, but it is an int.
    boolean isTrue; // correct type of the field

    void myMethod() {
        int hasMoneyLocal;      // the local variable name indicates a boolean, but it is an int.
        boolean hasSalaryLocal; // correct naming and type
    }

    // the name of the method indicates, it is a boolean, but the method returns an int.
    int isValid() {
        return 1;
    }
    // correct naming and return type
    boolean isSmall() {
        return true;
    }

    // the name indicates, this is a setter, but it returns something
    int setName() {
        return 1;
    }

    // the name indicates, this is a getter, but it doesn't return anything
    void getName() {
        // nothing to return?
    }

    // the name indicates, it transforms an object and should return the result
    void toDataType() {
        // nothing to return?
    }
    // the name indicates, it transforms an object and should return the result
    void grapeToWine() {
        // nothing to return?
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#localhomenamingconvention"
         language="java"
         message="The Local Home interface of a Session EJB should be suffixed by 'LocalHome'"
         name="LocalHomeNamingConvention"
         since="4.0">
      <description>
The Local Home interface of a Session EJB should be suffixed by 'LocalHome'.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;[&#xA;    pmd-java:typeIs('jakarta.ejb.EJBLocalHome')&#xA;    and not(ends-with(@SimpleName, 'LocalHome'))&#xA;]"/>
      </properties>
      <example>

public interface MyBeautifulLocalHome extends jakarta.ejb.EJBLocalHome {} // proper name

public interface MissingProperSuffix extends jakarta.ejb.EJBLocalHome {}  // non-standard name

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#localinterfacesessionnamingconvention"
         language="java"
         message="The Local Interface of a Session EJB should be suffixed by 'Local'"
         name="LocalInterfaceSessionNamingConvention"
         since="4.0">
      <description>
The Local Interface of a Session EJB should be suffixed by 'Local'.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;[&#xA;    pmd-java:typeIs('jakarta.ejb.EJBLocalObject')&#xA;    and not(ends-with(@SimpleName, 'Local'))&#xA;]"/>
      </properties>
      <example>

public interface MyLocal extends jakarta.ejb.EJBLocalObject {}                // proper name

public interface MissingProperSuffix extends jakarta.ejb.EJBLocalObject {}    // non-standard name

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.LocalVariableCouldBeFinalRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#localvariablecouldbefinal"
         language="java"
         message="Local variable ''{0}'' could be declared final"
         name="LocalVariableCouldBeFinal"
         since="2.2">
      <description>
A local variable assigned only once can be declared final.
        </description>
      <priority>3</priority>
      <example>

public class Bar {
    public void foo () {
    String txtA = "a";          // if txtA will not be assigned again it is better to do this:
    final String txtB = "b";
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.LocalVariableNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#localvariablenamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="LocalVariableNamingConventions"
         since="6.6.0">
      <description>
            Configurable naming conventions for local variable declarations and other locally-scoped
            variables. This rule reports variable declarations which do not match the regex that applies to their
            specific kind (e.g. final variable, or catch-clause parameter). Each regex can be configured through
            properties.

            By default this rule uses the standard Java naming convention (Camel case).
        </description>
      <priority>1</priority>
      <example>
            
            class Foo {
                void bar() {
                    int localVariable = 1; // This is in camel case, so it's ok
                    int local_variable = 1; // This will be reported unless you change the regex

                    final int i_var = 1; // final local variables can be configured separately

                    try {
                        foo();
                    } catch (IllegalArgumentException e_illegal) {
                        // exception block parameters can be configured separately
                    }

                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#longvariable"
         language="java"
         message="Avoid excessively long variable names like {0}"
         name="LongVariable"
         since="0.3">
      <description>
Fields, formal arguments, or local variable names that are too long can make the code difficult to follow.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath" value="//VariableId[string-length(@Name) &gt; $minimum]"/>
         <property description="The variable length reporting threshold"
                   max="100"
                   min="1"
                   name="minimum"
                   type="Integer">
            <value>17</value>
         </property>
      </properties>
      <example>

public class Something {
    int reallyLongIntName = -3;             // VIOLATION - Field
    public static void main( String argumentsList[] ) { // VIOLATION - Formal
        int otherReallyLongName = -5;       // VIOLATION - Local
        for (int interestingIntIndex = 0;   // VIOLATION - For
             interestingIntIndex &lt; 10;
             interestingIntIndex ++ ) {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#mdbandsessionbeannamingconvention"
         language="java"
         message="SessionBean or MessageBean should be suffixed by Bean"
         name="MDBAndSessionBeanNamingConvention"
         since="4.0">
      <description>
The EJB Specification states that any MessageDrivenBean or SessionBean should be suffixed by 'Bean'.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;[&#xA;    (pmd-java:typeIs('jakarta.ejb.SessionBean')&#xA;     or pmd-java:typeIs('jakarta.ejb.MessageDrivenBean'))&#xA;    and not(ends-with(@SimpleName, 'Bean'))&#xA;]"/>
      </properties>
      <example>

public class SomeBean implements SessionBean{}                  // proper name

public class MissingTheProperSuffix implements SessionBean {}   // non-standard name

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.MethodArgumentCouldBeFinalRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#methodargumentcouldbefinal"
         language="java"
         message="Parameter ''{0}'' is not assigned and could be declared final"
         name="MethodArgumentCouldBeFinal"
         since="2.2">
      <description>
            Reports method and constructor parameters that can be made final because they are never reassigned within the body of the method.

            This rule ignores unused parameters so as not to overlap with the rule {% rule java/bestpractices/UnusedFormalParameter %}.
            It will also ignore the parameters of abstract methods.
        </description>
      <priority>3</priority>
      <example>

class Foo {
    // reported, parameter can be declared final
    public String foo1(String param) {
        return param;
    }
    // not reported, parameter is declared final
    public String foo2(final String param) {
        return param.trim();
    }
    // not reported because param is unused
    public String unusedParam(String param) {
        return "abc";
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.MethodNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#methodnamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="MethodNamingConventions"
         since="1.2">
      <description>
            Configurable naming conventions for method declarations. This rule reports
            method declarations which do not match the regex that applies to their
            specific kind (e.g. JUnit test or native method). Each regex can be
            configured through properties.

            By default, this rule uses the standard Java naming convention (Camel case).
        </description>
      <priority>1</priority>
      <example>

public class Foo {
    public void fooStuff() {
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.ModifierOrderRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#modifierorder"
         language="java"
         message="Wrong modifier order (the actual message is written by the rule)"
         name="ModifierOrder"
         since="7.17.0">
      <description>
            Enforces the modifier order recommended by the JLS. Apart from sorting modifiers,
            this rule also enforces that all annotations appear before all modifier keywords.
            By setting the property `typeAnnotations`, you can also enforce that type
            annotations appear right of the modifier keywords, next to the type they apply to.
            This property can have three values:
            - `on type`: Type annotations must be placed next to the type they apply to
            - `on decl`: Type annotations must be placed with other annotations, before modifiers.
            This is not enforced if the type annotations syntactically appears within the type, e.g.
            in `public Map.@Nullable Entry&lt;K,V&gt; method()`.
            - `anywhere` (default): Either position fits. They still cannot be interspersed within keyword
            modifiers. Annotations that are not type annotations are still required to be before keyword
            modifiers.
        </description>
      <priority>1</priority>
      <example>

abstract public class Foo { // Warn: `public` should appear before `abstract`

    // This order is not recommended, annotations should appear before keyword modifiers,
    // and may appear after if they are type annotations.
    public
    @Override
    static fooStuff() {
    }

    // This order is ok if property typeAnnotations is "anywhere", and enforced if it is "on decl":
    @Nullable
    public Object fooStuff() {}

    // This order is ok if property typeAnnotations is "anywhere", and enforced if it is "on type":
    public @Nullable Object fooStuff() {}


}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#nopackage"
         language="java"
         message="All classes, interfaces, enums and annotations must belong to a named package"
         name="NoPackage"
         since="3.3">
      <description>
Detects when a class, interface, enum or annotation does not have a package definition.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="/CompilationUnit[not(PackageDeclaration)]/*[pmd-java:nodeIs(&#34;TypeDeclaration&#34;)][1]"/>
      </properties>
      <example>

// no package declaration
public class ClassInDefaultPackage {
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#useunderscoresinnumericliterals"
         language="java"
         message="Number {0} should separate every third digit with an underscore"
         minimumLanguageVersion="1.7"
         name="UseUnderscoresInNumericLiterals"
         since="6.10.0">
      <description>
            Since Java 1.7, numeric literals can use underscores to separate digits. This rule enforces that
            numeric literals above a certain length use these underscores to increase readability.

            The rule only supports decimal (base 10) literals for now. The acceptable length under which literals
            are not required to have underscores is configurable via a property. Even under that length, underscores
            that are misplaced (not making groups of 3 digits) are reported.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//NumericLiteral&#xA; (: Filter out literals in base other than 10 :)&#xA; [@Base = 10]&#xA; (: Filter out ignored field name :)&#xA; [not(ancestor::VariableDeclarator[1][@Name = 'serialVersionUID'])]&#xA; [&#xA;   some $num in tokenize(@Image, &#34;[dDfFlLeE+\-]&#34;)&#xA;   satisfies not(&#xA;                  ( contains($num, &#34;.&#34;)&#xA;                    and string-length(substring-before($num, &#34;.&#34;)) &lt;= $acceptableDecimalLength&#xA;                    and string-length(substring-after($num, &#34;.&#34;)) &lt;= $acceptableDecimalLength&#xA;                    or string-length($num) &lt;= $acceptableDecimalLength&#xA;                  )&#xA;                  and not(contains($num,&#34;_&#34;))&#xA;                  or matches($num, &#34;^[0-9]{1,3}(_[0-9]{3})*(\.([0-9]{3}_)*[0-9]{1,3})?$&#34;)&#xA;                )&#xA; ]"/>
         <property description="Length under which literals in base 10 are not required to have underscores"
                   max="1000"
                   min="3"
                   name="acceptableDecimalLength"
                   type="Integer">
            <value>4</value>
         </property>
      </properties>
      <example>
            
public class Foo {
    private int num = 1000000; // should be 1_000_000
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.TypeParameterNamingConventionsRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#typeparameternamingconventions"
         language="java"
         message="The {0} name ''{1}'' doesn''t match ''{2}''"
         name="TypeParameterNamingConventions"
         since="7.17.0">
      <description>
            Configurable naming conventions for type parameters in generic types and methods.
            This rule reports type parameter declarations which do not match the configured regex.
            Type parameters can appear on classes, interfaces, enums, records, and methods.

            By default, this rule uses the standard Java naming convention (single uppercase letter).
        </description>
      <priority>4</priority>
      <example>

// Generic types - valid
public interface Repository&lt;T&gt; { }
public class Cache&lt;K, V&gt; { }

// Generic types - invalid
public interface Repository&lt;type&gt; { }      // lowercase
public class Cache&lt;KEY, VALUE&gt; { }         // multiple letters

// Generic methods - valid
public class Util {
    public static &lt;T&gt; T identity(T value) { return value; }
    public &lt;T, R&gt; R transform(T input, Function&lt;T, R&gt; mapper) { }
}

// Generic methods - invalid
public class Util {
    public static &lt;element&gt; element get(element value) { }  // lowercase
    public &lt;INPUT, OUTPUT&gt; OUTPUT convert(INPUT in) { }     // multiple letters
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.OnlyOneReturnRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#onlyonereturn"
         language="java"
         message="A method should have only one exit point, and that should be the last statement in the method"
         name="OnlyOneReturn"
         since="1.0">
      <description>
A method should have only one exit point, and that should be the last statement in the method.
        </description>
      <priority>3</priority>
      <example>

public class OneReturnOnly1 {
  public String foo(int x) {
    if (x &gt; 0) {
      return "hey";   // first exit
    }
    return "hi";    // second exit
  }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#packagecase"
         language="java"
         message="Package name contains upper case characters"
         name="PackageCase"
         since="3.3">
      <description>
Detects when a package definition contains uppercase characters.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath" value="//PackageDeclaration[lower-case(@Name) != @Name]"/>
      </properties>
      <example>

package com.MyCompany;  // should be lowercase name

public class SomeClass {
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.PrematureDeclarationRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#prematuredeclaration"
         language="java"
         message="Declaration of ''{0}'' can be moved closer to its usages"
         name="PrematureDeclaration"
         since="5.0">
      <description>
Checks for variables that are defined before they might be used. A declaration is
deemed to be premature if there are some statements that may return or throw an
exception between the time the variable is declared and the time it is first read.

Some variables cannot be declared close to their first usage because of side-effects
occurring before they're first used. We try to avoid reporting those by considering
most method and constructor invocations to be impure. See the second example.

Note that this rule is meant to improve code readability but is not an optimization.
A smart JIT will not care whether the variable is declared prematurely or not, as it
can reorder code.
        </description>
      <priority>3</priority>
      <example>

public int getLength(String[] strings) {

    int length = 0; // could be moved closer to the loop

    if (strings == null || strings.length == 0) return 0;

    for (String str : strings) {
        length += str.length();
    }

    return length;
}

        </example>
      <example>

public int getLength(String[] strings) {

    int startTime = System.nanoTime(); // cannot be moved because initializer is impure

    if (strings == null || strings.length == 0) {
        // some error logic
        throw new SomeException(...);
    }

    for (String str : strings) {
        length += str.length();
    }

    return System.nanoTime() - startTime;
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#remoteinterfacenamingconvention"
         language="java"
         message="Remote Interface of a Session EJB should NOT be suffixed"
         name="RemoteInterfaceNamingConvention"
         since="4.0">
      <description>
Remote Interface of a Session EJB should not have a suffix.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;[&#xA;    pmd-java:typeIs('jakarta.ejb.EJBObject')&#xA;    and matches(@SimpleName, '.*(Session|EJB|Bean)$')&#xA;]"/>
      </properties>
      <example>

/* Poor Session suffix */
public interface BadSuffixSession extends jakarta.ejb.EJBObject {}

/* Poor EJB suffix */
public interface BadSuffixEJB extends jakarta.ejb.EJBObject {}

/* Poor Bean suffix */
public interface BadSuffixBean extends jakarta.ejb.EJBObject {}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#remotesessioninterfacenamingconvention"
         language="java"
         message="Remote Home interface of a Session EJB should be suffixed by 'Home'"
         name="RemoteSessionInterfaceNamingConvention"
         since="4.0">
      <description>
A Remote Home interface type of a Session EJB should be suffixed by 'Home'.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration&#xA;[&#xA;    pmd-java:typeIs('jakarta.ejb.EJBHome')&#xA;    and not(ends-with(@SimpleName, 'Home'))&#xA;]"/>
      </properties>
      <example>

public interface MyBeautifulHome extends jakarta.ejb.EJBHome {}       // proper name

public interface MissingProperSuffix extends jakarta.ejb.EJBHome {}   // non-standard name

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#shortclassname"
         language="java"
         message="Avoid short class names like {0}"
         name="ShortClassName"
         since="5.0">
      <description>
Short Classnames with fewer than e.g. five characters are not recommended.
        </description>
      <priority>4</priority>
      <properties>
         <property name="xpath"
                   value="//ClassDeclaration[string-length(@SimpleName) &lt; $minimum]"/>
         <property description="Number of characters that are required as a minimum for a class name."
                   max="100"
                   min="1"
                   name="minimum"
                   type="Integer">
            <value>5</value>
         </property>
      </properties>
      <example>

public class Foo {
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#shortmethodname"
         language="java"
         message="Avoid using short method names"
         name="ShortMethodName"
         since="0.3">
      <description>
Method names that are very short are not helpful to the reader.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//MethodDeclaration[string-length(@Name) &lt; $minimum]"/>
         <property description="Number of characters that are required as a minimum for a method name."
                   max="100"
                   min="1"
                   name="minimum"
                   type="Integer">
            <value>3</value>
         </property>
      </properties>
      <example>

public class ShortMethod {
    public void a( int i ) { // Violation
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#shortvariable"
         language="java"
         message="Avoid variables with short names like {0}"
         name="ShortVariable"
         since="0.3">
      <description>
Fields, local variables, enum constant names or parameter names that are very short are not helpful to the reader.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//VariableId[string-length(@Name) &lt; $minimum]&#xA; (: ForStatement :)&#xA; [not(../../parent::ForInit)]&#xA; (: Foreach statement :)&#xA; [not(../../parent::ForeachStatement)]&#xA; (: Catch statement parameter :)&#xA; [not(parent::CatchParameter)]&#xA; (: Lambda expression parameter :)&#xA; [not(parent::LambdaParameter)]&#xA; (: Exclude Unnamed Variables (JEP 456) :)&#xA; [@Unnamed = false()]"/>
         <property description="Number of characters that are required as a minimum for a variable name."
                   max="100"
                   min="1"
                   name="minimum"
                   type="Integer">
            <value>3</value>
         </property>
      </properties>
      <example>

public class Something {
    private int q = 15;                         // field - too short
    public static void main( String as[] ) {    // formal arg - too short
        int r = 20 + q;                         // local var - too short
        for (int i = 0; i &lt; 10; i++) {          // not a violation (inside 'for' loop)
            r += q;
        }
        for (Integer i : numbers) {             // not a violation (inside 'for-each' loop)
            r += q;
        }
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#toomanystaticimports"
         language="java"
         message="Too many static imports may lead to messy code"
         name="TooManyStaticImports"
         since="4.1">
      <description>
If you overuse the static import feature, it can make your program unreadable and
unmaintainable, polluting its namespace with all the static members you import.
Readers of your code (including you, a few months after you wrote it) will not know
which class a static member comes from (Sun 1.5 Language Guide).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value=".[count(ImportDeclaration[@Static = true()]) &gt; $maximumStaticImports]"/>
         <property description="All static imports can be disallowed by setting this to 0"
                   max="100"
                   min="0"
                   name="maximumStaticImports"
                   type="Integer">
            <value>4</value>
         </property>
      </properties>
      <example>

import static Lennon;
import static Ringo;
import static George;
import static Paul;
import static Yoko; // Too much !

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryannotationvalueelement"
         language="java"
         message="Avoid the use of value in annotations when it's the only element"
         name="UnnecessaryAnnotationValueElement"
         since="6.2.0">
      <description>
            Avoid the use of value in annotations when it's the only element.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//Annotation/AnnotationMemberList[count(*) = 1 and MemberValuePair[@Shorthand = false() and @Name = 'value']]"/>
         <property description="If disabled, the rule shows also violations that are applicable for java8+"
                   name="java7Compatibility"
                   type="Boolean">
            <value>false</value>
         </property>
      </properties>
      <example>
            
@TestClassAnnotation(value = "TEST")
public class Foo {

    @TestMemberAnnotation(value = "TEST")
    private String y;

    @TestMethodAnnotation(value = "TEST")
    public void bar() {
        int x = 42;
        return;
    }
}

// should be

@TestClassAnnotation("TEST")
public class Foo {

    @TestMemberAnnotation("TEST")
    private String y;

    @TestMethodAnnotation("TEST")
    public void bar() {
        int x = 42;
        return;
    }
}


        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryBoxingRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryboxing"
         language="java"
         message="Unnecessary {0}"
         minimumLanguageVersion="1.5"
         name="UnnecessaryBoxing"
         since="7.0.0">
      <description>
            Reports explicit boxing and unboxing conversions that may safely be removed,
            either because they would be inserted by the compiler automatically,
            or because they're semantically a noop (eg unboxing a value to rebox it immediately).

            Note that this only handles boxing and unboxing conversions occurring through
            calls to `valueOf` or one of the `intValue`, `byteValue`, etc. methods. Casts
            that command a conversion are reported by {% rule UnnecessaryCast %} instead.
        </description>
      <priority>3</priority>
      <example>
{
        // Instead of
        Integer integer = Integer.valueOf(2);
        // you may just write
        Integer integer = 2;

        int i = integer.intValue(); // similarly for unboxing

        // Instead of
        int x = Integer.valueOf("42");
        // you may just write
        int x = Integer.parseInt("42");
}
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryCastRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessarycast"
         language="java"
         message="Unnecessary cast ({0})"
         minimumLanguageVersion="1.5"
         name="UnnecessaryCast"
         since="6.24.0">
      <description>
Detects casts which could be removed as the operand of the cast is already suitable
for the context type. For instance, in the following:
```
Object context = (Comparable) "o";
```
The cast is unnecessary. This is because `String` already is a subtype of both
`Comparable` and `Object`.

This will also flag casts that can be avoided because of the autoboxing feature of Java 5.
```
Integer integer = (Integer) 1;
```
The literal would be autoboxed to `Integer` anyway.
            </description>
      <priority>3</priority>
      <example>

import java.util.function.Function;
class SomeClass {
   static {
      Object o; long l; int i; Integer boxedInt;

      // reference conversions

      o = (Object) new SomeClass();      // unnecessary
      o = (SomeClass) o;                 // necessary (narrowing cast)
      o = (Comparable&lt;String&gt;) "string"; // unnecessary

      // primitive conversions

      l = (long) 2;   // unnecessary
      l = (long) 2.0; // necessary (narrowing cast)
      l = (byte) i;   // necessary (narrowing cast)

      // boxing/unboxing casts (since java 5)

      o = (Integer) 3;    // unnecessary (autoboxing would apply)
      o = (long) 3;       // necessary (would be boxed to Long)
      l = (int) boxedInt; // necessary (cannot cast Integer to long)

      // casts that give a target type to a lambda/ method ref are necessary

      o = (Function&lt;Integer, String&gt;) Integer::toString; // necessary (no target type)
   }
}

        </example>
      <example>

import java.util.*;
class SomeClass {
   static {
       /* Casts involving access to collections were common before Java 5, because collections
        * were not generic. This rule may hence be useful when converting from using a raw
        * type like `List` to a parameterized type like `List&lt;String&gt;`.
        */
       List&lt;String&gt; stringList = Arrays.asList("a", "b");
       String element = (String) stringList.get(0); // this cast is unnecessary
   }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryConstructorRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryconstructor"
         language="java"
         message="Avoid unnecessary constructors - the compiler will generate these for you"
         name="UnnecessaryConstructor"
         since="1.0">
      <description>
This rule detects when a constructor is not necessary; i.e., when there is only one constructor and the
constructor is identical to the default constructor. The default constructor should has same access
modifier as the declaring class. In an enum type, the default constructor is implicitly private.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
  public Foo() {}
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryFullyQualifiedNameRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryfullyqualifiedname"
         language="java"
         message="Unnecessary qualifier ''{0}'': ''{1}'' is already in scope{2}"
         name="UnnecessaryFullyQualifiedName"
         since="5.0">
      <description>
Import statements allow the use of non-fully qualified names.  The use of a fully qualified name
which is covered by an import statement is redundant.  Consider using the non-fully qualified name.
        </description>
      <priority>4</priority>
      <example>

import java.util.List;

public class Foo {
    private java.util.List list1;   // Unnecessary FQN
    private List list2;             // More appropriate given import of 'java.util.List'
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryImportRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryimport"
         language="java"
         message="Unnecessary import ''{0}''"
         name="UnnecessaryImport"
         since="6.34.0">
      <description>
            Reports import statements that can be removed. They are either unused,
            duplicated, or the members they import are already implicitly in scope,
            because they're in java.lang, or the current package.

            If some imports cannot be resolved, for instance because you run PMD with
            an incomplete auxiliary classpath, some imports may be conservatively marked
            as used even if they're not to avoid false positives.
        </description>
      <priority>4</priority>
      <example>

            import java.io.File;            // not used, can be removed
            import java.util.Collections;   // used below
            import java.util.*;             // so this one is not used

            import java.lang.Object;        // imports from java.lang, unnecessary
            import java.lang.Object;        // duplicate, unnecessary

            public class Foo {
                static Object emptyList() {
                    return Collections.emptyList();
                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryLocalBeforeReturnRule"
         deprecated="true"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessarylocalbeforereturn"
         language="java"
         message="Consider simply returning the value vs storing it in local variable ''{0}''"
         name="UnnecessaryLocalBeforeReturn"
         since="3.3">
      <description>
Avoid the creation of unnecessary local variables.

This rule has been deprecated since 7.17.0. Use the new rule
{%rule VariableCanBeInlined %} instead, which additionally covers throw statements.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
   public int foo() {
     int x = doSomething();
     return x;  // instead, just 'return doSomething();'
   }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryModifierRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessarymodifier"
         language="java"
         message="Unnecessary modifier{0} on {1} ''{2}''{3}"
         name="UnnecessaryModifier"
         since="1.02">
      <description>
Fields in interfaces and annotations are automatically `public static final`, and methods are `public abstract`.
Classes, interfaces or annotations nested in an interface or annotation are automatically `public static`
(all nested interfaces and annotations are automatically static).
Nested enums are automatically `static`.
For historical reasons, modifiers which are implied by the context are accepted by the compiler, but are superfluous.
        </description>
      <priority>3</priority>
      <example>

public @interface Annotation {
    public abstract void bar();     // both abstract and public are ignored by the compiler
    public static final int X = 0;  // public, static, and final all ignored
    public static class Bar {}      // public, static ignored
    public static interface Baz {}  // ditto
}
public interface Foo {
    public abstract void bar();     // both abstract and public are ignored by the compiler
    public static final int X = 0;  // public, static, and final all ignored
    public static class Bar {}      // public, static ignored
    public static interface Baz {}  // ditto
}
public class Bar {
    public static interface Baz {}  // static ignored
    public static enum FooBar {    // static ignored
        FOO;
    }
}
public class FooClass {
    static record BarRecord() {}     // static ignored
}
public interface FooInterface {
    static record BarRecord() {}     // static ignored
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UnnecessaryReturnRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessaryreturn"
         language="java"
         message="Unnecessary return statement"
         name="UnnecessaryReturn"
         since="1.3">
      <description>
Avoid the use of unnecessary return statements. A return is unnecessary when no
instructions follow anyway.
        </description>
      <priority>3</priority>
      <example>

public class Foo {
    public void bar() {
        int x = 42;
        return;
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#unnecessarysemicolon"
         language="java"
         message="Unnecessary semicolon"
         name="UnnecessarySemicolon"
         since="6.46.0">
      <description>
            Reports unnecessary semicolons (so called "empty statements" and "empty declarations").
            These can be removed without changing the program. The Java grammar
            allows them for historical reasons, but they should be avoided.
            
            This rule will not report empty statements that are syntactically 
            required, for instance, because they are the body of a control statement.

            This rule replaces EmptyStatementNotInLoop.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="(: empty declarations :)&#xA;      //EmptyDeclaration&#xA;      (: empty statements :)&#xA;    | //Block/EmptyStatement"/>
      </properties>
      <example>

class Foo {
    {
        toString();; // one of these semicolons is unnecessary
        if (true); // this semicolon is not unnecessary, but it could be an empty block instead (not reported)
    }
}; // this semicolon is unnecessary


        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UseDiamondOperatorRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#usediamondoperator"
         language="java"
         message="Explicit type arguments can be replaced by a diamond: `{0}`"
         minimumLanguageVersion="1.7"
         name="UseDiamondOperator"
         since="6.11.0">
      <description>
In some cases, explicit type arguments in a constructor call for a generic type
may be replaced by diamond type arguments (`&lt;&gt;`), and be inferred by the compiler.
This rule recommends that you use diamond type arguments anywhere possible, since
it avoids duplication of the type arguments, and makes the code more concise and readable.

This rule is useful when upgrading a codebase to Java 1.7, Java 1.8, or Java 9.
The diamond syntax was first introduced in Java 1.7. In Java 8, improvements in Java's
type inference made more type arguments redundant. In Java 9, type arguments inference
was made possible for anonymous class constructors.
            </description>
      <priority>3</priority>
      <example>
            
            import java.util.*;
            class Foo {
                static {
                    List&lt;String&gt; strings;
                    strings = new ArrayList&lt;String&gt;(); // unnecessary duplication of type parameters
                    strings = new ArrayList&lt;&gt;();       // using diamond type arguments is more concise

                    strings = new ArrayList(); // accidental use of a raw type, you can use ArrayList&lt;&gt; instead

                    strings = new ArrayList&lt;&gt;() {
                        // for anonymous classes, this is possible since Java 9 only
                    };
                }
            }
            
        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#useexplicittypes"
         language="java"
         message="Use Explicit Types"
         minimumLanguageVersion="10"
         name="UseExplicitTypes"
         since="7.0.0">
      <description>
Java 10 introduced the `var` keyword. This reduces the amount of code written because java can infer the type
from the initializer of the variable declaration.

This is essentially a trade-off: On the one hand, it can make code more readable by eliminating redundant
information. On the other hand, it can make code less readable by eliding useful information. There is no
blanket rule for when `var` should be used or shouldn't.

It may make sense to use `var` when the type is inherently clear upon reading the statement
(ie: assignment to either a literal value or a constructor call). Those use cases
can be enabled through properties.

Notice that lambda parameters are allowed, as they are already inferred  by default (the `var` keyword
is completely optional).

See also [Local Variable Type Inference Style Guidelines](https://openjdk.org/projects/amber/guides/lvti-style-guide).
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//LocalVariableDeclaration[@TypeInferred = true()]&#xA;    [not(VariableDeclarator[*[pmd-java:nodeIs(&#34;Literal&#34;)]]) or $allowLiterals = false()]&#xA;    [not(VariableDeclarator[ConstructorCall]) or $allowCtors = false()]&#xA;    [not(VariableDeclarator[CastExpression]) or $allowCasts = false()]&#xA;    [not(parent::ForeachStatement) or $allowLoopVariable = false()]"/>
         <property description="Allow when variables are directly initialized with literals"
                   name="allowLiterals"
                   type="Boolean">
            <value>false</value>
         </property>
         <property description="Allow when variables are directly initialized with a constructor call"
                   name="allowCtors"
                   type="Boolean">
            <value>false</value>
         </property>
         <property description="Allow when variables are directly initialized with a the result of a cast"
                   name="allowCasts"
                   type="Boolean">
            <value>false</value>
         </property>
         <property description="Allow when variables are used as loop variables in enhanced for loops"
                   name="allowLoopVariable"
                   type="Boolean">
            <value>false</value>
         </property>
      </properties>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.UselessParenthesesRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#uselessparentheses"
         language="java"
         message="Useless parentheses."
         name="UselessParentheses"
         since="5.0">
      <description>
            Parenthesized expressions are used to override the default operator precedence
            rules. Parentheses whose removal would not change the relative nesting of operators
            are unnecessary, because they don't change the semantics of the enclosing expression.

            Some parentheses that strictly speaking are unnecessary, may still be considered useful
            for readability. This rule allows to ignore violations on two kinds of unnecessary parentheses:
            - "Clarifying" parentheses, which separate operators of difference precedence. While
            unnecessary, they make precedence rules explicit, which may be useful for rarely used
            operators. For example:
            ```java
                (a + b) &amp; c // is equivalent to `a + b &amp; c`, but probably clearer
            ```
            Unset the property `ignoreClarifying` to report them.

            - "Balancing" parentheses, which are unnecessary but visually balance out another pair
            of parentheses around an equality operator. For example, those two expressions are equivalent:
            ```java
                (a == null) != (b == null)
                a == null != (b == null)
            ```
            The parentheses on the right are required, and the parentheses on the left are
            just more visually pleasing. Unset the property `ignoreBalancing` to report them.

        </description>
      <priority>4</priority>
      <example>

public class Foo {
    {
        int n = 0;
        n = (n);         // here
        n = (n * 2) * 3; // and here
        n = n * (2 * 3); // and here
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#uselessqualifiedthis"
         language="java"
         message="Useless qualified this usage in the same class."
         name="UselessQualifiedThis"
         since="5.4.0">
      <description>
            Reports qualified this usages in the same class.
        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//ThisExpression/ClassType&#xA;[ ancestor::*[pmd-java:nodeIs('TypeDeclaration')][1]/@SimpleName = ./@SimpleName ]"/>
      </properties>
      <example>

public class Foo {
    final Foo otherFoo = Foo.this;  // use "this" directly

    public void doSomething() {
         final Foo anotherFoo = Foo.this;  // use "this" directly
    }

    private ActionListener returnListener() {
        return new ActionListener() {
            @Override
            public void actionPerformed(ActionEvent e) {
                doSomethingWithQualifiedThis(Foo.this);  // This is fine
            }
        };
    }

    private class Foo3 {
        final Foo myFoo = Foo.this;  // This is fine
    }

    private class Foo2 {
        final Foo2 myFoo2 = Foo2.this;  // Use "this" directly
    }
}

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.rule.xpath.XPathRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#useshortarrayinitializer"
         language="java"
         message="Array initialization can be written shorter"
         name="UseShortArrayInitializer"
         since="6.15.0">
      <description>

When declaring and initializing array fields or variables, it is not necessary to explicitly create a new array
using `new`. Instead one can simply define the initial content of the array as a expression in curly braces.

E.g. `int[] x = new int[] { 1, 2, 3 };` can be written as `int[] x = { 1, 2, 3 };`.

        </description>
      <priority>3</priority>
      <properties>
         <property name="xpath"
                   value="//VariableDeclarator&#xA;    [VariableId[@TypeInferred = false() and @ArrayType = true()]]&#xA;    [ArrayAllocation/ArrayInitializer]"/>
      </properties>
      <example>

Foo[] x = new Foo[] { ... }; // Overly verbose
Foo[] x = { ... }; //Equivalent to above line

        </example>
   </rule>
   <rule class="net.sourceforge.pmd.lang.java.rule.codestyle.VariableCanBeInlinedRule"
         deprecated="false"
         externalInfoUrl="https://docs.pmd-code.org/pmd-doc-7.17.0/pmd_rules_java_codestyle.html#variablecanbeinlined"
         language="java"
         message="Consider simply using the value vs. storing it in local variable ''{0}''."
         name="VariableCanBeInlined"
         since="7.17.0">
      <description>
            Local variables should not be declared and then immediately returned or thrown. Such
            variable declarations add unnecessary complexity and make the code harder to read.
            It is often simpler and cleaner to return or throw the value directly.

            This rule implements SonarSource rule [S1488](https://sonarsource.github.io/rspec/#/rspec/S1488).
        </description>
      <priority>3</priority>
      <example>
            
                class Foo {
                    Object foo() {
                        var foo = "foo";
                        return foo;  // instead, just 'return "foo";'
                    }

                    Object bar() {
                        var ex = getIllegalArgumentException();
                        throw ex; // instead, just 'throw getIllegalArgumentException();'
                    }

                    Object baz() {
                        var baz = switch (foo()) {
                            case "foo" -&gt; {
                                var foo = foo();
                                yield foo;  // Can be simplified to 'yield foo();'
                            }
                            case "bar" -&gt; {
                                var bar = bar();
                                yield bar;  // Can be simplified to 'yield bar();'
                            }
                            default -&gt; bar("baz");
                        };
                        return baz; // instead, just 'return switch (foo()) {...'
                    }
                }
            
        </example>
   </rule>
</ruleset>
